{
  "name": "vue-compiler",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "test": "cargo test --workspace",
    "test:napi": "cargo xtask verify-napi",
    "conformance": "cargo xtask run-conformance --all"
  }
}
