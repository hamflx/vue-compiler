# @vuec-rs/native-darwin-arm64

Optional native binding package for `@vuec-rs/native` on macOS arm64.

Users should install `@vuec-rs/native`; the loader selects this package automatically when the current platform matches.
