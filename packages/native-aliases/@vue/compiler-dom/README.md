# @vue/compiler-dom NAPI alias

Compatibility package that exposes the locked `@vue/compiler-dom` package name through the Rust-backed `@vuec-rs/native` bridge.

This package is used by conformance and package-alias verification. Coverage reports still distinguish `rust-backed`, `mixed`, and `shim-backed` behavior.
