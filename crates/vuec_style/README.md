# vuec_style

Vue SFC style compilation, scoped CSS, and CSS variable helpers for vuec.

This crate is part of the `vue-compiler` workspace and supports Vue 2.7 and Vue 3 SFC style compilation paths.
