# vuec_napi

NAPI binding crate for the vuec Rust Vue compiler.

This crate is packaged through the npm `@vuec-rs/native` loader and platform packages. It is marked `publish = false` for crates.io because Node distribution happens through npm artifacts.
