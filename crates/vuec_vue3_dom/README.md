# vuec_vue3_dom

Vue 3 compiler-dom facade and DOM transform implementation for vuec.

This crate builds on `vuec_vue3_core` and implements DOM parser defaults, DOM transforms, public compile/parse entrypoints, AST caching, and output contracts.
