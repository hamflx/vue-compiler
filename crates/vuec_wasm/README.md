# vuec_wasm

WASM binding crate for the vuec Rust Vue compiler.

This crate is packaged through the npm `@vuec-rs/wasm` package and wasm-bindgen output. It is marked `publish = false` for crates.io because browser and Node distribution happens through npm artifacts.
