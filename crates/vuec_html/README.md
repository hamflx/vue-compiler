# vuec_html

HTML tokenizer and template parsing primitives for the vuec Rust Vue compiler.

This crate is part of the `vue-compiler` workspace. Higher-level Vue 2 and Vue 3 parser crates build their public projections on top of these primitives.
