# vuec_node_bridge

Internal JSON bridge binary used by vuec conformance tooling.

This crate is marked `publish = false` and is not a public crates.io API. It supports generated JavaScript package aliases and conformance runners.
