# vuec_vue3_core

Vue 3 compiler-core parser, transform, and codegen implementation for vuec.

This crate owns the Vue 3 core compilation pipeline and the Rust-backed projections used by compiler-core, compiler-dom, compiler-sfc, and compiler-ssr compatibility layers.
