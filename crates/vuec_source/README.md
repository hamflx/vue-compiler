# vuec_source

Source text, source identity, and span types shared across the vuec Rust Vue compiler.

This crate is part of the `vue-compiler` workspace. The compiler architecture is documented in the repository root and the AST/HIR/MIR contract is governed by `docs/3.AST_HIR_MIR_DESIGN.md`.
