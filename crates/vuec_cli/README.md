# vuec_cli

Command-line interface for the vuec Rust Vue compiler.

The published binary is `vuec`. It exposes template, SFC, SSR, parser, conformance wrapper, and benchmark smoke commands backed by the workspace compiler crates.
