# Memory

- Current round: added Vue 3 DOM MIR native `v-bind` modifier payload and standalone codegen consumption.
- `HirBinding` now preserves source `v-bind` modifiers, while `Vue3DomBinding` projects DOM-target `.camel`, `.prop`, and `.attr` flags. HIR does not carry runtime helpers or DOM prop/attr prefix decisions.
- `generate_vue3_dom_mir` now consumes this payload directly, emitting static camelized keys, dynamic `_camelize(...)` keys, `.` / `^` DOM prop key prefixes, `.prop` hydration metadata, and FULL_PROPS dynamic-prop suppression from DOM MIR plus `JsAstStore`. SSR codegen keeps `.prop` / `.attr` DOM prefixes out of SSR attrs while still supporting `.camel` key normalization.
- Verification for this DOM MIR `v-bind` modifier slice: focused `cargo check -p vuec_ast -p vuec_vue3_core`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_v_bind_modifier_payloads`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_v_bind_modifier_payloads_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_marks_static_prop_modifier_for_hydration`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_ignores_dom_bind_prefix_modifiers`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, focused `cargo test -p vuec_vue3_core v_bind`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`, mixed official path, regression evidence), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, mixed official path, regression evidence).

- Current round: added Vue 3 DOM MIR native `v-on` modifier payload and standalone codegen consumption.
- `Vue3DomEvent` now carries DOM-target event modifier classification: `runtime_modifiers` for `_withModifiers`, `key_modifiers` for `_withKeys`, `option_modifiers` for `.capture` / `.once` / `.passive` event prop postfixes, and `click_event` for `.right` / `.middle` click remapping. HIR still only preserves the source directive modifiers; helper/runtime decisions stay in `Vue3DomMir`.
- `generate_vue3_dom_mir` now consumes these event fields directly, including cached guarded handlers, static/dynamic event names, dynamic click remapping, and helper import discovery. This is structural DOM MIR lowering/codegen work only; it does not touch `xtask/src/compat.rs`, does not change the legacy official DOM compile path, and does not count alias/runtime `vOn.spec.ts` support as standalone MIR completion.
- Verification for this DOM MIR `v-on` modifier slice: focused `cargo check -p vuec_ast -p vuec_vue3_core`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_v_on_modifier_payloads`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_v_on_modifier_payloads_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_caches_v_on_modifier_handler_from_mir`, focused `cargo test -p vuec_vue3_core v_on`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`, mixed official path, regression evidence), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, mixed official path, regression evidence).

- Current round: added Vue 3 DOM MIR native `v-model` payload and standalone codegen consumption.
- `Vue3VNodeCall` now carries `models: Vec<Vue3DomModel>`, and `Vue3DomPropSegment::Model` preserves source-ordered `onUpdate:modelValue` update props around object `v-bind`. The payload records `Text` / `Radio` / `Checkbox` / `Select` / `Dynamic` runtime helper kind, model expression `JsExprId`, and model modifiers.
- `generate_vue3_dom_mir` now consumes this payload directly, emitting `_withDirectives` with `_vModelText` / `_vModelRadio` / `_vModelCheckbox` / `_vModelSelect` / `_vModelDynamic`, model assignment props, and modifier objects from DOM MIR plus `JsAstStore`. This is structural DOM MIR lowering/codegen work only; it does not touch `xtask/src/compat.rs`, does not change the legacy official DOM compile path, and does not count the earlier mixed official `vModel.spec.ts` support as standalone MIR completion.
- Verification for this DOM MIR `v-model` slice: focused `cargo check -p vuec_ast -p vuec_vue3_core`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_native_v_model_payloads`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_native_v_model_payloads_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_merges_native_v_model_with_object_bindings`, focused `cargo test -p vuec_vue3_core v_model`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`, mixed official path, regression evidence), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, mixed official path, regression evidence).

- Current round: added Vue 3 DOM MIR `v-html` / `v-text` content override payload and standalone codegen consumption.
- `Vue3VNodeCall` now carries `content: Option<Vue3DomContent>`, and `Vue3DomPropSegment::Content` preserves source-ordered `innerHTML` / `textContent` props around object `v-bind`. DOM lowering skips original children for content override nodes, records `innerHTML` / `textContent` in `dynamic_props`, and sets `PROPS` patch flag through the target payload.
- `generate_vue3_dom_mir` now consumes this payload directly. `v-html` emits `innerHTML`, while `v-text` emits `textContent` and wraps dynamic values in `_toDisplayString(...)`; literal/static text expressions and missing expressions stay direct/empty. This is structural DOM MIR lowering/codegen work only and does not touch `xtask/src/compat.rs`.
- Verification for this DOM MIR content override slice: focused `cargo test -p vuec_vue3_core content_override`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`, mixed official path, regression evidence), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, mixed official path, regression evidence).

- Current round: added Vue 3 SSR MIR `v-html` / `v-text` content override payload and standalone codegen consumption.
- `Vue3SsrMirKind::RenderContent(Vue3SsrContent)` now stores native SSR children override target data. `Html { expression }` renders raw `expression ?? ""`; `Text { expression }` renders `_ssrInterpolate(expression)`. SSR lowering skips original children for these directives and expands self-closing elements into open/content/close MIR output.
- This is structural SSR MIR lowering/codegen work only. It does not touch `xtask/src/compat.rs` and does not change the legacy official `compile_ssr` path.
- Verification for this SSR MIR content override slice: focused `cargo test -p vuec_vue3_core content_override`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, existing mixed official path, not counted as standalone MIR completion by itself).

- Current round: added Vue 3 SSR MIR `v-model` target payload and standalone codegen consumption.
- `Vue3SsrAttrs` now carries `v_model: Option<Vue3SsrModel>`. The payload covers input value/radio/checkbox/dynamic type/object `v-bind`, textarea interpolation replacement, and select descendant option `selected` fragments. `MirExpr::Null` is now available for SSR model value fallback such as checkbox/dynamic-type inputs with no explicit value.
- SSR lowering records native `v-model` expression/value/type/true-value data into `Vue3SsrMir` and threads select model context to descendant `option` nodes, including options under `v-for`; standalone `generate_vue3_ssr_mir` consumes only SSR MIR + `JsAstStore` to emit `_ssrRenderAttr`, `_ssrIncludeBooleanAttr`, `_ssrLooseEqual`, `_ssrLooseContain`, `_ssrRenderDynamicModel`, and `_ssrGetDynamicModelProps` based output.
- This is structural SSR MIR lowering/codegen work only. It does not touch `xtask/src/compat.rs` and does not change the legacy official `compile_ssr` path.
- Verification for this SSR MIR `v-model` slice: focused `cargo check -p vuec_ast -p vuec_vue3_core`, focused `cargo test -p vuec_vue3_core v_model`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, existing mixed official path, not counted as standalone MIR completion by itself).

- Current round: added Vue 3 SSR MIR `v-show` style payload and standalone codegen consumption.
- `Vue3SsrAttrs` now carries `v_show: Option<JsExprId>` in addition to filtered props. SSR lowering reuses the HIR directive expression for `v-show`; when present, static `style` is removed from the opening-tag `PushString` and carried in the attrs payload so it can be merged with dynamic `:style` and the SSR-only `condition ? null : { display: "none" }` style.
- `generate_vue3_ssr_mir` now emits v-show style output from SSR MIR: direct `_ssrRenderStyle([...])` for normal attrs and `_ssrRenderAttrs(_mergeProps(objectBind, { style: [...] }))` when object `v-bind` can overwrite attrs. Static style parsing is local to the SSR target path and does not mutate AST/HIR.
- This is structural SSR MIR lowering/codegen work only. It does not touch `xtask/src/compat.rs` and does not change the legacy official `compile_ssr` path.
- Verification for this SSR MIR `v-show` style payload slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_projects_v_show_style_payload`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_emits_v_show_style_payload_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_merges_v_show_style_after_object_bindings`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, existing mixed official path, not counted as standalone MIR completion by itself).

- Current round: added Vue 3 SSR MIR built-in Teleport/Suspense payloads and standalone codegen consumption.
- `Vue3SsrMirKind::Teleport` now carries `Vue3SsrTeleport { target, disabled }`; SSR lowering extracts static/dynamic `to` and static/dynamic `disabled` props into `MirExpr`, and standalone SSR MIR codegen emits `_ssrRenderTeleport(_push, contentFn, target, disabled, _parent)` from that payload instead of treating Teleport as a normal component or hard-coding target/disabled.
- `Vue3SsrMirKind::Suspense` now carries `Vue3SsrSuspense { slots }`; SSR lowering projects stable implicit/explicit `default` and `fallback` slots into `Vue3DomSlots`, and standalone SSR MIR codegen emits `_ssrRenderSuspense(_push, { ...slots, _: flag })` from MIR payload.
- This is structural SSR MIR lowering/codegen work only. It does not touch `xtask/src/compat.rs` and does not change the legacy official `compile_ssr` path.
- Verification for this SSR MIR built-in component payload slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_projects_builtin_component_payloads`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_emits_builtin_component_payloads_from_mir`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`).

- Current round: added Vue 3 SSR MIR `v-for` alias payload and standalone codegen consumption.
- `Vue3SsrMirKind::For` now carries `Vue3SsrFor { source, value_alias, key_alias, index_alias }`, mirroring the HIR `v-for` alias projection for SSR target data without DOM MIR or AST fallback.
- `generate_vue3_ssr_mir` now emits `_ssrRenderList(source, (value, key, index) => ...)` from SSR MIR and treats all alias patterns as loop-local scope when prefixing expressions, so `key`, `index`, and destructured aliases are not rewritten as `_ctx.*`.
- This is structural SSR MIR lowering/codegen work only. It does not touch `xtask/src/compat.rs` and does not change the legacy official `compile_ssr` path.
- Verification for this SSR MIR `v-for` alias payload slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_projects_v_for_alias_payload`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_emits_v_for_alias_payload_from_mir`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`).

- Current round: fixed Vue 3 SSR MIR adjacent `v-if` / `v-else-if` / `v-else` alternate-chain lowering/codegen.
- SSR branch-chain lowering now attaches each alternate branch under the previous branch MIR node instead of attaching every alternate directly under the first `If`. The standalone SSR MIR emitter's primary-branch + alternate-child logic now emits `if/else if/else` bodies in source order without rendering later branch bodies inside the first branch.
- This is structural SSR MIR lowering/codegen work only. It does not touch `xtask/src/compat.rs` and does not change the legacy official `compile_ssr` path.
- Verification for this SSR MIR if/else alternate-chain slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_groups_if_else_branch_chains`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_emits_control_flow_and_components_from_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`).

- Current round: added Vue 3 SSR MIR component props payload and standalone component props codegen.
- `Vue3SsrMirKind::RenderComponent` now carries `Vue3SsrComponent { tag, props }`. SSR lowering projects component props from HIR into `Vue3DomProps` payload, including static attrs, dynamic bindings, object `v-bind`, object listeners, and ordered prop segments where present.
- `generate_vue3_ssr_mir` now emits the `_ssrRenderComponent` second argument from SSR MIR props using the existing structured props renderer (`_mergeProps`, `_normalizeProps`, `_guardReactiveProps`, `_toHandlers`, dynamic keys, and scoped JS-store rewrites) instead of hard-coding `null` or reading `Vue3Ast`.
- Verification for this SSR MIR component props slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_keeps_component_and_slot_target_split`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_emits_control_flow_and_components_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`).

- Current round: added Vue 3 SSR MIR attrs payload and standalone attrs codegen.
- `Vue3SsrMirKind::RenderAttrs` now carries `Vue3SsrAttrs { props: Vue3DomProps }`, filtered from HIR props for SSR native element dynamic attrs. Static attrs remain in opening-tag `PushString`; dynamic class/style/static-name/dynamic-name bindings and object `v-bind` are stored in SSR MIR payload.
- `generate_vue3_ssr_mir` now emits `_ssrRenderClass`, `_ssrRenderStyle`, `_ssrRenderAttr`, `_ssrRenderDynamicAttr`, and `_ssrRenderAttrs` from SSR MIR + `JsAstStore`, without reading DOM MIR or `Vue3Ast` and without misrouting `_attrs` as element attrs. Events/object listeners remain excluded from SSR element attrs payload.
- Verification for this SSR MIR attrs payload/codegen slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_records_target_split_edges_and_js_store`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir_emits_attrs_payload_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`).

- Current round: added standalone Vue 3 SSR MIR codegen foundation.
- `generate_vue3_ssr_mir` now consumes only `Vue3SsrMir` plus `JsAstStore` and emits SSR render function/module shells, `_push(...)`, `_ssrInterpolate(...)`, `_ssrRenderSlot(...)`, `_ssrRenderComponent(...)`, `_ssrRenderList(...)`, and statement `v-if` / `v-for` bodies from MIR. Slot outlet codegen uses `Vue3SsrSlot` name/props/fallback payload directly and preserves `v-for` aliases while prefixing outer expressions.
- This is structural target-codegen work and does not touch `xtask/src/compat.rs` or connect the official `compile_ssr` path. `RenderAttrs` remains a deliberate no-op marker until SSR attrs payload/codegen is added, so the emitter does not misroute `_attrs` as element attrs.
- Verification for this SSR MIR codegen foundation slice: focused `cargo test -p vuec_vue3_core generate_vue3_ssr_mir`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, `cargo fmt --all --check`, `git diff --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`).

- Current round: added `<slot>` outlet name / props / fallback payloads to structural Vue 3 SSR MIR.
- `Vue3SsrMirKind::RenderSlot` now carries `Vue3SsrSlot`, including static or dynamic `Vue3DomSlotName`, non-name `Vue3DomProps`, and fallback child node IDs in the same `Vue3SsrMir` document. SSR lowering filters the `name` prop out of outlet props and keeps fallback children as SSR MIR children.
- This is structural target-lowering work and does not derive SSR from DOM MIR or touch `xtask/src/compat.rs`. Exact SSR codegen remains separate from this target-split MIR payload slice.
- Verification for this SSR MIR slot-outlet payload slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_projects_slot_outlet_payload`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir_keeps_component_and_slot_target_split`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`), and `git diff --check`.

- Current round: added `<slot>` outlet name / props / fallback payloads to structural Vue 3 DOM MIR.
- `Vue3DomMirKind::RenderSlot` now carries `Vue3RenderSlot`, including static or dynamic `Vue3DomSlotName`, non-name `Vue3DomProps`, and fallback child node IDs in the same `Vue3DomMir` document. DOM lowering filters the `name` prop out of outlet props after using it as the render-slot target.
- `generate_vue3_dom_mir` now emits `_renderSlot(...)` name, props, and fallback function from MIR plus `JsAstStore`, including dynamic slot names and prefixed fallback interpolation expressions, without reading `Vue3Ast`.
- This is structural target-codegen work and does not touch `xtask/src/compat.rs`. Exact compiler-core codegen migration to consume complete `Vue3DomMir` remains open.
- Verification for this DOM MIR slot-outlet payload slice: focused `cargo test -p vuec_vue3_core slot_outlet_payload`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_keeps_slot_outlet_target_split`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`), and `git diff --check`.

- Current round: added dynamic component slot `v-if` / `v-else-if` / `v-else` alternate chains to structural Vue 3 DOM MIR.
- `Vue3DomConditionalSlot` now carries `alternate: Option<Box<Vue3DomDynamicSlot>>`. DOM lowering groups adjacent dynamic slot branch chains into one `dynamic_slots` entry, stores branch slot objects and generated keys in MIR, and keeps condition expressions in `JsAstStore` in source order.
- `generate_vue3_dom_mir` now renders nested dynamic slot alternates from MIR, so standalone codegen no longer has to treat `v-else-if` / `v-else` slot templates as independent dynamic slots or recover sibling branches from `Vue3Ast`.
- This is structural target-codegen work and does not touch `xtask/src/compat.rs`. Exact compiler-core codegen migration to consume complete `Vue3DomMir` remains open.
- Verification for this DOM MIR dynamic-slot alternate slice: focused `cargo test -p vuec_vue3_core dynamic_slot_if_else`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_dynamic_component_slots`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_dynamic_component_slots_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`), and `git diff --check`.

- Current round: added mode-aware function/module preamble generation to standalone Vue 3 DOM MIR codegen.
- `generate_vue3_dom_mir` now emits a target-codegen shell that matches the selected compiler mode: module mode keeps Vue helper imports and `export function render(...)`, prefix function mode emits top-level `const { helper: _helper } = Vue` plus `return function render(...)`, and non-prefix function mode emits `_Vue`, wraps render body in `with (_ctx)`, and declares render helpers inside that with block.
- Hoist declarations remain generated from `Vue3DomMirKind::Hoisted` children. In non-prefix function mode, static hoist helpers are destructured from `_Vue` before hoist declarations so render-external hoists can call `_createElementVNode` / related helpers without AST fallback scans.
- This is structural target-codegen work and does not touch `xtask/src/compat.rs`. Exact compiler-core codegen migration to consume `Vue3DomMir` remains open.
- Verification for this DOM MIR preamble shell slice: focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_component_tags_from_mir`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_imports_helpers_from_binding_rewrites`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_stable_component_slots_from_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: closed the Vue 3 public projection exactness stage item for directive `arg` / `exp` / `modifiers`.
- Added Rust bridge public-projection regression tests that parse `v-bind:[foo].camel="  bar  "` and `.foo="bar"` through `vue3_parse_value`, then assert the public JSON preserves dynamic arg raw bracket loc (`[foo]`) while keeping `arg.content = "foo"` / `isStatic = false`, exact directive expression content and loc including spaces, normal modifier loc/staticness, and `.prop` shorthand's synthetic non-static empty-loc modifier shape.
- This is validation of the Rust parser / Node bridge public projection contract, not alias-runtime compiler semantics. `xtask/src/compat.rs` was not touched.
- Verification for this directive public-projection exactness closure: focused `cargo test -p vuec_node_bridge vue3_directive_projection`, `cargo fmt --all --check`, `cargo check -p vuec_node_bridge -p vuec_vue3_core -p xtask`, `cargo test -p vuec_node_bridge -p vuec_vue3_core`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: added runtime directive dynamic argument payloads to Vue 3 HIR and DOM MIR.
- `HirDirectiveUse` and `Vue3DomDirective` now distinguish static `argument: Option<String>` from dynamic `dynamic_argument: Option<JsExprId>`. DOM lowering registers `v-focus:[arg]` style arguments once in `JsAstStore`, carries the expression id through HIR -> `Vue3DomMir`, and keeps static args as quoted string payloads.
- `generate_vue3_dom_mir` now renders dynamic runtime directive args with the scoped JS expression renderer, so prefix/module mode emits `[_directive_focus, _ctx.value, _ctx.arg, ...]` instead of quoting `"arg"`. This is structural target-codegen work and does not touch `xtask/src/compat.rs`; exact MIR-driven DOM codegen migration remains open.
- Verification for this runtime directive dynamic-arg slice: `cargo fmt --all`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_preserves_dynamic_runtime_directive_args`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_runtime_directives_from_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: added MIR-driven hoist declarations to standalone Vue 3 DOM MIR codegen.
- `generate_vue3_dom_mir` now renders each `Vue3DomMirKind::Hoisted { index }` wrapper's child body into `const _hoisted_{index} = ...` before the render function, and keeps normal render output as `_hoisted_n` references. The helper probe includes hoist declarations so hoisted VNodes can import `_createElementVNode` / related helpers in module mode.
- This closes the previous dangling hoist-reference gap in the structural standalone emitter. It is still conservative target-codegen work: exact official `cacheStatic` / `stringifyStatic` parity and full MIR-driven exact codegen migration remain open.
- Verification for this DOM MIR hoist declaration slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_static_hoist_wrappers`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_cache_and_hoist_wrappers`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: tightened standalone Vue 3 DOM MIR helper discovery for helpers introduced by prefix/bindingMetadata rewrites.
- `generate_vue3_dom_mir` now renders the DOM MIR body before finalizing module imports, then merges structural MIR helpers with helpers found in the generated declaration/body code. This covers `_unref` and `_isRef` introduced by inline `setup-maybe-ref` / `setup-let` rewrites, while preserving target-split MIR ownership for helpers such as `_withMemo`, `_renderList`, `_createSlots`, `_toHandlers`, and runtime directives.
- The shared helper order now includes the current Vue 3 DOM helper set used by code scanning. Inline `setup-let` reads in the lightweight codegen rewriter now use `_unref(...)`, and assignment handlers can emit `_isRef(x) ? x.value = ... : x` without leaving the helper import behind. This is Rust codegen work and does not touch `xtask/src/compat.rs`.
- Verification for this DOM MIR binding helper slice: focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, focused `cargo test -p vuec_vue3_core base_compile_uses_binding_metadata_for_prefixed_interpolations`, focused `cargo test -p vuec_vue3_core process_expression_projection_rewrites_setup_let_assignment_rhs`, `cargo test -p vuec_vue3_core`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: added prefix-identifier rewriting to standalone structural Vue 3 DOM MIR codegen. `generate_vue3_dom_mir` now rewrites registered `JsAstStore` expressions/statements through the existing Rust prefix helpers when `prefixIdentifiers` or module mode is active.
- `Vue3DomMirCodegen` now threads a `RenderScope` through recursive node/prop/slot/loop rendering. DOM MIR codegen preserves `v-for` aliases, destructured loop locals, stable/dynamic slot params, dynamic-slot `v-for` aliases, `$event`, and arrow handler params as locals while prefixing external reads such as `_ctx.msg`, `_ctx.list`, dynamic slot names, directive expressions, dynamic props, object listeners, dynamic components, and cached event handlers.
- This is structural target-codegen work and does not touch `xtask/src/compat.rs`. The exact AST emitter remains the official Vue 3 conformance path until complete MIR-driven exact codegen migration, but the previous standalone DOM MIR raw-expression gap is closed for the current MIR subset.
- Verification for this DOM MIR prefixing slice: focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, focused `cargo test -p vuec_vue3_core process_expression_projection_keeps_arrow_params_scoped_to_arrow_body`, focused `cargo test -p vuec_vue3_core base_compile_rewrites_event_handler_statement_scopes`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: added structural Vue 3 DOM MIR forwarded slot flag projection. Stable component slots now carry `Vue3SlotFlag::Forwarded` when slot bodies forward a `<slot>` outlet, keeping the slot flag as MIR target data.
- `generate_vue3_dom_mir` now reaches forwarded slot output through MIR lowering and emits `_: 3` together with child `_renderSlot(...)` nodes. This does not touch `xtask/src/compat.rs`; it is Rust structural AST/HIR/MIR target-codegen work.
- Verification for this forwarded-slot MIR slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_forwarded_component_slot_flag`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_forwarded_component_slot_flag`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`).

- Current round: added structural Vue 3 DOM MIR cacheHandlers event-cache payload. `vuec_ast::Vue3DomEvent` now carries optional `Vue3DomEventCache`, keeping cache slot indexes in the DOM target instead of HIR.
- `vuec_vue3_core` DOM lowering now assigns cacheHandlers slots during HIR props -> `Vue3DomMir` projection, skipping component handlers, dynamic event names, and `v-once` bodies. `generate_vue3_dom_mir` consumes the MIR payload to emit `_cache[n] || (_cache[n] = handler)`.
- This is structural target-codegen work. Standalone DOM MIR codegen still renders registered JS store expressions as raw source, and the exact AST emitter remains the official Vue 3 conformance path until complete MIR-driven exact codegen migration.
- Verification for this cacheHandlers MIR slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_cache_handlers_event_slots`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_cache_handlers_from_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: added `v-for` memo cache target payloads to structural Vue 3 DOM MIR.
- `Vue3DomMirKind::For` now carries `Vue3ForMir`, including source, value/key/index aliases, explicit key expression/string, and optional `Vue3ForMemo { expression, index }`. DOM lowering records the key/memo target payload after lowering the loop body, keeping `JsAstStore` ownership stable for existing element props.
- `generate_vue3_dom_mir` now emits memoized render-list cache code from MIR: `__, ___, _cached` params, `const _memo`, optional key guard, `_isMemoSame`, `const _item`, `_item.memo`, `_cache` argument, and keyed fragment flag.
- This is structural DOM MIR codegen progress only. The standalone MIR emitter still renders JS store expressions as registered source and does not claim exact prefix-identifier parity; the exact AST emitter remains the official Vue 3 conformance path until full migration.
- Verification for this v-for memo MIR slice: focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_v_for_memo_cache_target`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_v_for_memo_cache_target`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, focused official `scopeId.spec.ts` + `vMemo.spec.ts` (`11/11`), `cargo xtask run-conformance --suite vue3-core` (`652/652`; report distinguishes `rust-backed`, `mixed`, and `shim-backed`), and `git diff --check`.

- Current round: added non-`v-for` `v-memo` wrappers to structural Vue 3 DOM MIR.
- `Vue3DomMirKind::Memo { expression, index }` now stores the memo expression as a `JsExprId` and the target cache slot. DOM lowering wraps non-`v-for` memoized elements before lowering their VNode body; `v-for` memo cache guards remain out of scope for this slice.
- `generate_vue3_dom_mir` now imports `_withMemo` and emits `_withMemo(expression, () => vnode, _cache, index)` from MIR. Structural directives such as `memo`, `once`, `slot`, `pre`, `html`, and `text` are excluded from runtime HIR directives.
- This is structural DOM MIR codegen progress only. `v-for` memo cache MIR shape, cacheHandlers event caching, full hoist output, and exact DOM codegen parity remain open.
- Verification for this v-memo MIR slice: `cargo fmt --all`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_v_memo_wrappers`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_v_memo_wrappers`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: added dynamic component slots payloads to structural Vue 3 DOM MIR.
- `Vue3DomSlots` now carries `dynamic_slots` alongside stable slots. Dynamic entries cover static/dynamic slot names, optional slot params, conditional slot guards with generated keys, and slot `v-for` source / alias patterns. Slot params and loop expressions remain in `JsAstStore`; slot bodies remain child node IDs in the same `Vue3DomMir` document.
- DOM lowering now projects dynamic slot names, slot `v-if` / `v-else-if`, and slot `v-for` into `MirChildren::Slots` and sets `Vue3SlotFlag::Dynamic`; `generate_vue3_dom_mir` emits `_createSlots`, `_renderList`, dynamic slot objects, conditional `undefined` fallbacks, and `1024 /* DYNAMIC_SLOTS */` from MIR.
- This is structural DOM MIR codegen progress only. Forwarded slot flags, exact slot branch alternates, and complete exact component codegen parity remain open.
- Verification for this dynamic-slot MIR slice: `cargo fmt --all`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_dynamic_component_slots`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_dynamic_component_slots_from_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: added a stable component slots payload to structural Vue 3 DOM MIR.
- `MirChildren` now has a `Slots(Vue3DomSlots)` variant with stable slot entries, optional slot params stored as `JsPatternId`, and child MIR node IDs. DOM lowering projects default component children, static named `#slot` template children, and static on-component `v-slot` params into this payload, while leaving dynamic / conditional / loop slots for a later slice.
- Stable slots now also create `HirNodeKind::SlotDecl` nodes before lowering slot bodies, preserving the documented AST -> HIR -> Vue3DomMir slot boundary instead of making slots a DOM-only reconstruction.
- `generate_vue3_dom_mir` now emits stable component slot objects from MIR with `_withCtx(...)` slot functions and `_: 1`, and collects the `withCtx` helper from MIR children instead of the source AST. The slot child VNodes still reuse the same target-split MIR document and `JsAstStore`.
- This is structural DOM MIR codegen progress only. Dynamic slot objects, `_createSlots`, slot `v-if` / `v-for`, forwarded slot flags, and complete exact component codegen parity remain open.
- Verification for this stable-slot MIR slice: `cargo fmt --all`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_stable_component_slots`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_on_component_stable_slot_params`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_stable_component_slots_from_mir`, `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: structured Vue 3 DOM MIR component tags instead of encoding component targets as plain string tags.
- `vuec_ast::Vue3VNodeCall.tag` is now `Vue3DomTag`, distinguishing native tags, component assets, dynamic components, and runtime-helper built-ins. DOM lowering projects `<Child>`, `<Transition>`, and `<component :is="view">` into those target variants while reusing the existing `JsAstStore` expression for dynamic `is`.
- `generate_vue3_dom_mir` now derives `_resolveComponent` declarations, `_resolveDynamicComponent` calls, and built-in component helper imports from MIR tag variants. Dynamic component `is` bindings are consumed by the tag renderer and no longer emitted again as props in this structural emitter.
- This is structural DOM MIR codegen progress. It does not complete component slot object lowering, exact dynamic component edge cases, or the final migration of the exact emitter to full `Vue3DomMir` consumption.
- Verification for this component-tag MIR slice: `cargo fmt --all`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_projects_component_tags_structurally`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_component_tags_from_mir`, `cargo check -p vuec_ast -p vuec_vue3_core`, `cargo test -p vuec_vue3_core`, and `cargo test -p vuec_ast`.

- Current round: extended structural Vue 3 HIR and DOM MIR props with ordered segment payloads for merge-sensitive codegen.
- `HirProps` now keeps `segments` alongside the existing derived flat views, plus object `v-bind` / `v-on` payloads and dynamic-argument expression IDs. `Vue3DomProps` now mirrors those ordered target segments, records object binding/listener payloads, and carries normalize intent (`normalizeProps` / `guardReactiveProps`) so target codegen does not infer this from the source AST.
- `lower_vue3_ast_to_dom_mir` now preserves ordered static attrs, object `v-bind`, dynamic `:[arg]`, object `v-on`, and dynamic `@[event]` facts through HIR into `Vue3DomMir`; object spreads and dynamic args project to `FULL_PROPS` and do not leak synthetic names into `dynamic_props`.
- `generate_vue3_dom_mir` now emits `_mergeProps`, `_normalizeProps`, `_guardReactiveProps`, `_toHandlers`, `_toHandlerKey`, dynamic `v-bind` key fallback, object listener preserve-case flags, and `16 /* FULL_PROPS */` from MIR only. This is still structural DOM MIR codegen progress, not full official compiler-core parity or final exact DOM codegen migration.
- Verification for this DOM MIR props merge slice: `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_keeps_ordered_prop_segments_and_object_spreads`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir_emits_merge_and_dynamic_props_from_mir`, `cargo test -p vuec_vue3_core`, and `cargo test -p vuec_ast`.

- Current round: expanded `Vue3DomMir` VNode payloads so structural DOM MIR codegen can recover current props and runtime directive data without reaching back into `Vue3Ast`.
- `vuec_ast::Vue3VNodeCall` now carries `Vue3DomProps` and `Vue3DomDirective` target data. DOM lowering derives this payload from HIR props/directive uses, preserving existing `JsAstStore` ownership without double-registering expressions. The standalone MIR emitter now emits static attrs, dynamic bindings, event props, class normalization, runtime directive wrappers, directive asset declarations, and directive/runtime helpers from MIR.
- This closes the first missing MIR-payload gap for the structural emitter, but exact parity still needs richer prop semantics such as merge props, dynamic arg helper lowering, component asset resolution, directive arg dynamism, cacheHandlers, full hoist output, and official patch-flag parity.
- Verification for this DOM MIR payload slice: `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, focused `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir_records_hir_mir_edges_and_js_store`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: added a standalone structural Vue 3 DOM MIR codegen entry.
- `vuec_vue3_core::generate_vue3_dom_mir` now emits render code from `Vue3DomMir` plus `JsAstStore` without reading the original AST. The initial emitter covers the current structural MIR subset: root children, VNode calls, text/interpolation text VNodes, slot outlets, `v-if` comment fallbacks, `v-for` render-list wrappers, fragments, `v-once` cache wrappers, conservative hoist references, patch flags, dynamic prop names, and module-mode helper imports derived from MIR node kinds.
- This is target-split codegen foundation only. It does not replace the existing exact AST emitter, does not yet recover full props/directive payloads missing from current MIR, and does not complete official DOM/compiler-core codegen parity.
- Verification for this DOM MIR codegen slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, focused `cargo test -p vuec_vue3_core generate_vue3_dom_mir`, `cargo test -p vuec_vue3_core`, and `git diff --check`.

- Current round: migrated the Vue 3 exact render emitter's asset/helper selection away from legacy AST/code-string probing and onto transformed `Vue3Root` codegen state where available.
- `Vue3Dialect::generate` now consumes `Vue3Root.helpers`, `Vue3Root.components`, and `Vue3Root.directives` for helper imports/destructuring plus component/directive asset declarations, with raw-AST fallback scans preserved for direct public `generate(ast, options)` calls that have not run `transform`. Component rendering now uses runtime helper tags for built-in components, `_resolveDynamicComponent(...)` for dynamic `<component :is>`, and only emits `_resolveComponent(...)` declarations for collected component assets.
- The legacy emitter now lowers runtime directive usage for the current exact-emitter subset: custom directives and `v-show` wrap VNode calls with `_withDirectives(...)`, emit `_resolveDirective(...)` / `vShow` helpers from root or fallback state, and add `NEED_PATCH` when runtime directives are present. This is AST-root-driven exact emitter progress; full DOM MIR codegen and official transform/codegen parity remain open.
- Verification for this codegen-state slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, focused `cargo test -p vuec_vue3_core generate_`, focused `cargo test -p vuec_vue3_core base_compile_marks_vnode_hook_need_patch`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: moved Vue 3 root helper / component / directive collection onto the Rust `Vue3Ast` transform state.
- `Vue3Dialect::transform` now populates `Vue3Root.helpers`, `Vue3Root.components`, and `Vue3Root.directives` in addition to keeping `TransformContext.helpers` synchronized. The collection distinguishes component assets from built-in component helpers and dynamic component resolution, keeps structural directives out of runtime directive assets, maps `v-show` to the `Vue3VShow` helper, and records custom runtime directives with `Vue3ResolveDirective` / `Vue3WithDirectives`.
- Focused tests now verify the collected root fields through `AstDocument::project_public()`, so the collection is visible at the AST contract boundary rather than only through legacy emitter scans. Exact MIR/codegen consumption of these root fields remains a later compiler-pipeline step.
- Verification for this root collection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: extended structural Vue 3 DOM MIR lowering with target-level static hoist and `v-once` cache wrappers.
- `vuec_vue3_core::lower_vue3_ast_to_dom_mir` now emits `Vue3DomMirKind::Hoisted { index }` wrappers for conservative all-static native subtrees when `hoist_static` is enabled, skips hoisting the single root element, records HIR -> MIR edges to the hoist wrapper, and keeps nested static nodes inside the same hoist instead of creating nested hoists.
- DOM MIR lowering also emits `Vue3DomMirKind::Cache { index }` wrappers for top-level `v-once` nodes, suppresses nested `v-once` cache wrappers while lowering inside an existing once cache, and keeps expression ownership in `JsAstStore`. This is structural target projection only; exact hoist codegen, cacheHandlers event caching, `v-memo` MIR cache shape, and official output parity remain open.
- Verification for this hoist/cache MIR slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, and `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`.

- Current round: extended structural Vue 3 control-flow lowering from single `v-if` wrappers to adjacent `v-if` / `v-else-if` / `v-else` branch chains for both DOM and SSR MIR.
- `vuec_vue3_core::lower_vue3_ast_to_dom_mir` and `lower_vue3_ast_to_ssr_mir` now lower sibling branch chains into a single `HirIf` with multiple `HirIfBranch` entries, preserve `v-for` priority over branch-chain grouping, attach branch bodies as normal lowered elements, record AST -> HIR and HIR -> target MIR edges for branch nodes, and avoid double-registering the first condition expression. This is structural lowering; exact branch codegen/key injection parity remains open.
- Verification for this branch-chain lowering slice: `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir` and `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`.

- Current round: moved the structural Vue 3 DOM MIR lowering beyond placeholder patch flags.
- `vuec_vue3_core::lower_vue3_ast_to_dom_mir` now projects composed DOM MIR patch flag bits for class, style, text, dynamic props, hydration events, full-props dynamic keys, and vnode hooks in the current structural lowering subset, while preserving MIR `dynamic_props` as target data including class/style/event names. The exact render emitter remains separate, and full official patch-flag parity is still open.
- Verification for this DOM MIR patch-flag slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, `cargo test -p vuec_vue3_core`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: extended the structural Vue 3 AST/HIR/MIR lowering contract with directive-backed `v-for` / `v-if` control-flow wrappers for both DOM and SSR targets.
- `vuec_vue3_core::lower_vue3_ast_to_dom_mir` and `lower_vue3_ast_to_ssr_mir` now lower element-level `v-for` into `HirFor` plus target-specific `For` MIR nodes, lower `v-if` / `v-else-if` into `HirIf` plus target-specific `If` MIR nodes, register `v-for` source/aliases and `v-if` conditions in `JsAstStore`, and filter structural directives out of generic HIR element directive uses so expressions are not double-registered. This remains structural compiler work, not exact control-flow codegen parity.
- Verification for this control-flow lowering slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, `cargo test -p vuec_vue3_core`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: added the structural Vue 3 SSR lowering counterpart to the existing DOM lowering entry.
- `vuec_vue3_core::lower_vue3_ast_to_ssr_mir` / `Vue3Dialect::lower_to_ssr_mir` now produce HIR plus target-split `Vue3SsrMir`, explicit `LoweringMap`, and `JsAstStore` expression registrations for basic Root/Element/Text/Interpolation/Comment, component render, dynamic attr marker, and slot outlet nodes. SSR lowering is independent from `Vue3DomMir` and remains a contract foundation, not exact SSR codegen parity.
- Verification for this SSR lowering slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_ssr_mir`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, `cargo test -p vuec_vue3_core`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, and `git diff --check`.

- Current round: moved Vue 3 compiler-core `transformVBindShorthand` decisions into a Rust-backed bridge projection and added the first structural `Vue3Ast -> HIR -> Vue3DomMir` lowering contract entry.
- `vuec_vue3_core::transform_v_bind_shorthand_projection` now owns same-name shorthand expansion for static args, browser empty-expression expansion, invalid dynamic-arg diagnostics, and official first-character eligibility including Unicode `\xA0-\uFFFF` plus hyphen. `vuec_node_bridge` exposes `vue3.core.transformVBindShorthand`.
- `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias runtime calls Rust for the shorthand projection and materializes Rust-selected `prop.exp` plus diagnostics back into official-shaped nodes. This is not standalone JavaScript compiler semantics.
- `vuec_vue3_core::lower_vue3_ast_to_dom_mir` / `Vue3Dialect::lower_to_dom_mir` now produce a structural HIR document, target-split `Vue3DomMir`, explicit `LoweringMap`, and `JsAstStore` registrations for basic Vue 3 Root/Element/Text/Interpolation/Comment and slot outlet nodes. This is a contract foundation and does not yet replace the existing exact render emitter or complete control-flow/patch-flag lowering.
- Verification for this round: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core lower_vue3_ast_to_dom_mir`, `cargo test -p vuec_vue3_core transform_v_bind_shorthand_projection`, `cargo test -p vuec_vue3_core build_slots_projection_tracks_slot_locals_and_dynamic_slots`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`99/99` Vue 3 core tests and `20/20` xtask tests pass), `cargo test -p vuec_ast -p vuec_pass -p vuec_js`, `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, `cargo xtask audit-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core` (`8/8`), `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core` (`5/5`), `cargo xtask run-conformance --suite vue3-core` (`652/652`, coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`, `mixed 133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`, `mixed 129/129`).

- Current round: moved Vue 3 compiler-core `transformSlotOutlet` / `processSlotOutlet` decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_slot_outlet_projection` now owns `<slot>` outlet recognition, default/static/dynamic slot names, `:name` same-name shorthand, non-name prop selection and camelize mutations, fallback presence, `scopeId` / `slotted` argument length, and `_ctx.$slots` vs `$slots` selection.
- `vuec_node_bridge` exposes `vue3.core.transformSlotOutlet`. `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias runtime calls Rust for the outlet projection, applies Rust-selected prop mutations, lets existing `buildProps` materialize slot props and directive diagnostics, and builds the official `_renderSlot(...)` call from Rust-selected arguments. This is not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformSlotOutlet.spec.ts` passes `14/14` through the Rust-backed slot-outlet projection. The file-level conformance classification remains `mixed`, because the official spec still executes through alias-runtime traversal, `buildProps`, and codegen materialization even though the slot-outlet decisions are Rust-backed.
- Adjacent focused official checks remain green: `vOnce.spec.ts` + `transformSlotOutlet.spec.ts` `22/22`, `vSlot.spec.ts` + `transformElement.spec.ts` `155/155`, and `vFor.spec.ts` + `vIf.spec.ts` `84/84`.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with `mixed 133/133`; `vue3-ssr` `129/129` with `mixed 129/129`.
- Verification for this Rust-backed `transformSlotOutlet` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core transform_slot_outlet_projection`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`95/95` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `transformSlotOutlet.spec.ts` (`14/14`), adjacent focused specs (`261/261`), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: moved Vue 3 compiler-core `transformBind` / `v-bind` decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_bind_projection` now owns empty-expression diagnostics, dynamic argument `|| ""` guards, `.camel` static/dynamic helper handling, `.prop` / `.attr` prefixes, SSR prefix skipping, and browser empty-expression undefined values.
- `vuec_node_bridge` exposes `vue3.core.transformBind`. `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias runtime sends directive/context facts to Rust, forwards Rust diagnostics, and materializes Rust simple/compound/helper projections back into official-shaped AST properties. This is not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vBind.spec.ts` now passes `19/19` through the Rust-backed `transformBind` projection. The file-level conformance classification remains `mixed`, because the official spec still executes through alias-runtime traversal and element props materialization even though the `transformBind` decisions are Rust-backed.
- Adjacent focused official checks remain green: `vOn.spec.ts` + `vModel.spec.ts` `54/54`, and `transformElement.spec.ts` `124/124`.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with `mixed 133/133`; `vue3-ssr` `129/129` with `mixed 129/129`.
- Verification for this Rust-backed `transformBind` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core transform_bind_projection`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`93/93` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `vBind.spec.ts` (`19/19`), adjacent focused specs (`178/178`), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: moved Vue 3 compiler-core internal `transformMemo` decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_memo_projection` now owns the official `v-memo` eligibility checks, already-seen skip, SSR skip, component-vs-plain-element block conversion decision, `WITH_MEMO` helper requirement, memo expression forwarding, and deterministic cache index selection.
- `vuec_node_bridge` exposes `vue3.core.transformMemo`. `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias runtime sends dehydrated node/context plus adapter-local seen state to Rust, marks the JS node as seen, materializes `convertToBlock`, builds the official `_withMemo(...)` call, and increments the transform cache slot. This is not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vMemo.spec.ts` remains `7/7` passing. A direct alias-runtime transform harness check also confirms internal `transformMemo` materializes `withMemo`, converts plain elements to block VNodes, and increments `cached`.
- Adjacent focused official checks remain green: `vOnce.spec.ts` + `cacheStatic.spec.ts` + `vFor.spec.ts` + `vIf.spec.ts` `126/126`.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with `mixed 133/133`; `vue3-ssr` `129/129` with `mixed 129/129`.
- Verification for this Rust-backed internal `transformMemo` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`90/90` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `vMemo.spec.ts` (`7/7`), adjacent focused specs (`126/126`), direct alias-runtime transform harness check, `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: moved Vue 3 compiler-core `transformOnce` decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_once_projection` now owns the official `v-once` eligibility checks: element-only, `v-once` directive presence, already-seen skip, nested `context.inVOnce` skip, SSR skip, helper requirement, enter `inVOnce`, and exit-time codegen caching intent.
- `vuec_node_bridge` exposes `vue3.core.transformOnce`. `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias runtime sends dehydrated node/context plus adapter-local seen state to Rust, marks the JS node as seen, toggles `context.inVOnce`, registers `SET_BLOCK_TRACKING`, and applies `context.cache(...)` on exit from Rust-returned operations. This is not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vOnce.spec.ts` remains `8/8` passing. The file-level conformance classification remains `mixed`, because the official transform file still runs through the alias traversal/runtime transform harness even though `transformOnce` decisions are now Rust-backed.
- Adjacent focused official checks remain green: `cacheStatic.spec.ts` + `transformText.spec.ts` + `vMemo.spec.ts` `50/50`.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with `mixed 133/133`; `vue3-ssr` `129/129` with `mixed 129/129`.
- Verification for this Rust-backed `transformOnce` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`87/87` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `vOnce.spec.ts` (`8/8`), adjacent focused specs (`50/50`), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: moved Vue 3 compiler-core `transformExpression` node/directive dispatch into a Rust-backed bridge projection. `vuec_vue3_core::transform_expression_projection` now owns the official interpolation vs element branching, directive filtering, `v-for` skip, `v-on:arg` exp skip, `v-slot` params handling, dynamic arg processing, and `v-memo` key skip decisions, then delegates expression rewriting to Rust `process_expression_projection`.
- `vuec_node_bridge` exposes `vue3.core.transformExpression`. `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias runtime dehydrates official-shaped nodes/context, calls Rust, and writes Rust-returned process-expression projections back by path. This is not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformExpressions.spec.ts` remains `47/47` passing. The file-level conformance classification remains `mixed`, because the official spec still executes through the alias traversal/runtime transform harness even though both `transformExpression` dispatch and `processExpression` decisions are now Rust-backed.
- Adjacent focused official checks remain green: `cacheStatic.spec.ts` + `vSlot.spec.ts` + `vMemo.spec.ts` + `vOn.spec.ts` `105/105`.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with `mixed 133/133`; `vue3-ssr` `129/129` with `mixed 129/129`.
- Verification for this Rust-backed `transformExpression` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`85/85` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `transformExpressions.spec.ts` (`47/47`), adjacent focused specs (`105/105`), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: moved Vue 3 compiler-core `processExpression` decisions into a Rust-backed bridge projection. `vuec_vue3_core::process_expression_projection` now owns identifier prefixing, setup binding rewrites, local/function/arrow parameter scoping, function-parameter destructuring, object shorthand rewriting, assignment RHS setup-let handling, parser validation through the JS/OXC store, helper registration metadata, and const classification for static literals and static object expressions.
- `vuec_node_bridge` exposes `vue3.core.processExpression`. `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias runtime dehydrates official-shaped expression/context payloads, calls Rust, and materializes Rust simple/compound/setConstType projections back into official-shaped AST nodes. This is not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformExpressions.spec.ts` remains `47/47` passing. The file-level conformance classification remains `mixed`, because the official spec still executes through the alias traversal/runtime transform harness even though `processExpression` decisions are now Rust-backed.
- The static object literal const-type fix also keeps adjacent official suites green: focused `cacheStatic.spec.ts` + `vSlot.spec.ts` `65/65`, DOM `stringifyStatic.spec.ts` `25/25`, and SSR `ssrComponent.spec.ts` `16/16`.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with `mixed 133/133`; `vue3-ssr` `129/129` with `mixed 129/129`. `transformExpressions.spec.ts` remains `mixed` at file level unless a report explicitly marks a narrower Rust-backed entry.
- Verification for this Rust-backed `processExpression` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`82/82` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `transformExpressions.spec.ts` (`47/47`), focused `cacheStatic.spec.ts` + `vSlot.spec.ts` (`65/65`), focused DOM `stringifyStatic.spec.ts` (`25/25`), focused SSR `ssrComponent.spec.ts` (`16/16`), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: moved Vue 3 compiler-core `transformText` decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_text_projection` now owns adjacent text/interpolation merging, `TEXT_CALL` wrapping, single-root/plain-element fast paths, custom directive detection, compat `<template>` handling, and the official `context.ssr`-only dynamic text patch-flag rule.
- `vuec_node_bridge` exposes `vue3.core.transformText`. `xtask/src/compat.rs` changes in this round are bridge/materialization support only: the alias transform dehydrates the official-shaped node/context, calls Rust, then materializes Rust `mergeText` / `wrapTextCall` operations back into official-shaped AST nodes. This is not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformText.spec.ts` remains `9/9` passing. The file-level conformance classification remains `mixed`, because the same official spec file also exercises alias-runtime `transformExpression` / transform harness / codegen dependencies that are not fully Rust-backed yet.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with coverage `mixed 133/133`; `vue3-ssr` `129/129` with coverage `mixed 129/129`.
- Verification for this Rust-backed `transformText` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`76/76` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: routed public Vue 3 `@vue/compiler-core.generate(ast, options)` through the Rust bridge and Rust public-AST codegen path. `compiler-core/__tests__/codegen.spec.ts` is now classified as `rust-backed` and passes `34/34`.
- `vuec_vue3_core::generate_public_ast` now emits official-shaped Vue 3 public codegen for hydrated official AST JSON: module/function preambles, `optimizeImports`, prefix/with modes, inline `preamble` separation, SSR helper imports from `vue/server-renderer`, hoists, temps/assets, VNode/directive/block calls, patch-flag comments, JS expression/codegen node families, and official helper alias naming.
- `vuec_ast::RuntimeHelper` now covers the Vue 3 public codegen helper surface needed by DOM and SSR official ASTs, including directive/runtime DOM helpers, built-in components, static vnode helpers, and SSR renderer helpers. `vuec_node_bridge` exposes the new `vue3.core.generate` command and forwards `ssr` / `optimizeImports` options.
- `xtask/src/compat.rs` changes in this round are API/AST adapter support only: generated aliases route `generate` to the Rust bridge, dehydrate official AST values/Sets/helper symbols for the bridge, hydrate helper symbols by official runtime names, and preserve the observable returned `ast` reference. These adapter changes are not counted as JavaScript shim compiler semantics.
- Full Vue 3 conformance after this slice: `vue3-core` `652/652` with coverage `rust-backed 192/192`, `mixed 460/460`, `shim-backed 0/0`; `vue3-dom` `133/133` with coverage `mixed 133/133`; `vue3-ssr` `129/129` with coverage `mixed 129/129`.
- Verification for this Rust-backed public generate slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`74/74` Vue 3 core tests and `20/20` xtask tests pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `codegen.spec.ts` (`34/34`), focused SSR `ssrElement.spec.ts` + `ssrComponent.spec.ts` (`48/48`), focused `ssrInjectCssVars.spec.ts` inline helper case, `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129`).

- Current round: closed the remaining Vue 3 compiler-ssr prepared official suite through mixed alias-runtime compiler-core/codegen adapter support. Full Vue 3 SSR conformance now reports `129/129` official tests passing.
- Focused SSR specs now pass for the remaining files: `ssrComponent.spec.ts` (`16/16`), `ssrScopeId.spec.ts` (`5/5` from the previous focused fix in this slice), and `ssrInjectCssVars.spec.ts` (`6/6`). The earlier slot outlet / transition / structural / element / text slices remain passing.
- The generated compiler-core alias now respects official vnode fallback behavior in SSR sub-transforms: `processIf` / `processFor` only suppress default client codegen for active `context.ssr`, `getBaseTransformPreset(true)` includes `trackVForSlotScopes`, `resolveComponentType(node, context, ssr)` honors the explicit `ssr` argument, and dynamic slot `v-for` materialization preserves official helper registration order.
- The alias generator now supports setup-inline output shape by separating helper preamble into `result.preamble` and emitting `result.code` as an arrow function, matching the `ssrInjectCssVars` inline helper snapshot. SSR vnode fallback generation now uses SSR-aware VNode helper selection where the official source expects `createBlock` / `createVNode`.
- Coverage remains `mixed`: this is alias-runtime adapter/codegen support for prepared official SSR source execution and Rust bridge projections, not Rust-backed SSR transform/codegen parity. The latest `vue3-ssr` report has `mixed 129/129`, `rust-backed 0/0`, and `shim-backed 0/0`.
- Verification for this SSR conformance closure slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`20/20` pass), `git diff --check`, `cargo xtask export-api --rust --version-line vue3`, focused `ssrComponent.spec.ts` (`16/16` pass), focused `ssrInjectCssVars.spec.ts` (`6/6` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (`129/129` pass).

- Current round: closed the Vue 3 compiler-ssr `ssrSlotOutlet.spec.ts` focused slice through mixed alias-runtime compiler-core support. The official SSR slot outlet spec now passes `11/11`, `ssrTransition.spec.ts` and `ssrTransitionGroup.spec.ts` are also fully passing, and full Vue 3 SSR conformance improves from `99/129` to `121/129`.
- The generated compiler-core alias now materializes Rust `helperName` component projections back to the caller's own `context.isBuiltInComponent(...)` symbol when available. This keeps official SSR source comparisons such as `resolveComponentType(parent, context, true) === TRANSITION` valid for `Transition` and `TransitionGroup`.
- The alias `buildSlots(node, context, buildSlotFn)` path now honors the official third-argument slot function builder used by SSR component transforms, including forwarding template-slot `v-for` metadata when present. This fixes the forwarded-scopeId vnode fallback branch for `<Comp><slot/></Comp>`.
- Coverage remains `mixed`: this is alias-runtime adapter support for official SSR source execution and symbol/API compatibility, not Rust-backed SSR transform/codegen parity.
- Vue 3 compiler-core and compiler-dom conformance were rechecked after the alias runtime change and remain `652/652` and `133/133` respectively. Latest Vue 3 SSR state is a real expected failure with `121/129` passing and `8` failing; remaining failures are `ssrComponent.spec.ts` (`5`), `ssrInjectCssVars.spec.ts` (`1`), and `ssrScopeId.spec.ts` (`2`).
- Verification for this SSR slot/Transition slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`20/20` pass), focused `ssrSlotOutlet.spec.ts` (`11/11` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (expected fail with `121/129`).

- Current round: closed the Vue 3 compiler-ssr `ssrVShow.spec.ts` focused slice through mixed alias-runtime compiler-core support. The official SSR `v-show` spec now passes `7/7`, and full Vue 3 SSR conformance improves from `97/129` to `99/129`.
- The generated compiler-core alias `buildProps` now applies caller-provided directive transforms for built-in directives when they exist, so official SSR `show` can materialize style props while non-prop built-ins such as `v-text` / `v-html` still avoid being treated as custom directive props.
- Coverage remains `mixed`: this is alias materialization support for official SSR source execution, not Rust-backed SSR transform/codegen parity.
- Vue 3 compiler-core and compiler-dom conformance were rechecked after the alias runtime change and remain `652/652` and `133/133` respectively. Latest Vue 3 SSR state is a real expected failure with `99/129` passing and `30` failing.
- Verification for this SSR `v-show` slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`20/20` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (expected fail with `99/129`, `ssrVShow.spec.ts` `7/7`).

- Current round: closed Vue 3 compiler-ssr structural directive first-pass compatibility through mixed alias-runtime support. Official SSR `ssrVFor.spec.ts`, `ssrVIf.spec.ts`, `ssrVModel.spec.ts`, and `ssrFallthroughAttrs.spec.ts` now pass fully; full Vue 3 SSR conformance improves from `75/129` to `97/129`.
- The generated compiler-core alias `processFor` and `processIf` now run as structural-only transforms when `context.inSSR` is true and no caller-provided client codegen callback is active. This keeps the official SSR second-pass transforms in charge and avoids registering client-side Vue helpers such as `renderList`, `Fragment`, `openBlock`, `createBlock`, and `createCommentVNode`.
- Coverage remains `mixed`: this is alias-runtime support for the official SSR source pipeline and does not count as Rust-backed SSR transform/codegen parity.
- Vue 3 compiler-core and compiler-dom conformance were rechecked after the alias runtime changes and remain `652/652` and `133/133` respectively. Latest Vue 3 SSR state is a real expected failure with `97/129` passing and `32` failing.
- Verification for this SSR structural directive slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`20/20` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (expected fail with `97/129`; `ssrVFor`, `ssrVIf`, `ssrVModel`, and `ssrFallthroughAttrs` all passing).

- Current round: closed the Vue 3 compiler-ssr `ssrElement.spec.ts` focused slice through mixed alias-runtime compiler-core support. The official SSR element spec now passes `32/32`, and full Vue 3 SSR conformance improves from `68/129` to `75/129`.
- The generated compiler-core alias `buildProps` now matches the SSR element contract for the remaining basic cases: object `v-on` is ignored in SSR element attrs, built-in directives such as `v-text` are not treated as custom directive runtime props, and SSR conditional expressions around helper calls keep the official snapshot parentheses.
- This also improves adjacent SSR `v-model` coverage from `4/7` to `6/7` because the same built-in directive filtering and boolean-attr formatting are shared by official SSR transforms.
- Coverage remains `mixed`: this is alias materialization/codegen support used by official SSR source execution, not Rust-backed SSR compiler semantic parity.
- Vue 3 compiler-core and compiler-dom conformance were rechecked after the alias runtime changes and remain `652/652` and `133/133` respectively. Latest Vue 3 SSR state is a real expected failure with `75/129` passing and `54` failing.
- Verification for this SSR element slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`20/20` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (expected fail with `75/129`, `ssrElement.spec.ts` `32/32`).

- Current round: closed the Vue 3 compiler-ssr `ssrText.spec.ts` focused slice through mixed alias-runtime support. The official SSR text spec now passes `8/8`, and full Vue 3 SSR conformance improves from `12/129` to `68/129`.
- The generated compiler-core alias `buildProps` now treats argument-less `v-bind="obj"` as a props / merge-props expression instead of a runtime directive. This lets official SSR `ssrTransformElement` produce `_ssrRenderAttrs(_attrs)` for the test wrapper and fixes a broad class of SSR element / v-model / v-show snapshot failures.
- The generated alias codegen now formats SSR template literals with multiple dynamic parts using official-style multiline `${ ... }` interpolation blocks while preserving single-expression literals as one line. This closes the remaining `ssrText` nested interpolation snapshot mismatch.
- Coverage remains `mixed`: these changes are compiler-core alias materialization/codegen support used by official SSR source execution, not Rust-backed SSR compiler semantic parity. The next SSR work should migrate these contracts into Rust-backed compiler-core/SSR projections instead of expanding temporary JS semantics indefinitely.
- Vue 3 compiler-core and compiler-dom conformance were rechecked after the alias runtime changes and remain `652/652` and `133/133` respectively. Latest Vue 3 SSR state is a real expected failure with `68/129` passing and `61` failing.
- Verification for this SSR text/buildProps slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`20/20` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), `cargo xtask run-conformance --suite vue3-dom` (`133/133`), and `cargo xtask run-conformance --suite vue3-ssr` (expected fail with `68/129`, `ssrText.spec.ts` `8/8`).

- Current round: wired Vue 3 compiler-ssr official conformance to a real Vitest execution path. `cargo xtask run-conformance --suite vue3-ssr` now runs the 15 official SSR spec files instead of reporting `not-wired` pending.
- The prepared SSR suite copies official `compiler-ssr/__tests__`, official `compiler-ssr/src`, and official `compiler-dom/src`, then uses generated compiler-core shims plus alias/npm resolution for package imports. The runner also aliases the monorepo-only `packages/compiler-core/src/transform` import used by SSR source.
- Coverage for `vue3-ssr` is now explicitly `mixed`: prepared SSR source tests execute official TypeScript SSR/DOM transforms, generated import shims, alias adapter code, and Rust bridge projections. This is test-runner/import wiring and does not count as Rust-backed SSR compiler semantics.
- Latest Vue 3 SSR conformance state is a real failure, not pending: `12/129` official tests pass and `117` fail. Passing files include `ssrPortal.spec.ts` (`2/2`) and partial slot/component/fallthrough/text cases; failures are now visible snapshot/output mismatches across SSR element, component, scopeId, slot, suspense, transition, v-for, v-if, v-model, and v-show paths.
- Vue 3 compiler-core and compiler-dom conformance were rechecked after the SSR runner wiring and remain `652/652` and `133/133` respectively.
- Verification for this SSR runner slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`20/20` pass), `cargo xtask run-conformance --suite vue3-ssr` (real fail with `12/129`), `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-dom` (`133/133`).

- Current round: closed the Vue 3 compiler-dom official conformance suite. `cargo xtask run-conformance --suite vue3-dom` now reports `133/133` official tests passing, up from `115/133`, with `stringifyStatic.spec.ts` now `25/25`.
- `vuec_vue3_core` now decodes HTML attribute entities in directive expression `content` while preserving the original encoded value span for public projection. This matches the official DOM parser contract where examples such as `:class="'foo' + '&gt;ar'"` expose decoded `exp.content` (`'foo' + '>ar'`) but keep `exp.loc.source` on the raw encoded source.
- The alias runtime now applies official DOM `transformHoist` after Rust `cacheStatic` materialization when a caller supplies `context.transformHoist`, so cached static children are passed through official `stringifyStatic`. This is mixed Rust projection plus alias/runtime DOM transform execution; it is not counted as pure Rust-backed DOM stringification semantics.
- Vue 3 compiler-core conformance was rechecked after the parser and adapter changes and remains `652/652`. Coverage remains honest: Vue 3 DOM is `mixed 133/133`, `rust-backed 0/0`, `shim-backed 0/0`; Vue 3 core remains `rust-backed 158/158` plus `mixed 494/494`.
- Verification for this Vue 3 DOM `stringifyStatic` / conformance closure slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask` (`74/74` core tests and `18/18` xtask tests pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-dom` (`133/133`).

- Current round: closed the Vue 3 compiler-dom `vModel.spec.ts` remaining compatibility failures. The alias runtime `transformElement` now records `needRuntime` returned by official DOM `transformModel` and routes model directives through `withDirectives(...)`, including dynamic model helper selection for text/radio/checkbox/select/custom-element cases.
- The directive-args materializer now preserves the official `void 0` placeholder before modifier objects when a runtime directive has modifiers but no argument, matching DOM `v-model.number` / `.trim` / `.lazy` snapshots. The `v-bind` handling was narrowed so custom transform callers that do not provide `bind` do not synthesize props, while still preserving the official `:key` block-forcing signal.
- Latest Vue 3 DOM conformance state: `cargo xtask run-conformance --suite vue3-dom` remains expected-failing overall but improves from `102/133` to `115/133` official tests passing. `vModel.spec.ts` is no longer in the failure list; remaining failures are only `stringifyStatic` (`18`). Vue 3 core conformance was rechecked and remains `652/652`.
- Verification for this Vue 3 DOM `v-model` slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`18/18` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-dom` (expected fail with `115/133`).

- Current round: closed the Vue 3 compiler-dom `vOn.spec.ts` remaining compatibility failures. `vuec_vue3_core::transform_on_projection` now emits `valueConstant` metadata for handler values, preserving the Rust-selected setup-const handler constness across downstream DOM modifier wrapping.
- The alias runtime `transformOn` now implements the official compiler-core augmentor contract used by DOM `vOn`: it materializes the Rust projection, lets the DOM augmentor wrap handlers with helpers such as `withKeys` / `withModifiers`, then reapplies Rust-selected cache / handler-key / constness metadata to the final property. This is mixed Rust projection plus alias-runtime adapter support, not a pure Rust DOM transform implementation.
- Latest Vue 3 DOM conformance state: `cargo xtask run-conformance --suite vue3-dom` remains expected-failing overall but improves from `91/133` to `102/133` official tests passing. `vOn.spec.ts` is no longer in the failure list; remaining failures are `stringifyStatic` (`18`) and `vModel` (`13`). Vue 3 core conformance was rechecked and remains `652/652`.
- Verification for this Vue 3 DOM `v-on` slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo check -p vuec_vue3_core`, `cargo test -p xtask` (`18/18` pass), `cargo test -p vuec_vue3_core` (`73/73` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-dom` (expected fail with `102/133`).

- Current round: closed the Vue 3 compiler-dom `Transition.spec.ts` remaining snapshot failures. `vuec_vue3_core::resolve_component_type_projection` now accepts dynamic built-in component helper metadata from the alias runtime, allowing DOM parserOptions built-ins such as `Transition` / `transition` and `TransitionGroup` / `transition-group` to resolve to runtime helpers instead of `_resolveComponent(...)` assets.
- The alias runtime now exposes DOM built-in helper symbols in its helper map, samples `context.isBuiltInComponent` for DOM built-ins when calling the Rust component resolver, and materializes Rust `helperName` projections back into registered helpers so codegen destructures `Transition: _Transition` correctly.
- Latest Vue 3 DOM conformance state: `cargo xtask run-conformance --suite vue3-dom` remains expected-failing overall but improves from `88/133` to `91/133` official tests passing. `Transition.spec.ts` is no longer in the failure list; remaining failures are `stringifyStatic` (`18`), `vModel` (`13`), and `vOn` (`11`). Vue 3 core conformance was rechecked and remains `652/652`.
- Verification for this Vue 3 DOM `Transition` slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`73/73` pass), `cargo test -p xtask` (`18/18` pass), `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-dom` (expected fail with `91/133`).

- Current round: closed the Vue 3 compiler-dom `vShow.spec.ts` compatibility gap. `vuec_vue3_core::build_directive_args_projection` now preserves dynamically registered runtime helper names (for example DOM `V_SHOW -> vShow`) instead of downgrading unknown helper symbols to `resolveDirective(...)` assets, and the alias materializer can emit `_vShow` directive args from `helperName`.
- The focused `v-show` generated code now matches the official snapshot shape: helper destructuring includes `vShow`, `withDirectives`, `openBlock`, and `createElementBlock`; codegen emits `_withDirectives((_openBlock(), _createElementBlock(..., 512 /* NEED_PATCH */)), [[_vShow, a]])` without `_resolveDirective("show")`.
- Latest Vue 3 DOM conformance state: `cargo xtask run-conformance --suite vue3-dom` remains expected-failing overall but improves from `87/133` to `88/133` official tests passing. `vShow.spec.ts` is no longer in the failure list; remaining failures are `stringifyStatic` (`18`), `vModel` (`13`), `vOn` (`11`), and `Transition` (`3`). Coverage is still `mixed`.
- Verification for this Vue 3 DOM `v-show` slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`73/73` pass), direct alias `v-show` codegen reproduction, and `cargo xtask run-conformance --suite vue3-dom` (expected fail with `88/133`).

- Current round: opened the Vue 3 compiler-dom transform/codegen compatibility path by allowing generated `@vue/compiler-core` `baseCompile` aliases to fall back to the local alias runtime when the caller passes real JS `nodeTransforms`, `directiveTransforms`, or `transformHoist` functions. This is mixed/alias-runtime support for official DOM source execution, not Rust-backed compiler semantics.
- The immediate DOM side-effect tag behavior is now covered on both paths: public `@vue/compiler-dom.compile` still calls the Rust `vue3.dom.compile` bridge and now forwards returned Rust diagnostics to `options.onError`, while prepared official `compiler-dom/src/index.ts` can run its `ignoreSideEffectTags` node transform through the alias runtime and remove `<script>` / `<style>` via `context.removeNode()`.
- Latest Vue 3 DOM conformance state: `cargo xtask run-conformance --suite vue3-dom` remains expected-failing overall but improves from `69/133` to `87/133` official tests passing. `ignoreSideEffectTags.spec.ts` is no longer in the failure list; remaining failures are concentrated in `stringifyStatic` (`18`), `vModel` (`13`), `vOn` (`11`), `Transition` (`3`), and `vShow` (`1`).
- Vue 3 compiler-core conformance was rechecked after the `baseCompile` split and remains `652/652`, preserving the Rust bridge fast path for pure core `baseCompile` calls.
- Verification for this Vue 3 DOM transform-runtime slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`18/18` pass), `cargo check -p vuec_vue3_dom -p vuec_vue3_core -p vuec_node_bridge`, `cargo test -p vuec_vue3_dom`, direct alias checks for `<script>` / `<style>` removal plus `onError` diagnostic forwarding, `cargo xtask run-conformance --suite vue3-core` (`652/652`), and `cargo xtask run-conformance --suite vue3-dom` (expected fail with `87/133`).

- Current round: closed the Rust-backed Vue 3 compiler-dom parser compatibility slice for official `packages/compiler-dom/__tests__/parse.spec.ts`. The focused DOM parser spec now passes `33/33` through the prepared Vue 3 DOM conformance runner.
- `vuec_vue3_core` now handles DOM parser raw text/RCDATA and namespace semantics in Rust: HTML `<textarea>` / `<title>` parse as RCDATA with interpolation and entity decoding, HTML `<script>` / `<style>` parse as raw text without entity decoding, strict raw-text end-tag matching preserves malformed textarea content, root namespace is honored, and DOM namespace integration rules cover SVG, MathML, `annotation-xml`, MathML text integration points, and SVG `foreignObject` / `desc` / `title`.
- Entity compatibility now covers the official DOM parse cases for text, attributes, numeric references, C1 control replacement (`&#x86;` -> dagger), and interpolation content decoding while keeping public expression `loc.source` on the original encoded source. Leading-newline removal for `textarea` / `pre` now preserves the original loc span like the official parser.
- `vuec_node_bridge` now forwards deterministic DOM parser metadata from the generated alias (`__vuecDomNamespaces`, root `ns`) into `Vue3CompilerOptions`, and parse diagnostics use the same raw text / namespace rules so bridge hydration no longer raises false CDATA, invalid end-tag, missing end-tag, or interpolation-end diagnostics inside DOM raw-text regions. `xtask/src/compat.rs` changes in this slice are metadata/import adapter support only, not JavaScript compiler semantics.
- Latest Vue 3 DOM conformance state: `cargo xtask run-conformance --suite vue3-dom` is still expected-failing overall, but improves from `45/133` to `69/133` official tests passing. `parse.spec.ts` is now `33/33`; remaining failures are outside this parser slice and concentrated in DOM transform/codegen suites (`Transition`, `validateHtmlNesting`, `vModel`, `vOn`, `vShow`, and `index.spec.ts` snapshot).
- Verification for this Vue 3 DOM parser slice: `cargo check -p vuec_vue3_core`, `cargo check -p vuec_node_bridge`, `cargo check -p xtask`, `cargo build -p vuec_node_bridge`, `cargo test -p vuec_vue3_core` (`73/73` pass), `git diff --check`, and `cargo xtask run-conformance --suite vue3-dom` as an expected failing full DOM suite with `69/133` passing and `parse.spec.ts` `33/33`.

- Current round: wired real Vue 3 compiler-dom official conformance execution. `cargo xtask run-conformance --suite vue3-dom` now prepares and runs a Vitest suite instead of reporting `not-wired`; the prepared suite copies official `compiler-dom/__tests__`, official DOM `src`, compiler-core test utilities, and generated compiler-core import shims.
- `xtask/src/compat.rs` changed as test-runner/import support only: Vue 3 DOM now shares the Vitest runner path, installs `jsdom` for browser-decoder specs, overwrites only the prepared `compiler-dom/src/transforms/transformStyle.ts` import to route that focused transform through the existing Rust-backed alias export, and classifies the suite as `mixed` coverage. This does not count official DOM transform/parser failures as Rust compiler completion.
- The first real Vue 3 DOM conformance run reports `45/133` passing and `88` failing. Passing files include `decoderHtmlBrowser.spec.ts` (`1/1`), `transformStyle.spec.ts` (`2/2`), `vHtml.spec.ts` (`3/3`), and `vText.spec.ts` (`3/3`). Remaining failures are now visible and concentrated in Rust DOM parser rules (`parse.spec.ts` raw text/entity/namespace cases), DOM compile/codegen snapshots, `vModel`, `vOn`, `Transition`, `validateHtmlNesting`, `ignoreSideEffectTags`, and `stringifyStatic`.
- Verification for this Vue 3 DOM conformance-runner slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask` (`18/18` pass), `git diff --check`, and `cargo xtask run-conformance --suite vue3-dom` (expected fail with real execution result `45/133`, not pending/not-wired).

- Current round: closed the remaining Vue 3 compiler-core official conformance failures. `vuec_vue3_core::is_member_expression_projection` now provides a Rust/OXC-backed public utility projection for `isMemberExpressionBrowser` / `isMemberExpressionNode`, including official browser lexer behavior and TypeScript-aware node parsing. The previous `v-if` key-injection failure is fixed by aligning the alias materializer's `injectProp` behavior for `normalizeProps(guardReactiveProps(...))`, so Rust-selected branch key props are applied to the official nested call shape.
- `vuec_node_bridge` exposes `vue3.core.isMemberExpression`. `xtask/src/compat.rs` changed as import/API/test-runner and materialization support: generated aliases call Rust for `isMemberExpression`, expose `@vue/compiler-sfc` `babelParse` / `walkIdentifiers` through the existing runtime for the compiler-core utility test, and adjust `injectProp` operation materialization. The `babelParse` / `walkIdentifiers` support is not compiler semantics and is not counted as Rust compiler completion.
- Full Vue 3 compiler-core conformance now reports `652/652` official tests passing with `0` failures. Coverage remains honest: `rust-backed 158/158`, `mixed 494/494`, `shim-backed 0/0`; the remaining `mixed` classification means many internal transform/codegen tests still execute through the alias adapter even though the official Vue 3 core suite is green.
- Verification for this Vue 3 compiler-core conformance-closure slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`68/68` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-sfc`, focused prepared Vitest `utils.spec.ts` + `vIf.spec.ts` (`38/38` pass), and `cargo xtask run-conformance --suite vue3-core` (`652/652` pass).

- Current round: moved Vue 3 compiler-core `cacheStatic` / `getConstantType` decisions into a Rust-backed bridge projection. `vuec_vue3_core::cache_static_projection` now owns official static-cache walk decisions for static node/text caching, children-array caching, slot-return caching, props and dynamic-props hoisting, `CACHED` patch flags, SVG/math/foreignObject block downgrade rules, and custom-directive exclusions. `vuec_vue3_core::get_constant_type_projection` now owns the constant-type lattice for static expressions, interpolation/text calls, compound expressions, generated props, and allowed hoisted helper calls.
- `vuec_node_bridge` exposes `vue3.core.cacheStatic` and `vue3.core.getConstantType`. `xtask/src/compat.rs` changed as dehydration/materialization/codegen support: generated alias transforms dehydrate official-shaped AST summaries into Rust, apply Rust-returned cache/hoist/block operations through the transform context, materialize slot-return lookups, and align cacheStatic snapshot codegen details such as `CACHED` patch-flag labels, hoist blank lines, and pure helper-call annotations. These changes are adapter/materialization and test-runner support for Rust decisions, not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/cacheStatic.spec.ts` now reports `34/34` passing, up from `9/34` at the start of the slice and `0` remaining cacheStatic failures in the full suite. Adjacent focused `cacheStatic.spec.ts` + `utils.spec.ts` + `vIf.spec.ts` + `transformElement.spec.ts` reports `192/196`; the remaining failures are the existing `utils` member/reference helpers (`3`) and `vIf` key-injection case (`1`). Full Vue 3 compiler-core conformance now reports `648/652` passing and `4` failing. Coverage remains honest: `rust-backed 158/158`, `mixed 490/494`, `shim-backed 0/0`.
- Verification for this Rust-backed `cacheStatic` / `getConstantType` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`68/68` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `cacheStatic.spec.ts` (`34/34` pass), adjacent prepared Vitest `cacheStatic.spec.ts` + `utils.spec.ts` + `vIf.spec.ts` + `transformElement.spec.ts` (`192/196`, expected existing `utils`/`vIf` failures), and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `648/652` passing.

- Current round: moved Vue 3 compiler-core `transformOn` / `v-on` directive decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_on_projection` now owns official event name generation for static and dynamic args, `vue:` vnode hook normalization and diagnostics, OXC-backed handler classification for member/function/inline statement expressions, prefixIdentifiers rewriting with function parameter locals, cacheHandlers decisions, setup-const handler constness, dynamic handler-key patch flag behavior, and the official distinction between dynamic handler keys that set `FULL_PROPS` and handler keys that must not trigger `normalizeProps`.
- `vuec_node_bridge` exposes `vue3.core.transformOn`. `xtask/src/compat.rs` changed as bridge/materialization/props-summary support: generated alias `transformOn` calls Rust, forwards Rust diagnostics, materializes official-shaped key/value object-property AST nodes, applies Rust-selected handler caching through the transform context, and passes Rust-selected handler-key metadata into `transformElement` props analysis. These changes are adapter/materialization support for Rust decisions, not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vOn.spec.ts` now reports `33/33` passing, up from `30/33`. Adjacent focused official `vOn.spec.ts`, `vIf.spec.ts`, `vFor.spec.ts`, `vSlot.spec.ts`, and `transformElement.spec.ts` now reports `271/272`; the only remaining adjacent failure is the pre-existing `vIf` key-injection case. Full Vue 3 compiler-core conformance now reports `623/652` passing and `29` failing; remaining failures are `cacheStatic.spec.ts` (`25`), `utils.spec.ts` (`3`), and `vIf.spec.ts` (`1`). Coverage remains honest: `rust-backed 158/158`, `mixed 465/494`, `shim-backed 0/0`.
- Verification for this Rust-backed `transformOn` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`63/63` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `vOn.spec.ts` (`33/33` pass), adjacent prepared Vitest `vOn.spec.ts` + `vIf.spec.ts` + `vFor.spec.ts` + `vSlot.spec.ts` + `transformElement.spec.ts` (`271/272`, expected fail from existing `vIf`), `git diff --check`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `623/652` passing.

- Current round: moved Vue 3 compiler-core `buildSlots`, `trackSlotScopes`, and `trackVForSlotScopes` slot decisions into Rust-backed bridge projections. `vuec_vue3_core::build_slots_projection` now owns on-component slots, template named/default slots, dynamic slot names, v-if/v-else/v-for dynamic slot objects, duplicate/mixed/extraneous/misplaced slot diagnostics, slot flags (`STABLE` / `DYNAMIC` / `FORWARDED`), forwarded slot detection, prefixIdentifiers slot-scope tracking, and scope-ref based dynamic-slot forcing.
- `vuec_node_bridge` exposes `vue3.core.buildSlots`, `vue3.core.trackSlotScopes`, and `vue3.core.trackVForSlotScopes`. `xtask/src/compat.rs` changed as bridge/traversal/materialization support: generated alias traversal preserves official `<template v-slot>` structural-directive ownership, calls Rust slot projections, materializes official-shaped slot object / `createSlots` / `renderList` AST, applies Rust-returned slot locals to transform identifiers, forwards Rust-selected diagnostics, and keeps codegen helper order/conditional parentheses aligned with official snapshots. These changes are adapter/materialization support for Rust decisions, not standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vSlot.spec.ts` now reports `31/31` passing, up from `1/31`. Adjacent focused official `parse.spec.ts`, `vSlot.spec.ts`, `vFor.spec.ts`, and `transformElement.spec.ts` all pass together. Full Vue 3 compiler-core conformance now reports `620/652` passing and `32` failing; remaining failures are concentrated in `cacheStatic.spec.ts`, `utils.spec.ts`, and small `vIf` / `vOn` gaps. Coverage remains honest: `rust-backed 158/158`, `mixed 462/494`, `shim-backed 0/0`.
- Verification for this Rust-backed `buildSlots` / slot-scope projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`59/59` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `vSlot.spec.ts` (`31/31` pass), adjacent prepared Vitest `parse.spec.ts` + `vSlot.spec.ts` + `vFor.spec.ts` + `transformElement.spec.ts` (all pass), `git diff --check`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `620/652` passing.

- Current round: moved Vue 3 compiler-core `processFor` / `transformFor` parsing and main codegen decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_for_projection` now owns `v-for` expression splitting with skipped alias slots, destructuring alias handling, source/value/key/index locs, malformed/no-expression diagnostics, prefixIdentifiers rewriting, OXC-backed alias local extraction, template child-key diagnostics, fragment patch-flag selection, stable-fragment tracking, key property projection, and loop child-block wrapper decisions.
- `vuec_node_bridge` exposes `vue3.core.transformFor`. `xtask/src/compat.rs` changed as bridge/materialization/traversal adapter support: generated alias `processFor` / `transformFor` calls Rust projections, materializes official-shaped AST expressions/VNode calls, maintains v-for scope identifiers from Rust-returned locals, and forwards Rust-selected diagnostics. The only non-semantic alias compatibility change is generator indentation for existing snapshot output; it is formatting support and not counted as Rust compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vFor.spec.ts` now reports `53/53` passing, up from `17/53`. Full Vue 3 compiler-core conformance now reports `589/652` passing and `63` failing; `transformElement.spec.ts` is also `124/124`, while remaining failures are concentrated in `cacheStatic.spec.ts`, `vSlot.spec.ts`, `utils.spec.ts`, and small `vIf` / `vOn` gaps. Coverage remains honest: `rust-backed 158/158`, `mixed 431/494`, `shim-backed 0/0`.
- Verification for this Rust-backed `processFor` / `transformFor` projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`55/55` pass), `cargo build -p vuec_node_bridge`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `vFor.spec.ts` (`53/53` pass), `git diff --check`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `589/652` passing.

- Current round: added Rust-backed Vue 3 public `baseCompile` vnode-hook codegen compatibility. `vuec_vue3_core` now maps static `@vue:*` event args to official `onVnode*` handler prop names, excludes vnode hooks from dynamic prop lists, and emits `NEED_PATCH` only when no stronger patch flag is present.
- `vuec_node_bridge` now returns an official-shaped public `baseCompile` result with `ast`, `code`, `map`, and `preamble`; the returned AST includes Rust-projected element `codegenNode.patchFlag` values for this path. No `xtask/src/compat.rs` JavaScript shim semantics changed in this round.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformElement.spec.ts` now reports `88/124` passing, up from `87/124`; element-transform own failures are `0`. The remaining `36` failures in the focused file are existing `v-for` suite failures outside this vnode-hook slice.
- Verification for this Rust-backed public `baseCompile` vnode-hook slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`51/51` pass), `cargo build -p vuec_node_bridge`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `transformElement.spec.ts` (`88/124` pass, expected fail from `v-for`, element failures `0`), and `git diff --check`.

- Current round: added a Rust-backed Vue 3 `transformElement` children projection for built-in component lowering. `vuec_vue3_core::transform_element_children_projection` now owns the official children decisions for `Suspense`, `BaseTransition`, and `KeepAlive`: stable slot object projection for `Suspense` / `BaseTransition`, template `#default` / `#fallback` slot unwrapping, and `KeepAlive` raw children with `DYNAMIC_SLOTS` / block forcing.
- `vuec_node_bridge` exposes `vue3.core.transformElementChildren`. `xtask/src/compat.rs` changed as bridge/materialization support only: generated alias `transformElement` calls Rust for children lowering decisions and materializes returned slot functions / stable slot flags / KeepAlive patch flags into official-shaped JS AST. This is not counted as standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformElement.spec.ts` now reports `87/124` passing, up from `84/124`; element-transform own failures are down to `1`. The remaining element-transform failure is the `baseCompile` vnode-hook materialization path.
- Verification for this Rust-backed transformElement children projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`50/50` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `transformElement.spec.ts` (`87/124` pass, expected fail), and `git diff --check`.

- Current round: added a Rust-backed Vue 3 `buildDirectiveArgs` projection for runtime directive argument materialization. `vuec_vue3_core::build_directive_args_projection` now owns the directive runtime reference shape, inclusion of `exp` / `arg`, and modifier object entries for runtime directives.
- `vuec_node_bridge` exposes `vue3.core.buildDirectiveArgs`. `xtask/src/compat.rs` changed as bridge/materialization support only: generated alias `buildDirectiveArgs` calls Rust and materializes the returned projection into official-shaped JS array/object expression nodes, including modifier objects. This is not counted as standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformElement.spec.ts` now reports `84/124` passing, up from `83/124`; element-transform own failures are down to `4`. Remaining element failures are built-in component children/slot lowering for `Suspense` / `KeepAlive` / `BaseTransition`, and the `baseCompile` vnode-hook materialization path.
- Verification for this Rust-backed directive-args projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`49/49` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `transformElement.spec.ts` (`84/124` pass, expected fail), and `git diff --check`.

- Current round: extended the Rust-backed Vue 3 `transformElement` props projection slice for v-for scoped refs and selected block-forcing decisions. `vuec_vue3_core::transform_element_props_projection` now owns official decisions for inserting `ref_for` markers in `v-for`, forcing blocks for dynamic/static `:key` and `@vue:before-update` with children, and projecting inline template ref key materialization for script-setup refs.
- `xtask/src/compat.rs` changed as bridge/traversal/materialization support only: generated alias traversal now maintains `context.scopes.vFor`, forwards `vForDepth`, `inline`, and `bindingMetadata` facts to Rust, and materializes Rust-selected `ref_for` / `ref_key` props into official-shaped JS AST props. It also avoids calling the default `transformOn` for `v-on` when no directive transform was configured, matching official `buildProps` ownership boundaries. These changes do not count as standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformElement.spec.ts` now reports `83/124` passing, up from `77/124`; element-transform own failures are down to `5`. Remaining element failures are built-in component children/slot lowering for `Suspense` / `KeepAlive` / `BaseTransition`, runtime directive materialization exactness, and the `baseCompile` vnode-hook materialization path.
- Verification for this Rust-backed props/materialization slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`48/48` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `transformElement.spec.ts` (`83/124` pass, expected fail), and `git diff --check`.

- Current round: implemented a Rust-backed Vue 3 DOM `transformStyle` projection slice and tightened the related Vue 3 core props projection normalization rules. `vuec_vue3_dom::transform_style_projection` now owns static `style` attribute parsing for compiler-dom, including CSS comments, duplicate declarations, and semicolons inside function values, and returns deterministic official-shaped style object expressions.
- `vuec_node_bridge` exposes `vue3.dom.transformStyle`. `xtask/src/compat.rs` changed as bridge/materialization support only: generated compiler-dom `transformStyle` exports call the Vue 3 runtime adapter, the adapter replaces Rust-selected static style attributes with official-shaped `v-bind:style` directives, and `dedupeProperties` / `mergePropertyAsArray` materialize official class/style/event duplicate-prop arrays. These `compat.rs` changes do not count as standalone JavaScript compiler semantics.
- `vuec_vue3_core::transform_element_props_projection` now also owns official `normalizeStyle` decisions for style array literals and static-style + dynamic-style merge cases. The JS adapter only forwards final prop value shape facts (`valueType`, `valueStartsWithArray`) into Rust and applies the Rust-selected helper instruction.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformElement.spec.ts` now reports `77/124` passing, up from `72/124`; element-transform own failures are down to `11`. The file remains classified as `mixed` because built-in component children/slot lowering, runtime directive materialization, ref_for/v-for markers, and dynamic-key block forcing still need later Rust-backed slices.
- Verification for this Rust-backed DOM transformStyle / props materialization slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_dom -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_dom` (`3/3` pass), `cargo test -p vuec_vue3_core` (`45/45` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-dom`, and direct prepared Vitest `transformElement.spec.ts` (`77/124` pass, expected fail).

- Current round: implemented a Rust-backed Vue 3 `transformElement` props / patch-flag projection slice. `vuec_vue3_core::transform_element_props_projection` now owns core `buildProps` decisions for object `v-bind` / `v-on` full-props handling, class/style/props patch flags, ref/runtime-directive/vnode-hook need-patch decisions, hydration event flags, class/style normalization, normalizeProps / guardReactiveProps wrapping, and runtime-directive block forcing.
- `vuec_node_bridge` exposes `vue3.core.transformElementProps`. `xtask/src/compat.rs` changed as a bridge/materialization adapter: generated alias `transformElement` still calls directive transforms to materialize official-shaped JS AST properties, then sends a property summary to Rust for the semantic props/flag decisions and applies the returned instructions. This is adapter support for Rust decisions; it is not counted as standalone JavaScript compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformElement.spec.ts` now reports `72/124` passing, up from `57/124` after the previous component-resolution slice and `40/124` before the transformElement Rust migration began. The file remains classified as `mixed` because children/slot lowering, style transform, ref_for/v-for marker handling, and some VNode materialization details still run through or depend on the alias harness.
- Verification for this Rust-backed props/patch-flag projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`44/44` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `transformElement.spec.ts` (`72/124` pass, expected fail), and `git diff --check`.

- Current round: implemented a Rust-backed Vue 3 `resolveComponentType` projection slice as part of migrating `transformElement` semantics. `vuec_vue3_core::resolve_component_type_projection` now owns component tag resolution decisions for core built-ins, `<component is>`, dynamic `:is`, `vue:` is-casting, script-setup binding references, namespaced setup/props bindings, and implicit self-reference component registration.
- Rust Vue 3 parsing now classifies lowercase core built-ins (`teleport`, `suspense`, `keep-alive`, `base-transition`) and `<component>` / `<Component>` as component elements so the transform path reaches Rust-backed component resolution. `vuec_node_bridge` exposes `vue3.core.resolveComponentType`.
- `xtask/src/compat.rs` changed only as a bridge/materialization adapter for this slice: generated alias `resolveComponentType` calls Rust and materializes official-shaped helpers, component registrations, expressions, and `resolveDynamicComponent(...)` calls. The small block-forcing hook in alias `transformElement` uses the Rust-selected dynamic/core tag and remains adapter support; broad `buildProps`, `buildSlots`, patch-flag, and children semantics are still pending Rust migration.
- Focused official Vue 3 `compiler-core/__tests__/transforms/transformElement.spec.ts` now reports `57/124` passing, up from `40/124`. The file remains classified as `mixed` because transform traversal, props analysis, slot building, and VNode materialization still run through the alias harness, but component type resolution decisions in this slice are Rust-backed through `vue3.core.resolveComponentType`.
- Verification for this Rust-backed component resolution slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`40/40` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `transformElement.spec.ts` (`57/124` pass, expected fail), and `git diff --check`.

- Current round: implemented a Rust-backed Vue 3 `transformIf` / `processIf` projection bridge for compiler-core structural directive parity. `vuec_vue3_core::transform_if_projection` now owns v-if/v-else branch creation decisions, template branch child selection, condition prefixing decisions, adjacent branch scanning, comment carry-over, duplicate-key diagnostics, branch key-base calculation, and branch codegen wrapper decisions.
- `vuec_node_bridge` exposes `vue3.core.transformIf`. `xtask/src/compat.rs` changed as a bridge/API/test-runner adapter: generated alias `processIf` calls Rust for structural decisions and materializes official-shaped JS AST, while small materializer updates apply Rust-selected key injection to `VNodeCall` / `renderSlot` / merge-props forms. The touched `compat.rs` code is adapter/materialization support for Rust decisions, not counted as standalone JS compiler progress.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vIf.spec.ts` now reports `31/31` passing, up from `8/31`. The file remains classified as `mixed` because traversal, element transform, and codegen materialization still execute through the alias test harness, but the `processIf` structural decisions in this slice are Rust-backed through `vue3.core.transformIf`.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `467` passing and `185` failing. Coverage remains honest: `rust-backed: 158/158`, `mixed: 309/494`, `shim-backed: 0/0`. Remaining failures are concentrated in `transformElement`, `vFor`, `cacheStatic`, `vSlot`, `utils`, and `transformText`.
- Verification for this Rust-backed transformIf projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`34/34` pass), `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `vIf.spec.ts` (`31/31` pass), and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `467/652` passing.

- Current round: implemented Vue 3 `v-memo` codegen in the Rust public `baseCompile` path. `vuec_vue3_core` now emits `_withMemo`, `_isMemoSame`, memo cache indexes, v-if branch memo wrappers, and v-for memo cache guards / `_item.memo` assignment from Rust codegen.
- `vuec_ast::RuntimeHelper` now includes `withMemo` and `isMemoSame`. Vue 3 helper collection was tightened to preserve official snapshot import order for memo, component, v-if, v-for, and scopeId cases while avoiding unused helper imports in dynamic slots.
- This round did not change `xtask/src/compat.rs`; no JavaScript alias-runtime compiler semantics were added. The focused `vMemo.spec.ts` file is still reported as `mixed` by the current file-level classifier because it lives under transform tests, but the `baseCompile` outputs exercised here are Rust-backed through `vuec_node_bridge`.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vMemo.spec.ts` now reports `7/7` passing, up from `0/7`. Focused `compile.spec.ts` remains `3/3` and `scopeId.spec.ts` remains `4/4`, confirming no public compile/scopeId regression.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `438` passing and `214` failing. Coverage remains honest: `rust-backed: 158/158`, `mixed: 280/494`, `shim-backed: 0/0`. Remaining failures are concentrated in `transformElement`, `vFor`, `cacheStatic`, `vSlot`, `vIf`, and `utils`.
- Verification for this Rust-backed v-memo codegen slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core` (`34/34` pass), `cargo build -p vuec_node_bridge`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `vMemo.spec.ts` (`7/7` pass), focused prepared Vitest `compile.spec.ts` (`3/3` pass), focused prepared Vitest `scopeId.spec.ts` (`4/4` pass), `git diff --check`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `438/652` passing.

- Current round: moved Vue 3 compiler-core `transformModel` / `v-model` directive transform decisions into a Rust-backed bridge projection. `vuec_vue3_core::transform_model_projection` now owns `v-model` error codes, prop/event/modifier key projection, assignment expression projection, cache/dynamic prop decisions, static-argument hydration flagging, and OXC-backed member-expression validation.
- `vuec_node_bridge` exposes `vue3.core.transformModel`. `xtask/src/compat.rs` only changed as bridge adapter/API support: generated alias runtime calls the Rust projection, materializes official-shaped JS AST nodes from returned instructions, forwards Rust-selected diagnostics, and uses the Rust-selected dynamic/cache/hydration flags. The patch flag comment name was also synced from `HYDRATE_EVENTS` to the locked official `NEED_HYDRATION`; this is output enum text compatibility, not compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transforms/vModel.spec.ts` now reports `21/21` passing, up from `1/21`. The file remains classified as `mixed` because surrounding transform traversal/element codegen still runs through alias runtime, but the v-model directive decisions in this slice are Rust-backed through `vue3.core.transformModel`.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `431` passing and `221` failing. Coverage remains honest: `rust-backed: 158/158`, `mixed: 273/494`, `shim-backed: 0/0`. Remaining failures are concentrated in `transformElement`, `vFor`, `cacheStatic`, `vSlot`, `vIf`, `vMemo`, and `utils`.
- Verification for this Rust-backed v-model transform projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core transform_model_projection`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `vModel.spec.ts` run (`21/21` pass), `git diff --check`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `431/652` passing.

- Current round: moved Vue 3 compiler-core root `codegenNode` selection into a Rust-backed bridge projection. `vuec_vue3_core::root_codegen_projection` now mirrors the official root rules for empty roots, single non-slot element codegen/block conversion, slot outlets, structural roots (`IF`/`FOR`), and root fragments with `STABLE_FRAGMENT | DEV_ROOT_FRAGMENT`.
- `vuec_node_bridge` exposes the new `vue3.core.rootCodegen` bridge command. `xtask/src/compat.rs` only changed as a bridge adapter: generated alias runtime calls the Rust projection and applies the returned instruction to the already-hydrated official AST shape. This is bridge/API/test-runner support, not JS compiler semantics.
- Focused official Vue 3 `compiler-core/__tests__/transform.spec.ts` now reports `21/21` passing; the previous 4 root-codegen failures are closed by the Rust-backed projection bridge. The file remains classified as `mixed` because the surrounding transform walker and internal transform imports still execute through the alias runtime.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `409` passing and `243` failing. Coverage remains honest: `rust-backed: 158/158`, `mixed: 251/494`, `shim-backed: 0/0`. Remaining failures are concentrated in `transformElement`, `vFor`, `cacheStatic`, `vSlot`, `vIf`, `vModel`, `vMemo`, and `utils`.
- Verification for this Rust-backed root codegen projection slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core root_codegen_projection`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `transform.spec.ts` run (`21/21` pass), and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `409/652` passing.

- Current round: implemented a Rust-backed Vue 3 `baseCompile` component slot / `scopeId` slice in `vuec_vue3_core`. Rust codegen now resolves component assets with `_resolveComponent`, emits `_createBlock` for component roots, lowers component children into stable slots with `_withCtx`, and emits dynamic slots with `_createSlots`, `_renderList`, and `1024 /* DYNAMIC_SLOTS */`.
- `vuec_ast::RuntimeHelper` now includes Vue 3 component/slot helpers (`resolveComponent`, `withCtx`, `createBlock`, `createVNode`, `createSlots`). `vuec_vue3_core` collects component slot helper needs in Rust and preserves existing public `compile.spec.ts` helper ordering for non-component paths.
- This round did not change `xtask/src/compat.rs`; the official `scopeId.spec.ts` improvement comes through the public `baseCompile` bridge into Rust parser/transform/codegen, not through alias-runtime compiler semantics.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `405` passing and `247` failing. `scopeId.spec.ts` is now `4/4` passing. Remaining failures are still dominated by internal transform/codegen parity (`transformElement`, `vFor`, `vSlot`, `vIf`, `vModel`, `cacheStatic`) and remain outside this Rust-backed public compile slice.
- Verification for this Rust-backed component slot / scopeId slice: `cargo fmt --all --check`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused prepared Vitest `scopeId.spec.ts` run (`4/4` pass), focused prepared Vitest `compile.spec.ts` + `codegen.spec.ts` run (`3/3` and `34/34` pass), `git diff --check`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `405/652` passing.

- Current round: completed the Rust-backed Vue 3 `compiler-core` parser recovery slice to official `parse.spec.ts` parity. Shared `vuec_html` now tokenizes invalid-space end tags, empty end tags, bogus question tags, and invalid `=`-prefixed attributes as observable markup boundaries instead of folding them into text.
- `vuec_vue3_core::base_parse` now matches official recovery for invalid/non-matching end tags, raw-text `textarea`/`title` content, complete vs incomplete empty end tags, bogus `<?...>` nodes, EOF comments, adjacent text merging across skipped bogus markup, and EOF start-tag recovery. Public projection handles empty interpolation inner locs, dynamic directive arguments missing `]`, and the official EOF trailing-solidus loc quirk at bridge projection time.
- `vuec_node_bridge` parse diagnostics now cover these recovery paths with official messages/locations, including `UNEXPECTED_EQUALS_SIGN_BEFORE_ATTRIBUTE_NAME`, `MISSING_END_TAG_NAME`, `UNEXPECTED_QUESTION_MARK_INSTEAD_OF_TAG_NAME`, invalid-space `X_INVALID_END_TAG`, EOF comment/tag, and dynamic directive argument recovery.
- `xtask/src/compat.rs` changes in this round are API/test-runner support only: generated Vue 3 aliases hydrate additional official parser error messages and the prepared Vitest harness installs a `toHaveBeenWarned` matcher for official warning assertions. Compiler semantics for parser/tokenizer/diagnostics remain Rust-backed.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `402` passing and `250` failing. `parse.spec.ts` is now `151/151` passing. Remaining failures are concentrated in transform/codegen suites and are outside this parser recovery slice.
- Verification for this Rust-backed parser completion slice: `cargo fmt --all --check`, `cargo test -p vuec_html -p vuec_ast -p vuec_vue2 -p vuec_vue3_core -p vuec_node_bridge`, `cargo check -p xtask`, `cargo build -p vuec_node_bridge`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, focused official-vs-Rust alias parser recovery smoke comparisons, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `402/652` passing.

- Current round: added Rust-backed Vue 3 namespace-aware CDATA parsing for official `parse.spec.ts`. `Vue3CompilerOptions` now carries deterministic tag namespace overrides, `vuec_vue3_core::base_parse` maintains a namespace stack, projects configured SVG namespace on elements, emits CDATA body text only in XML namespace with body-only spans, and drops CDATA children in HTML namespace.
- `vuec_node_bridge` now parses the serialized namespace override map and uses the same namespace stack for CDATA diagnostics. The generated Vue 3 alias only gained an API adapter that samples function-valued `getNamespace` over tags present in the template; compiler CDATA semantics stay in Rust.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `386` passing and `266` failing. `parse.spec.ts` is now `135/151` passing with `16` failures remaining, mostly raw-text/invalid-end-tag/error-message exactness plus the `decodeEntities` warning matcher setup gap.
- Verification for this namespace/CDATA slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge`, `git diff --check`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct official-vs-Rust alias CDATA namespace smoke comparisons, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `386/652` passing.
- Current round: tightened Rust-backed Vue 3 EOF/error-recovery parsing for official `parse.spec.ts`. `vuec_vue3_core::base_parse` now extends still-open element spans to EOF, treats an empty incomplete end tag like `</` as text under the current parent, and suppresses EOF-interrupted child start tags from becoming real AST elements while preserving recoverable trailing solidus text.
- `vuec_node_bridge` parse diagnostics now distinguishes true EOF-in-tag from an incomplete start tag interrupted by a later end tag, so the IDE recovery case keeps the official missing-end-tag diagnostic path instead of reporting a synthetic EOF_IN_TAG.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `380` passing and `272` failing. `parse.spec.ts` is now `129/151` passing with `22` failures remaining; these are concentrated in CDATA/raw-text/error-snapshot exactness plus the non-compiler `decodeEntities` warning matcher setup case.
- Verification for this Rust-backed EOF recovery slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_html -p vuec_node_bridge`, `git diff --check`, direct official-vs-Rust alias smoke comparisons for EOF and IDE recovery inputs, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `380/652` passing.
- Current round: extended the Rust-backed Vue 3 `baseParse` parser/diagnostic path for official parser option and recovery parity. `vuec_node_bridge` now forwards `whitespace`, `isPreTag`, and `isIgnoreNewlineTag` hits into `Vue3CompilerOptions`, and parse diagnostics now use the same `voidTags`, custom delimiters, and `v-pre` state as the Rust parser.
- `vuec_html::HtmlTokenizer` now terminates an incomplete start tag before a following end tag, so the official IDE recovery case `<template><Hello\n</template><script>...` keeps `<script>` at root level. `vuec_vue3_core::base_parse` also extends unmatched element spans to the interrupting end tag, matching the official invalid-HTML snapshot for missing end tags.
- `xtask/src/compat.rs` only changed API/hydration behavior in this round: generated Vue 3 aliases sample `isPreTag` / `isIgnoreNewlineTag` function options against tags present in the current template, and hydrate error code 24 with the official message `Element is missing end tag.`. No compiler semantics were moved into the JS shim.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `366` passing and `286` failing. `parse.spec.ts` is now `115/151` passing with `36` failures remaining: `35` official parser error-recovery/snapshot cases plus the non-compiler `decodeEntities` warning matcher setup case.
- Verification for this parser option/recovery slice: `cargo fmt --all --check`, `cargo check -p vuec_html -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_html -p vuec_vue3_core -p vuec_node_bridge`, `cargo build -p vuec_node_bridge`, direct alias smoke checks for void tags, `v-pre` half-open interpolation, pre/whitespace options, IDE tag termination, invalid HTML span recovery, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `366/652` passing.
- Current round: implemented a Rust-backed Vue 3 `baseParse` parser/projection slice in `vuec_vue3_core` and `vuec_node_bridge`. Parser now handles builtin text entity decoding, `v-pre` raw parsing semantics, component/directive suppression inside `v-pre`, and half-open interpolation inside `v-pre` by switching the shared tokenizer to streaming mode with runtime interpolation delimiter control.
- Vue 3 public AST projection now fills the official parse-expression `ast` surface for `prefixIdentifiers` through Rust/OXC-backed projection: interpolation and dynamic `v-bind` expressions expose `BinaryExpression` / `CallExpression`, `v-on` multi-statements expose `Program.body`, `v-slot` params expose `ArrowFunctionExpression`, and `v-for` exposes `forParseResult` with source/value/key/index simple expressions.
- Vue 3 parse diagnostics now cover a broader Rust bridge subset of official parser errors: CDATA-in-HTML, duplicate attributes, EOF-before-tag-name, EOF-in-CDATA/comment/tag, missing attribute value, missing end tag name, unexpected characters in attribute names / unquoted values, unexpected equals before attribute names, unexpected question mark, unexpected solidus, invalid end tag, missing end tag, missing interpolation end, missing directive name, and missing dynamic directive argument end.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `356` passing and `296` failing. Coverage reports `rust-backed.pass=108`, `rust-backed.total=154`, `mixed.pass=248`, and `mixed.total=498`; `parse.spec.ts` is now `105/151` passing while `compile.spec.ts` remains `3/3` passing.
- Verification for this Rust-backed parser/OXC projection slice: `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_html -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_html -p vuec_vue3_core -p vuec_node_bridge`, `cargo build -p vuec_node_bridge`, direct alias smoke checks for official parse-expression AST fields, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `356/652` passing and file-level coverage classification.
- Current round: implemented the first Vue 3 compiler-core Rust-backed `baseCompile` integration slice in `vuec_vue3_core`. Rust codegen now handles the official integration fixture shape for static props, `:class` / `_normalizeClass`, `v-if` + `v-else`, basic `v-for` / `_renderList` / `_Fragment`, text vnode creation, v-for alias scoping under `prefixIdentifiers`, helper import/destructure ordering, and source-map mappings needed by official `compile.spec.ts`.
- `vuec_node_bridge` now reads `options.filename` into `TemplateSource.filename`, so public `baseCompile(..., { sourceMap: true, filename })` returns official-shaped source-map `sources` / `sourcesContent`.
- `cargo xtask run-conformance` now aggregates Vue 3 compiler-core coverage by file. Public API files such as `compile.spec.ts` and `parse.spec.ts` are classified as `rust-backed`; internal transform/codegen files that still exercise `xtask/src/compat.rs` semantics remain `mixed`.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with `334` passing and `318` failing. Coverage now reports `rust-backed.pass=86`, `rust-backed.total=154`, `mixed.pass=248`, and `mixed.total=498`. The full suite still honestly fails and is not completion evidence.
- Verification for this Rust-backed compile slice: `cargo fmt --all --check`, `cargo check -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_ast -p vuec_vue3_core -p vuec_node_bridge`, `cargo test -p xtask`, `git diff --check`, focused prepared Vitest `compile.spec.ts` run (`3/3` pass), focused prepared Vitest `transformExpressions.spec.ts` + `compile.spec.ts` run (`50/50` pass), and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `334/652` passing and file-level coverage classification.
- Current round: implemented machine-readable conformance coverage classification in `cargo xtask run-conformance`. Vue 3 compiler-core reports now include `coverage.source`, `coverage.counts_by_source`, `rust_backed_pass`, `rust_backed_total`, and per-test-file coverage entries.
- Latest `vue3-core.json` now classifies the current `331/652` official compiler-core result as `mixed`, with `mixed.pass=331`, `mixed.fail=321`, and `rust-backed.pass=0`. This makes the current prepared-suite pass count a compatibility signal rather than Rust compiler completion evidence.
- Verification for this reporting slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with coverage fields present in `target/conformance/ee33465b421a58b83fac04aa850a6d250ee09ec169fa80ed8820794a0c9a2769/vue3-core.json`.
- Current steering update: Vue 3 compiler-core official conformance pass counts must now be classified as `rust-backed`, `shim-backed`, or `mixed`. Focused specs that pass through `xtask/src/compat.rs` JavaScript alias runtime are alias-runtime slices, not Rust compiler parity.
- `compat.rs` should stop growing into a second compiler-core implementation. Its intended role is package/import compatibility, AST hydration/dehydration, and temporary adapter code. Compiler semantics such as `processExpression`, `transformExpression`, `transformElement`, `buildProps`, structural transforms, text transforms, slots, and codegen need Rust-backed implementations before they can count toward final Rust compiler completion.
- Current Vue 3 core conformance number `331/652` remains useful as a prepared-suite compatibility signal, but it must not be read as `331` Rust-backed passing tests until the report classifies each pass by execution source.
- Current round: closed the official Vue 3 `compiler-core/__tests__/transforms/vBind.spec.ts` slice through the alias runtime. The focused spec now passes `19/19` in the prepared Vue 3 core Vitest tree.
- Vue 3 compiler-core alias runtime `transformVBindShorthand` and `transformBind` now mirror official same-name shorthand expansion, empty-expression diagnostics, dynamic argument `|| ""` guards, `.camel`, `.prop`, `.attr`, prefixIdentifiers compound argument handling, and `NORMALIZE_PROPS` wrapping for dynamic keys in element props.
- Latest full Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with 331 passing and 321 failing. This improves from the previous 311/652 state. Adjacent `transformElement.spec.ts` remains broad and incomplete at `30/124`.
- Verification for this `v-bind` slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask`, `git diff --check`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `vBind.spec.ts` run (`19/19` pass), direct prepared Vitest `transformSlotOutlet.spec.ts` run (`14/14` pass), adjacent prepared Vitest `transformElement.spec.ts` run (`30/124` pass, expected fail), `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `331/652` passing.
- Current round: closed the official Vue 3 `compiler-core/__tests__/transforms/transformSlotOutlet.spec.ts` slice through the alias runtime. The focused spec now passes `14/14` in the prepared Vue 3 core Vitest tree.
- Vue 3 compiler-core alias runtime `processSlotOutlet` now mirrors official slot outlet name selection, dynamic `:name` handling with `prefixIdentifiers`, slot prop object construction, fallback function arguments, `scopeId` + `slotted: false` argument retention, and custom-directive diagnostics on `<slot>`.
- Latest full Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with 311 passing and 341 failing. This improves from the previous 298/652 state. Adjacent `vBind.spec.ts` remains `1/19` and is still a separate broader directive-transform slice.
- Verification for this slot outlet slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask`, `git diff --check`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `transformSlotOutlet.spec.ts` run (`14/14` pass), adjacent prepared Vitest `vBind.spec.ts` run (`1/19` pass, expected fail), `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `311/652` passing.
- Current round: closed the official Vue 3 `compiler-core/__tests__/transforms/transformText.spec.ts` and `compiler-core/__tests__/transforms/vOnce.spec.ts` slices through the alias runtime. The focused specs now pass `9/9` and `8/8` respectively in the prepared Vue 3 core Vitest tree.
- Vue 3 compiler-core alias runtime now mirrors official text merging / `createTextVNode` pre-conversion rules, preserves element-between whitespace needed by `transformText`, skips `v-once` / `v-memo` as runtime directives in `transformElement`, resolves component assets through `_component_*`, emits basic `FOR` codegen, and caches `v-once` codegen with `setBlockTracking`.
- Latest full Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with 298 passing and 354 failing. This improves from the previous 257/652 state. Adjacent `vFor.spec.ts` / `vIf.spec.ts` focused status also improved to `25/84` passing while remaining intentionally incomplete.
- Verification for this text / once slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p xtask`, `cargo test -p xtask`, `git diff --check`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `transformText.spec.ts` run (`9/9` pass), direct prepared Vitest `vOnce.spec.ts` run (`8/8` pass), adjacent prepared Vitest `vFor.spec.ts` + `vIf.spec.ts` run (`25/84` pass, expected fail), `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `298/652` passing.
- Current round: closed the official Vue 3 `compiler-core/__tests__/transforms/vOn.spec.ts` compatibility slice through the alias runtime. The focused spec now passes `33/33` in the prepared Vue 3 core Vitest tree.
- Vue 3 compiler-core alias runtime `transformOn` now follows more official semantics for static/dynamic handler keys, `vue:` vnode hook names, vnode hook diagnostics, inline statement wrapping, member-expression invocation wrapping, `prefixIdentifiers`, `$event` locals, `cacheHandlers`, and v-for alias cache bailouts. `transformElement` now consumes directive transform results for `v-on` / `v-bind` instead of synthesizing those props independently.
- Latest full Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with 257 passing and 395 failing. This improves from the previous 227/652 state. Remaining failures are still broad parser/transform/codegen parity gaps outside this `v-on` slice.
- Verification for this `v-on` slice: `cargo fmt --all --check`, `cargo check -p xtask`, `cargo test -p xtask`, `git diff --check`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, direct prepared Vitest `vOn.spec.ts` run (`33/33` pass), `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with `257/652` passing.
- Current round: closed the remaining official Vue 3 `compiler-core/__tests__/transforms/transformExpressions.spec.ts` failures. The focused spec now passes 47/47 through the prepared alias Vitest runner.
- Vue 3 alias/runtime and Rust core expression handling now cover object method keys, `SETUP_LET` assignment rewriting, `bindingMetadata` inline/non-inline access, literal const text patch skipping, raw `v-on` handler statement rewriting, and loop/catch/switch local-scope behavior needed by the official expression transform tests.
- `vuec_node_bridge` now forwards Vue 3 `bindingMetadata`, `__propsAliases`, and `inline` options into `Vue3CompilerOptions`; `vuec_vue3_core` uses them for prefixed interpolation and event handler codegen, including official render args for non-inline binding metadata.
- Latest full Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with 227 passing and 425 failing. This improves from the previous 214/652 state. Remaining failures are now concentrated outside `transformExpressions.spec.ts`, especially `transformElement` / `buildProps`, `v-if`, `v-for`, slots, parser exactness, and other transform/codegen contracts.
- Verification for this expression-closure slice: `cargo fmt --all --check`, `cargo check -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `cargo test -p vuec_vue3_core -p vuec_node_bridge -p xtask`, `git diff --check`, `cargo build -p vuec_node_bridge`, direct prepared Vitest `transformExpressions.spec.ts` run (`47/47` pass), `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with 227/652 passing.
- Current round: implemented a real Vue 3 compiler-core alias runtime expression transform in `xtask/src/compat.rs`. `processExpression` / `transformExpression` now lazily use `@babel/parser`, traverse expression ASTs, prefix identifiers through `_ctx`, `$setup`, `$props`, and `__props`, handle simple local scopes for functions/objects/members/variables/catch params, report parse failures through compiler error 46, and keep a lexer fallback when the parser is unavailable.
- The generated alias wrappers now use normal function bodies for non-public arity wrappers so `arguments` forwarding works, and `namedArity()` returns a bound function to avoid an own `prototype`, preserving public API manifest parity.
- Latest focused Vue 3 expression-transform state: official `compiler-core/__tests__/transforms/transformExpressions.spec.ts` improved to 34/47 passing. Latest full Vue 3 core conformance state improved to 214/652 passing and 438 failing. Remaining expression failures are exact official semantics for object property keys, loop/catch/switch local scopes, and bindingMetadata inline/non-inline edge cases.
- Verification for this expression slice: `cargo fmt --all --check`, `cargo check -p xtask -p vuec_node_bridge`, `cargo test -p xtask`, `cargo xtask diff-api --version-line vue3 --package @vue/compiler-core`, `cargo xtask verify-npm-alias --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core`, `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core`, and `cargo xtask run-conformance --suite vue3-core` as an expected failing full suite with 214/652 passing.
- Design constraint update: `docs/3.AST_HIR_MIR_DESIGN.md` is the current authoritative AST/HIR/MIR contract. New compiler work must treat `Vue2Ast`, `Vue3Ast`, `HIR`, `Vue2Mir`, `Vue3DomMir`, `Vue3SsrMir`, and `VaporMir` as first-class target documents and keep JS expressions represented through `vuec_js::JsAstStore` ids rather than raw strings as the only semantic source.
- Current round: completed a Vue 3 compiler-core alias runtime codegen slice in `xtask/src/compat.rs`. The generated alias runtime now emits official-shaped `generate()` output for module/function preambles, optimized imports, helper destructuring, assets, hoists, temps, VNode calls, object/array/function/conditional/cache/template/SSR statement nodes, and patch flag comments. The official Vue 3 `compiler-core/__tests__/codegen.spec.ts` now passes 34/34 through the prepared alias Vitest runner.
- Latest Vue 3 core conformance state: `cargo xtask run-conformance --suite vue3-core` runs 652 official tests with 184 passing and 468 failing. This is an improvement from the previous 149/652 baseline. Remaining failures are now dominated by parser edge cases and transform semantics outside standalone codegen, including `transformElement` / `buildProps`, expression rewriting, `v-if`, `v-for`, slot transforms, text transforms, cache/static transforms, and parser whitespace/v-pre/diagnostic coverage.
- Current round: aligned `docs/2.DEVELOPMENT_PLAN.md` and `docs/goal.md` to the deterministic AST/HIR/MIR contract in `docs/3.AST_HIR_MIR_DESIGN.md`, emphasizing that `Vue2Ast`, `Vue3Ast`, `HIR`, `Vue2Mir`, `Vue3DomMir`, `Vue3SsrMir`, and `VaporMir` are first-class target documents with fixed public projection, lowering, and target-split rules.
- Active objective: build a Rust Vue compiler that can replace official Vue 2.6, Vue 2.7, and Vue 3 compiler flows.
- Current focus: use the now-real Vue 3 compiler-core official conformance runner to drive missing parser/transform/codegen compatibility while keeping implementation aligned with the deterministic AST/HIR/MIR contract.
- `cargo xtask run-conformance --suite vue3-core` now executes the official Vue 3 `packages/compiler-core/__tests__` suite through a prepared Vitest tree and generated Rust alias shims. Current result is real failure, not pending: 652 official tests run, 134 pass, 518 fail. The dominant remaining failures are transformElement/buildProps semantics, v-if/v-for codegen, expression rewriting, exact codegen snapshots, and remaining parser edge cases around whitespace, v-pre, parse-expression ASTs, and full diagnostic coverage.
- The Vue 3 core conformance runner copies official tests into `target/conformance/<lock-hash>/prepared/vue3-core`, generates `packages/compiler-core/src/*` and transform shims that load the Rust alias package plus its non-enumerable `__vuecRuntime` internal compatibility surface, and runs Vitest from the pinned npm runner install. Zero-test executions are explicitly fail, not pass.
- Vue 3 compiler-core alias now has a first public runtime compatibility layer for enum values, runtime helper symbols, `helperNameMap`, `locStub`, AST builder functions, source-position helpers, compiler errors, a JS-side transform context/walker, basic structural directive transforms, minimal core transform shims, and a minimal `generate` implementation. This removes the previous broad `transform` / `createSimpleExpression` not-implemented failure class without treating the suite as passed.
- Vue 3 parser now projects raw directive attributes such as `:class`, `@click`, `#default`, and `v-*` into `Vue3Prop::Directive` with directive name, raw name, arg, expression, modifiers, and dynamic-arg metadata before bridge hydration to official-like public AST objects.
- Vue 3 parser/projection now carries lossless attribute name/value spans and quote kind from `vuec_html::HtmlTokenizer` into `Vue3Attribute` / `Vue3Directive`, so Node bridge public AST projection emits deterministic official-style `loc`, `nameLoc`, quoted `value.loc.source`, directive `arg` / `exp` / modifier locations, and undefined hydration for absent fields. The current parser slice also preserves official directive `arg` raw bracket spans, directive `exp` whitespace, and the `.prop` synthetic modifier's empty `loc` / non-static shape in public projection instead of normalizing them away.
- Vue 3 parser options now cover the first official parse-spec set: `comments`, custom `delimiters`, `isVoidTag`, `isNativeTag`, `isCustomElement`, and `isBuiltInComponent`. Function-valued tag predicates are sampled in the generated JS alias against tags present in the current template, then passed to Rust as deterministic arrays.
- Vue 3 parse diagnostics now flow from the Rust bridge back through alias hydration to caller-provided `onError` callbacks for the implemented diagnostic subset: invalid first tag character, missing interpolation end, invalid end tag, and missing directive name. Diagnostics intentionally remain incomplete and are not marked passing globally.
- `docs/3.AST_HIR_MIR_DESIGN.md` is the authoritative AST/IR contract; `vuec_ast` now has the deterministic arena root, `NodeSpan`, `LoweringMap`, public projection scaffolding, CST structs, first-class Vue2Ast/Vue3Ast/HIR schema structs, HIR without runtime helper/codegen call variants, target-split MIR enums, runtime-helper enum, and JS id model.
- `vuec_ast` now exposes concrete Vue 2 AST structs (`Vue2Root`, `Vue2Element`, `Vue2Text`, `Vue2ExpressionText`, `Vue2Comment`, Vue 2 filter/model/directive/event structs) and concrete Vue 3 AST structs (`Vue3Root`, `Vue3Element`, `Vue3Text`, `Vue3Comment`, `Vue3Interpolation`, `Vue3CompoundExpression`, `Vue3If`, `Vue3IfBranch`, `Vue3For`, `Vue3TextCall`) while keeping compatibility constructors for existing call sites.
- `vuec_vue3_core`, `vuec_vue3_dom`, `vuec_vue3_ssr`, `vuec_vue2`, and `vuec_node_bridge` have been migrated to the new tuple-variant AST schema without changing current public compiler behavior.
- Vue 3 `TemplateSource.base_offset` now correctly maps closing-tag-updated element spans back to absolute original-file offsets; a regression test covers a template block starting at offset 42.
- `vuec_js` now has the first real registry/store layer: `JsEntry`, `JsSourceType`, id allocation for expressions/statements/patterns/programs, lookup APIs, and parse-by-id APIs while preserving the old direct parse wrappers.
- Vue 3 `@vue/compiler-core` now validates interpolation expressions through `vuec_js::JsAstStore` and Oxc, with `isTS` / `expressionPlugins: ["typescript"]` selecting TypeScript expression parsing.
- Vue 3 `@vue/compiler-core` option matrix now has all 8 schema v2 rows passing for the current fixtures: prefixIdentifiers, mode, hoistStatic, cacheHandlers, scopeId, slotted, isTS, and expressionPlugins.
- Vue 3 core now mirrors official `cacheHandlers` / `scopeId` invalid option-combination errors through the generated npm alias preflight layer, lowers `<slot>` to `renderSlot`, and emits the current static-child cache signal for `hoistStatic`.
- `vuec_sfc::compile_script` now registers normal script and script setup blocks as `JsProgramId`s and serializes official-style `type`, `setup`, `lang`, `imports`, `scriptAst`, `scriptSetupAst`, and `deps` fields.
- Vue 3 SFC option matrix now has parse, compileTemplate, compileScript, and compileStyle executable rows passing for the current schema v2 fixtures.
- Vue 3 `@vue/compiler-ssr` option matrix now has scopeId and slotted executable rows passing for the current schema v2 fixtures.
- Vue 3 `@vue/compiler-dom` option matrix now has prefixIdentifiers, transformAssetUrls, decodeEntities, and isCustomElement executable rows passing for the current schema v2 fixtures.
- Vue 2.7 `vue/compiler-sfc` now uses version-specific npm alias bridge commands and has parse, compileTemplate, compileScript, and compileStyle option rows passing for the current schema v2 fixtures.
- Vue 2.7 `vue/compiler-sfc` output-contract probing now uses the official 2.7 single-object `parse({ source, filename })` API instead of the Vue 3 `parse(source, options)` call shape; `cargo xtask run-output-contract --all` has no failing checks and all seven targets are now `5/5`, with runtime parity executed directly in the probe.
- Vue 2.7 `vue-template-compiler` option matrix now has warn, modules, and directives executable rows passing for the current schema v2 fixtures.
- Vue 2.6 `vue-template-compiler` option matrix now has all 10 schema v2 rows passing, including `outputSourceRange` on the malformed tag fixture.
- Vue 2 parser recovery now closes through a matching end tag and reports the unmatched intermediate element once, matching the current Vue 2.6 `outputSourceRange` fixture. Vue 2 compile defaults now enable static optimization like the official compiler.
- Vue 2 public compile projection now classifies compiler warnings into official `errors`, serializes `errors` / `tips` as string arrays unless `outputSourceRange` is enabled, and hides internal `diagnostics` from the npm alias surface.
- `vuec_ast` now carries the new base arena shape from the AST/HIR/MIR design: `AstDocument.root` is a required `NodeId`, parent/index tracking is validated by `validate_tree`, `NodeSpan::Generated` / `Missing` have explicit constructors, `LoweringMap` has explicit AST->HIR and HIR->MIR edge APIs, and generic nested `PublicProjection` is available for deterministic snapshots.
- `vuec_ast` no longer exposes the old generic `MirNodeKind` model. MIR is split into `Vue2Mir`, `Vue3DomMir`, `Vue3SsrMir`, and `VaporMir`, wrapped by `Mir` with a target discriminator so downstream codegen can be made target-specific.
- `vuec_pass` now uses `RuntimeHelper` enums and exposes a depth-first `DocumentPass` walker over `AstDocument`, so the transform layer can align with the new IR plan.
- Vue 3 SFC work is now past the skeleton stage: template compilation keeps raw source for official-style output and source maps, module-mode render codegen emits direct Vue imports, and side-effect `<script>/<style>` handling is AST/diagnostic-driven.
- Remaining Vue 3 parity work is broader output-contract and official test execution coverage beyond the current option-matrix fixtures, including exact AST/codegen shape where the matrix only checks a deterministic compatibility signal.
- Compatibility rule: Vue 2.7 SFC export is `vue/compiler-sfc`, not a standalone `@vue/compiler-sfc@2.7.x` package.
- Workspace now has initial crates for `vuec_source`, `vuec_diagnostics`, `vuec_html`, `vuec_js`, `vuec_ast`, `vuec_pass`, `vuec_codegen`, plus an `xtask` compatibility harness scaffold.
- Workspace now also has initial `vuec_sfc` and `vuec_vue3_core` crates with compileable public interfaces and smoke tests.
- Added `vuec_vue3_dom`, `vuec_vue3_ssr`, and `vuec_style` crates. These are functional early backends with unit tests, not official parity-complete implementations.
- `vuec_sfc` now routes `compileTemplate` through DOM or SSR backends, routes `compileStyle` through `vuec_style`, and uses Oxc-backed `vuec_js` program summaries for script bindings/errors.
- `xtask sync-official-tests --locked` now performs real git checkout of the official Vue 2.6 / 2.7 / 3 repositories pinned by `compat/official-revisions.lock`.
- `xtask run-conformance --all` now discovers official test files, checks Rust alias package readiness, provisions deterministic official runner dependencies from the synced official lock/manifests, and executes per-suite alias smoke calls through the generated bridge. All seven suites are runner-ready; full official spec execution is still pending because the harness has not yet invoked the official Vitest/Jasmine specs through the alias package setup.
- Runner dependency provisioning is lock-aware: Vue 2.6 installs `@babel/register@7.0.0` and `jasmine@2.99.0` from `yarn.lock`; Vue 2.7 installs Vitest/esbuild/TypeScript from its `pnpm-lock.yaml`; Vue 3 installs Vitest/esbuild/TypeScript/source-map-js from its `pnpm-lock.yaml`.
- `vuec_vue2` has moved beyond the skeleton: it now has a recursive Vue 2 element AST, directive parsing for core Vue 2 directives, filter rewriting, static optimization, official-style render/staticRenderFns codegen, event handler generation, model helpers, diagnostics, and Vue 2 codeframe generation. It is still not official parity-complete.
- `vuec_html` now accepts Vue directive attribute names such as `@click.stop`, `#default`, `.prop`, and dynamic-argument syntax without tokenizer stalls.
- `xtask --version-line` now accepts the canonical plan values `vue2_6`, `vue2_7`, and `vue3` while retaining `vue26` / `vue27` aliases.
- `cargo xtask export-api --all` now installs exact official npm versions from `compat/official-revisions.lock`, generates Rust npm alias packages under `target/compat/rust-alias/<version-line>`, probes both sides with Node, and writes normalized API manifests for all seven compiler targets.
- `cargo xtask diff-api --all` now passes for all seven compiler targets at API export/arity/type-declaration shape level, using `compat/api/allowed-diff.json` for explicit approved differences.
- `cargo xtask verify-npm-alias --all` now builds the `vuec_node_bridge` JSON bridge, generates alias packages, requires each official package name, and smoke-calls a representative Rust-backed compiler entry for each target. This is a development bridge, not the final NAPI package.
- `cargo xtask run-option-matrix --all` now executes real official-vs-Rust npm alias probes and writes `target/conformance/<lock-hash>/option-matrix.json`; all seven current option-matrix targets pass with no pending or failing rows.
- `cargo xtask run-output-contract --all` now executes real official-vs-Rust npm alias probes and writes `target/conformance/<lock-hash>/output-contract.json`; schema, exact JS output, diagnostics, source-map, and runtime parity checks have no failing rows across all seven targets.
- `cargo xtask summarize-compat --locked` now reads real API, option, output, conformance, and lock artifacts instead of returning a scaffolded summary. Current summary is `pending` only because official conformance reports still have pending full-spec execution; API, option, output, lock, and conformance alias smoke statuses are `pass`.
- Vue 2 newline option handling now parses `shouldDecodeNewlines` / `shouldDecodeNewlinesForHref` through the Node bridge and emits href newline escapes compatible with the Vue 2.6 option probe.
