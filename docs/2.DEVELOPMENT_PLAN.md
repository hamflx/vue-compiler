# Rust Vue Compiler 开发计划

本文基于：

- `docs/0.RESEARCH.md`
- `docs/1.RUST_VUE_COMPILER_DESIGN.md`
- `docs/3.AST_HIR_MIR_DESIGN.md`（AST/HIR/MIR 的结构、投影和 lowering 规则以此为准）

目标是把“同时支持 Vue 2 / Vue 2.7 / Vue 3、使用 Oxc 处理 JS、完整通过官方 compiler 测试、具备完整编译器抽象”拆成可执行任务，并把 AST/HIR/MIR 结构、public projection、lowering 关系和验收产物全部固定为确定性清单。`docs/3.AST_HIR_MIR_DESIGN.md` 是 AST/HIR/MIR 的唯一结构约束，`Vue2Ast`、`Vue3Ast`、`HIR`、`Vue2Mir`、`Vue3DomMir`、`Vue3SsrMir`、`VaporMir` 都必须作为一等目标文档来验收。每个模块都必须能用简单信号判断是否完成。若与旧的泛化 IR 口径冲突，以 `docs/3.AST_HIR_MIR_DESIGN.md` 为准。

这里的 AST/HIR/MIR contract 不是抽象口号，而是直接验收 `NodeSpan`、`GeneratedReason`、`MissingSpanReason`、`LoweringMap`、`PublicProjection`、`TemplateSource.base_offset`、`JsAstStore`、`RuntimeHelper` 以及 target-split MIR 的可观察行为。

AST / HIR / MIR 的具体结构不再使用模糊的“泛化 IR”表述，而是直接采用 `docs/3.AST_HIR_MIR_DESIGN.md` 里的确定 schema：`Vue2Ast`、`Vue3Ast`、`HIR`、`Vue2Mir`、`Vue3DomMir`、`Vue3SsrMir`、`VaporMir` 都必须作为一等目标文档来验收。

## 完成判定总则

一个模块只有同时满足以下条件，才允许标记为完成：

- [x] 模块 API 已稳定到本阶段要求，公开类型有文档注释。
- [x] 模块单元测试通过。
- [x] 模块参与的官方兼容测试通过。
- [x] 模块参与的官方兼容测试必须标明覆盖来源：`rust-backed`、`shim-backed`、`mixed`。只有 `rust-backed` 或明确验证 Rust 输出的测试可以作为该模块完成证据。
- [ ] 诊断、source span、source map 行为有覆盖。
- [ ] 没有新增 `todo!()`、`unimplemented!()`、`panic!("TODO")` 进入主路径。
- [ ] CI 在 Windows、Linux、macOS 通过。

项目最终完成的判定：

- [x] `compat/official-revisions.lock` 固定 Vue 2.6.x、Vue 2.7.x、Vue 3.x 三条基线；每条基线都有官方 repo、40 位 commit SHA、npm 包版本，`cargo xtask verify-official-lock --require-vendor` 会禁止浮动 tag、branch、range version，并核对 synced vendor checkout / package manifest。lock 生成时间仍由 JSON 验收报告 `created_unix` 记录。
- [x] Vue 2.6 基线的 `test/unit/modules/compiler` 相关测试 100% 通过。
- [x] Vue 2.7 基线的 template compiler、`vue-template-compiler` 兼容入口、`vue/compiler-sfc` 兼容入口相关测试 100% 通过。
- [x] Vue 3 基线的 `compiler-core`、`compiler-dom`、`compiler-sfc`、`compiler-ssr` 测试 100% 通过。
- [x] Vue 2.6 / Vue 2.7 / Vue 3 的 compiler options 矩阵测试 100% 通过，且无未分类官方选项。
- [x] Vue 2.6 / Vue 2.7 / Vue 3 的输出契约测试 100% 通过，包括 result object shape、字段存在性、`undefined`/missing 区别、诊断顺序、source map JSON、render/code 字符串。
- [x] Vue 2 / Vue 3 的 AST / HIR / MIR 结构契约测试 100% 通过，包括 `Vue2Ast` / `Vue3Ast` public projection、`NodeSpan`、`LoweringMap`、`RuntimeHelper`、JS id store、MIR target-split。
- [x] 所有 skip 都来自官方原测试，项目本身不新增 skip。
- [x] NAPI 包能按版本 alias 替换官方 JS compiler 包跑官方测试和 options/output 差分测试。
- [x] 官方 conformance 报告不能只给总 pass/fail 数；必须同时给出 `rust-backed`、`shim-backed`、`mixed` 计数。`shim-backed` 通过项不能计入 Rust 编译器完成度。
- [x] Rust API、NAPI API、WASM API 都有 smoke test。
- [x] 文档记录官方测试 commit、官方包版本、已知兼容模式、发布流程：`docs/COMPATIBILITY_MATRIX.md`、`docs/RELEASE_CHECKLIST.md`。

## 确定性兼容验收标准

本节是兼容性验收的唯一口径。任何“兼容官方 Vue compiler”的完成声明，都必须同时通过本节定义的版本基线、API 导出、选项矩阵、输出契约和官方测试。

### 版本基线锁定

开发期可以临时跑 tag 或 branch，但合并到主分支的兼容报告只能读取 `compat/official-revisions.lock`。该 lock 文件必须是机器可读格式，并至少包含以下字段：

```text
vue2_6.repo
vue2_6.rev
vue2_6.npm.vue
vue2_6.npm.vue-template-compiler

vue2_7.repo
vue2_7.rev
vue2_7.npm.vue
vue2_7.npm.vue-template-compiler
vue2_7.exports["vue/compiler-sfc"]

vue3.repo
vue3.rev
vue3.npm.vue
vue3.npm.@vue/compiler-core
vue3.npm.@vue/compiler-dom
vue3.npm.@vue/compiler-ssr
vue3.npm.@vue/compiler-sfc
```

完成判定：

- [x] `cargo xtask verify-official-lock` 校验所有 `rev` 都是 40 位 commit SHA。
- [x] `cargo xtask verify-official-lock --require-vendor` 校验 npm 包版本与官方仓库 package manifest 一致。
- [x] `cargo xtask verify-official-lock` / `--require-vendor` 禁止 `latest`、`next`、`v2-latest`、`main`、`master`、semver range 作为验收输入，并要求 synced vendor checkout 的 `rev` 是 commit object 而不是 tag object。
- [x] conformance 报告头部记录 lock 文件 hash，报告无法脱离 lock 文件复现时判定失败。

### 官方 API 导出验收

每个官方包都要先生成 export manifest，再对 Rust NAPI 包做 alias 替换后重新生成 manifest。两个 manifest 必须逐字段一致，除非差异列入 `compat/api/allowed-diff.json` 并附带理由。

验收对象：

| 基线 | 官方包/入口 | 必须验收的内容 |
| --- | --- | --- |
| Vue 2.6 | `vue-template-compiler` | `Object.keys(require(pkg)).sort()`、函数 arity、同步/异步行为、异常类型、TypeScript 声明若存在 |
| Vue 2.7 | `vue-template-compiler` | 同 Vue 2.6，并验证 2.7 行为不能回退成 2.6 |
| Vue 2.7 | `vue`（`vue/compiler-sfc`） | export manifest、SFC descriptor/result shape、`compileTemplate` / `compileScript` / `compileStyle` 入口行为 |
| Vue 3 | `@vue/compiler-core` | export manifest、helper enum、AST node type、error code 导出 |
| Vue 3 | `@vue/compiler-dom` | export manifest、DOM transform 导出、compile result shape |
| Vue 3 | `@vue/compiler-ssr` | export manifest、SSR compile result shape |
| Vue 3 | `@vue/compiler-sfc` | export manifest、SFC parse/template/script/style result shape |

完成判定：

- [x] `cargo xtask export-api --official --all` 生成官方 manifest。
- [x] `cargo xtask export-api --rust --all` 生成 Rust alias manifest。
- [x] `cargo xtask diff-api --all` 无 diff，或 diff 全部位于 `compat/api/allowed-diff.json`。
- [x] 每个 allowed diff 都有 issue id、影响范围、计划移除条件；没有理由的 allowed diff 判定失败。

### Compiler Options 矩阵验收

选项兼容不能只靠官方测试间接覆盖。必须为每个版本线、每个入口生成机器可读矩阵，文件名固定为 `compat/options/<version-line>/<package>/<entry>.json`。

每个 option 条目至少包含：

```text
option_name
option_path
entry
version_line
accepted_types
default_when_missing
behavior_when_undefined
behavior_when_null
side_effects
diagnostics
output_fields_affected
official_fixture_ids
```

最低覆盖要求：

| 基线 | 入口 | 至少覆盖的选项类别 |
| --- | --- | --- |
| Vue 2.6 | template compiler | modules、directives、warn、outputSourceRange、comments、delimiters、whitespace/preserveWhitespace、decode newlines、platform predicates、codeframe |
| Vue 2.7 | template compiler | Vue 2.6 全量类别，加 2.7 差异项；任何 2.7 行为不得用 2.6 结果代替 |
| Vue 2.7 | SFC compiler | parse、compileTemplate、compileScript、compileStyle、source map、id/scope、TS/script setup、CSS modules/preprocess |
| Vue 3 | compiler-core | parse、transform、codegen、prefixIdentifiers、mode、hoistStatic、cacheHandlers、nodeTransforms、directiveTransforms、bindingMetadata、inline、isTS、expressionPlugins、error/warn callbacks |
| Vue 3 | compiler-dom | DOM parser/platform predicates、custom element、entity decode、v-model variants、event/key modifiers、asset URL 相关选项 |
| Vue 3 | compiler-ssr | SSR mode、scope/slotted、SSR helpers、teleport/suspense/component/slot 输出相关选项 |
| Vue 3 | compiler-sfc | parse、compileTemplate、compileScript、compileStyle、transformAssetUrls、ssrCssVars、HMR、source map、preprocess、macro/type resolve 相关选项 |

完成判定：

- [x] `cargo xtask generate-option-matrix --all` 可从官方包和人工补充清单生成矩阵。
- [x] `cargo xtask run-option-matrix --all` 对每个 option 的 missing、`undefined`、`null`、典型有效值、典型非法值都执行官方/Rust 差分。
- [x] `cargo xtask audit-option-matrix --all` 证明官方 manifest 中没有未分类 option。
- [x] option 差分报告中 `missing_rows == 0`、`unexpected_pass == 0`、`unexpected_fail == 0`。

### 输出契约验收

输出兼容分为五种模式，全部通过才算兼容：

| 模式 | 判定内容 |
| --- | --- |
| `schema-parity` | result object 的 `Object.keys`、字段存在性、`undefined` 与 missing、数组顺序、嵌套对象 shape 与官方一致 |
| `exact-js-output` | `render`、`staticRenderFns`、`code`、helper import、preamble、whitespace 与官方 exact compare |
| `diagnostic-parity` | errors、warnings、tips 的数量、顺序、message/code、location/codeframe 与官方一致 |
| `source-map-parity` | source map JSON 的 `version`、`file`、`sources`、`sourcesContent`、`names`、`mappings` 与官方一致或存在明确 allowed diff |
| `runtime-parity` | 生成的 render/SSR code 在对应 Vue runtime 下执行结果与官方一致 |

完成判定：

- [x] `cargo xtask run-output-contract --version-line vue2_6 --all` 通过。
- [x] `cargo xtask run-output-contract --version-line vue2_7 --all` 通过。
- [x] `cargo xtask run-output-contract --version-line vue3 --all` 通过。
- [x] `target/conformance/<lock-hash>/output-contract.json` 中所有模式 `failed == 0`。
- [x] 任何 schema allowed diff 都必须记录在 `compat/output/allowed-diff.json`，且不能掩盖字段缺失、字段类型错误、诊断顺序错误。

### AST / HIR / MIR 结构验收

本节是 AST/HIR/MIR 的唯一结构口径。只有满足本节，才允许认为内部编译模型已经对齐新设计。
本节也是 `vuec_ast`、`vuec_pass`、`vuec_codegen`、`vuec_vue2`、`vuec_vue3_core`、`vuec_vue3_dom`、`vuec_vue3_ssr` 的共同验收标准。

必须满足：

- `AstDocument<K>` 节点必须包含 `NodeId`、`NodeSpan`、`parent`、`children`、`index_in_parent`。
- `NodeSpan` 必须显式区分 `Source`、`Generated`、`Missing`，并保留 `GeneratedReason` / `MissingSpanReason`；生成节点不能再用空值冒充正常源码节点。
- `LoweringMap` 必须显式记录 `ast_to_hir` 与 `hir_to_mir`，跨层关系不能藏进树边。
- `PublicProjection` 必须把 arena 结构还原成官方可比较的 public shape，不能补造内部已经丢失的信息；所有官方可观察字段都必须保留，尤其是 `loc`、`innerLoc`、`codegen_node`、`ssr_codegen_node` 这类 projection 字段。对于 Vue 3 directive，`arg` / `exp` / `modifiers` 的 `content`、`loc`、`isStatic`、`constType` 必须保持和官方 AST 一致，不能用统一 trim 或 synthetic fallback 掩盖差异。
- `Vue2Ast` 与 `Vue3Ast` 必须作为一等方言 AST 存在，它们的 public projection 必须能还原官方测试可比较的结构。
- `Vue2Ast` 的 projection 必须保留 `Vue2Root`、`Vue2Element`、`Vue2Text`、`Vue2ExpressionText`、`Vue2Comment` 的字段语义；`Vue3Ast` 的 projection 必须保留 `Vue3Root`、`Vue3Element`、`Vue3Prop`、`Vue3Directive`、`Vue3Text`、`Vue3Comment`、`Vue3Interpolation`、`Vue3CompoundExpression`、`Vue3If`、`Vue3IfBranch`、`Vue3For`、`Vue3TextCall`、`Vue3CodegenRef`、`Vue3SsrCodegenRef` 的字段语义，且 `Vue3Directive` 必须保留 `raw_name`、`arg`、`exp`、`modifiers`、`is_dynamic_arg` 的可观察差异。
- `Vue3Ast` 的 directive 语义必须保持在 `Element.props` 中，不能拆成独立 child node；`codegen_node` / `ssr_codegen_node` 也是 public projection 的可观察字段，directive arg 的原文内容不能被折叠或重写，且 dynamic arg 的 raw bracket span、`.prop` synthetic modifier、directive exp whitespace 都属于可观察差异。
- `HIR` 必须保留 `HirRoot`、`HirElement`、`HirComponent`、`HirText`、`HirInterpolation`、`HirIf`、`HirFor`、`HirSlotOutlet`、`HirSlotDecl`、`HirFragment` 的共享语义，不允许出现 `Call`、`Helper`、patch flag、hoist/cache、SSR push 或 codegen-only 节点。
- `HIR` 只能表达跨方言语义，不允许出现 runtime helper、patch flag、hoist/cache、SSR push 或 codegen-only 调用结构；所有方言 lowering 必须先落到 HIR，再进入对应 MIR。
- `MIR` 必须按目标拆分为 `Vue2Mir`、`Vue3DomMir`、`Vue3SsrMir`、`VaporMir`，codegen 只能消费对应目标的 MIR，不允许把多个目标收敛成单一通用枚举。
- `JsAstStore` 必须按 id 记录 JS 源、span、parse mode、source type，并提供 `JsExprId` / `JsStmtId` / `JsPatternId` / `JsProgramId` 的解析入口；JS program 不能退化成字符串拼接的旁路数据。
- `TemplateSource.base_offset` 必须把 SFC block 局部 offset 还原为原始 `.vue` 文件绝对 span。
- `RuntimeHelper` 必须是枚举，不允许 helper 名称退化回自由字符串。
- `DocumentPass` 遍历顺序必须稳定为 depth-first pre-order enter、post-order exit。
- `vuec_ast`、`vuec_pass`、`vuec_js` 的 snapshot / serializer 测试必须稳定，不允许依赖 `HashMap` 顺序。

完成判定：

- [x] `cargo test -p vuec_ast -p vuec_pass -p vuec_js` 通过。
- [x] `Vue2Ast` / `Vue3Ast` public projection 快照稳定且可重复比较，且 directive arg / exp / modifier 的 `content`、`loc`、`isStatic` 与官方 parse AST 一致。
- [x] `HIR` / `Vue2Mir` / `Vue3DomMir` / `Vue3SsrMir` / `VaporMir` 的 snapshot 和 target split 可重复比较。
- [x] `LoweringMap` 可追踪 AST -> HIR -> MIR 的映射。
- [x] `NodeSpan` 生成/缺失语义可在 snapshot 中区分，且 `GeneratedReason` / `MissingSpanReason` 可被快照比较。
- [x] `DocumentPass` 的 enter / exit 顺序和节点遍历顺序稳定可测。

### 总体验收命令

最终验收只接受以下命令组的结果，不接受人工挑选子集：

```powershell
cargo xtask verify-official-lock
cargo xtask sync-official-tests --locked
cargo xtask verify-official-lock --require-vendor
cargo xtask export-api --all
cargo xtask diff-api --all
cargo xtask generate-option-matrix --all
cargo xtask audit-option-matrix --all
cargo xtask run-option-matrix --all
cargo xtask run-conformance --all
cargo xtask run-output-contract --all
cargo xtask verify-npm-alias --all
```

完成判定：

- [ ] 所有命令在 Windows、Linux、macOS CI 通过。
- [x] 每个官方验收命令都输出 JSON 报告，报告写入 lock hash、操作系统、Node/Rust 版本、官方 commit、Rust compiler commit；当前证据：`JsonReport.metadata` 与 `target/conformance/<lock>/option-matrix.json`、`output-contract.json`、per-suite conformance JSON 均包含 `official_commits` 和 `rust_compiler_commit`，已用 `cargo xtask run-option-matrix --all`、`cargo xtask run-output-contract --all`、`cargo xtask run-conformance --suite vue3-dom` 验证。
- [x] conformance JSON 报告对每个 suite、test file 或测试分组标记覆盖来源：`rust-backed`、`shim-backed`、`mixed`。没有来源分类的 conformance 报告不能作为完成证据。
- [x] Vue 3 compiler-core 内部 transform/codegen 相关官方测试只有在对应语义由 Rust AST/transform/codegen 管线执行，或由明确 Rust-backed bridge 执行时，才允许标记为 Rust parity。
- [x] `cargo xtask summarize-compat --locked` 输出的总表中 Vue 2.6、Vue 2.7、Vue 3 所有行均为 `pass`。

## 完成状态总览

| 编号 | 模块 | 完成判定 |
| --- | --- | --- |
| M00 | 仓库与工程骨架 | `cargo test --workspace` 和 `pnpm test` 可执行，即使早期测试失败也能稳定收集报告 |
| M01 | 官方测试同步与 conformance harness | 一条命令可同步 Vue 2.6 / Vue 2.7 / Vue 3 官方测试并生成 pass/fail 报告 |
| M02 | `vuec_source` | 任意 `.vue` block 内 span 能映射回原文件行列 |
| M03 | `vuec_diagnostics` | Vue 2 / Vue 3 parser/codegen 错误码和位置可快照对比 |
| M04 | `vuec_html` | Vue 2 / Vue 3 parser 基础 HTML/tokenizer 测试通过 |
| M05 | `vuec_js` / Oxc 集成 | template 表达式、事件语句、`v-for`、script、TS 都登记到 `JsAstStore` 并可按 id 解析 |
| M06 | `vuec_ast` | CST、Vue2Ast、Vue3Ast、HIR、Vue2Mir/Vue3DomMir/Vue3SsrMir/VaporMir 类型完整，NodeSpan/LoweringMap/public projection/lowering/span 可序列化快照 |
| M07 | `vuec_pass` | Vue 2 module hooks 和 Vue 3 node/directive transforms 都可表达 |
| M08 | `vuec_codegen` | 支持 exact emitter、MIR emit、source map merge |
| M09 | `vuec_vue2` | Vue 2.6 compiler 官方测试 100% 通过 |
| M09b | `vuec_vue27` | Vue 2.7 template compiler 与 SFC compiler 官方测试 100% 通过 |
| M10 | `vuec_vue3_core` | Vue 3 `compiler-core` 官方测试 100% 通过 |
| M11 | `vuec_vue3_dom` | Vue 3 `compiler-dom` 官方测试 100% 通过 |
| M12 | `vuec_vue3_ssr` | Vue 3 `compiler-ssr` 官方测试 100% 通过 |
| M13 | `vuec_sfc` | Vue 3 `compiler-sfc` 官方测试 100% 通过 |
| M14 | `vuec_style` | scoped CSS、CSS vars、modules、preprocessor 测试通过 |
| M15 | `vuec_runtime_tests` | Vue 2 / Vue 3 render 和 SSR runtime smoke 通过 |
| M16 | `vuec_napi` | npm alias 官方 compiler 包跑 conformance 通过 |
| M17 | `vuec_wasm` | 浏览器/WASI smoke 和错误传播测试通过 |
| M18 | `vuec_cli` | CLI 可编译单文件、SFC、项目 fixture 并输出诊断 |
| M19 | 性能与增量编译 | benchmark 报告可复现，不影响兼容测试 |
| M20 | 发布与文档 | 版本、CHANGELOG、兼容矩阵、发布包验证完成 |

## 里程碑

### P0: Harness First

目标：先让官方测试能跑起来，哪怕全部失败。

- [x] 创建 Rust workspace、npm workspace、`xtask`。
- [x] 创建空 NAPI 包，API 名称对齐官方 compiler 包。
- [x] 实现 `xtask sync-official-tests`。
- [x] 实现 `xtask run-conformance`。
- [x] 输出统一 JSON 报告：测试总数、通过数、失败数、skip 数、失败原因。
- [x] CI 能运行 harness：`.github/workflows/ci.yml` 在 Windows、Linux、macOS 矩阵中执行官方 lock、generated alias API/option/output/conformance、NAPI alias/API/option/output/conformance gate；远端平台实际通过仍以 GitHub Actions 运行结果为准。

完成判定：

- [ ] `cargo xtask sync-official-tests --vue2-6 <rev> --vue2-7 <rev> --vue3 <rev>` 成功。
- [x] `cargo xtask run-conformance --suite vue2-compiler` 有稳定报告。
- [x] `cargo xtask run-conformance --suite vue27-compiler` 有稳定报告。
- [x] `cargo xtask run-conformance --suite vue27-sfc` 有稳定报告。
- [x] `cargo xtask run-conformance --suite vue3-core` 有稳定报告。

### P1: 编译器基础设施

目标：完成所有后续模块共享的 source、diagnostic、HTML、Oxc、AST、pass、codegen 基础。

- [x] `vuec_source` 完成。
- [x] `vuec_diagnostics` 完成。
- [x] `vuec_html` 完成基础 tokenization。
- [x] `vuec_js` 完成 Oxc-backed `JsAstStore` 注册表。
- [x] `vuec_ast` 完成 arena、projection、HIR、目标 MIR 基础类型。
- [x] `vuec_pass` 完成 transform scheduler 和 `DocumentPass` walker。
- [x] `vuec_codegen` 完成 code writer 和 source map 合并。

完成判定：

- [x] Vue 2 parser 基础测试子集通过。
- [x] Vue 3 parser 基础测试子集通过。
- [x] 所有 AST/HIR/MIR 节点有 `NodeSpan`。
- [x] 所有错误有 stable code 和 location。

### P2: Vue 2 Compiler

目标：完整复刻 Vue 2 compiler 行为。

- [x] Vue 2 parser 完成。
- [x] Vue 2 optimizer 完成。
- [x] Vue 2 codegen 完成。
- [x] Vue 2 platform modules 完成。
- [x] Vue 2 `compile` / `compileToFunctions` 兼容层完成。

完成判定：

- [x] Vue 2 parser 官方测试通过。
- [x] Vue 2 optimizer 官方测试通过。
- [x] Vue 2 codegen 官方测试通过。
- [x] Vue 2 compiler options / warning / codeframe 测试通过。
- [x] Vue 2 compiler 官方测试总套件 100% 通过。

### P3: Vue 3 Compiler Core / DOM

目标：完整复刻 Vue 3 `compiler-core` 和 `compiler-dom`。

当前官方 generated-alias conformance 已通过，但 coverage 仍必须按报告口径读取：`vue3-core` 为 `rust-backed 199/199`、`mixed 453/453`，`vue3-dom` 为 `rust-backed 34/34`、`mixed 99/99`。以下官方验收项的勾选表示当前官方套件、option matrix 和 output contract 通过；未标记为纯 Rust-backed 的 mixed 项仍不能作为 Rust 编译器完成度证据。

- [ ] Vue 3 base parser 完成。
- [ ] Vue 3 core transforms 完成。
- [ ] Vue 3 core codegen 完成。
- [ ] DOM parser options 完成。
- [ ] DOM directive transforms 完成。
- [ ] DOM asset / model / event modifier 相关行为完成。
- [x] Vue 3 DOM custom-element `v-model` 输出切片完成：Rust DOM compile 尊重 custom-element 配置，跳过非法 native `v-model` 诊断，并让 native/custom DOM model 输出 `vModelText` runtime directive 与 `8 /* PROPS */, ["onUpdate:modelValue"]` dynamic props 合同对齐。该项只关闭 DOM model/custom-element 子行为，不代表 DOM directive transforms 全量完成；`vue3-dom` generated-alias conformance 当前为 `133/133`。

完成判定：

- [x] Vue 3 `compiler-core/__tests__` 100% 通过。
- [x] Vue 3 `compiler-dom/__tests__` 100% 通过。
- [x] DOM / core error location 与官方一致。

### P4: SFC + Oxc Script

目标：完整实现 Vue 3 SFC parser、`compileScript`、`compileTemplate`、JS/TS/Oxc 分析。

当前 Vue 3 SFC 官方 generated-alias conformance 为 `461/461`，coverage 为 `rust-backed 87/87`、`mixed 374/374`、`shim-backed 0/0`。其中 `packages/compiler-sfc/__tests__/parse.spec.ts`、`rewriteDefault.spec.ts`、`compileStyle.spec.ts` 和 `cssVars.spec.ts` 已作为 public `@vue/compiler-sfc` 路径迁移为 Rust-backed；SFC 内部 TypeScript source、alias adapter 与 Rust bridge 的其余 mixed 路径仍需后续迁移为 Rust-backed 覆盖，不能当作纯 Rust SFC 完成度。

- [ ] SFC descriptor parser 完成。
- [ ] `<script>` parse 与 default export rewrite 完成。
- [ ] `<script setup>` merge 完成。
- [ ] macro analysis 完成。
- [ ] TypeScript props / emits / model type resolve 完成。
- [ ] binding metadata 完成。
- [ ] `compileTemplate` 接入 DOM / SSR compiler。
- [ ] asset URL / srcset / scopeId / slotted / HMR 完成。
- [x] Vue 3 SFC public `parse()` official `parse.spec.ts` Rust-backed migration slice：prepared official SFC parse spec 现在从 public `@vue/compiler-sfc` alias 导入并通过 Rust SFC parser / bridge / NAPI projection 验收；Rust 侧覆盖 DOM void tag template scan、非 HTML template lang 的 raw text AST、template parse options custom elements 和 public descriptor/template AST 投影。`xtask/src/compat.rs` 只承担 import/API/test-runner materialization、非序列化 option 归一化和 caller `compiler.parse` callback 应用，不计作 Rust 编译器语义。该切片当时 Vue 3 SFC 总套件为 `461/461`，coverage 是 `rust-backed 44/44`、`mixed 417/417`。
- [x] Vue 3 public `parse()` descriptor projection slice：Rust SFC descriptor 经 `vuec_sfc` / bridge / NAPI 投影为官方 public `scriptSetup`、`customBlocks`、attrs、block loc、source map、`cssVars`、`slotted` 和 Rust DOM template AST 形状；SFC conformance 暴露的多行 SSR CSS vars 格式边界已在 Vue 3 core 修复；非 JSON `shouldForceReload` HMR 函数已由单独的 package API adapter 覆盖，不计作 Rust 编译器语义完成。
- [x] Vue 3 SFC public `parse()` `descriptor.shouldForceReload` API/HMR adapter slice：generated alias 与 NAPI alias 在 JavaScript package 边界把 Rust JSON descriptor 的 `shouldForceReload: null` hydrate 为官方可调用函数，基于 Rust-projected template AST 对齐 TS/TSX `<script setup>` import usage reload 判定；这是 package API/HMR materialization，不计作 Rust-backed SFC parser 完成。
- [x] Vue 3 SFC parse descriptor validation slice：Rust SFC parser 负责 duplicate top-level `template` / `script` / `script setup`、空非 src script 忽略、`script setup src`、`script src` + `script setup` 和缺失 template/script 的 public parse diagnostics；bridge 与 NAPI `parseSfcResult()` 返回官方 parse-result error shape。Vue 3 SFC 官方 conformance 仍是 mixed runner 覆盖，不能据此声明完整 SFC Rust-backed。
- [x] Vue 3 SFC parse options / padding / dedent slice：Rust SFC parser 支持 `pad: true | "line" | "space"`、`ignoreEmpty`、pug/jade template descriptor dedent，并按官方 padded line offset / dedent column offset 生成 descriptor block source map；bridge 与 NAPI 在 public parse 投影前传入这些 Rust parse options。Vue 3 SFC 官方 conformance 仍是 mixed runner 覆盖，不能据此声明完整 SFC Rust-backed。
- [x] Vue 3 SFC descriptor `src` presence / functional template validation slice：Rust SFC parser 区分 `src` 属性存在与非空字符串值，保留 boolean / empty-string `src` blocks，按官方跳过对应 descriptor map / template AST、只对非空 script `src` 触发 setup 冲突，并对 accepted first `<template functional>` 输出属性级 loc 诊断；NAPI `parseSfcResult()` 现在将 template AST 挂到 `descriptor.template`。Vue 3 SFC 官方 conformance 仍是 mixed runner 覆盖，不能据此声明完整 SFC Rust-backed。
- [x] Vue 3 SFC descriptor block attribute parity slice：Rust SFC parser 对 Vue 3 public descriptor attrs 执行 HTML attribute entity decode，保留官方 `lang` / `src` / `module` / `generic` 解码值，输出 duplicate attribute parse diagnostics 的官方 zero-length loc，并限制非 script block 不投影顶层 `setup`；Vue 2.7 parseComponent 保持独立原始 attr 路径。Vue 3 SFC 官方 conformance 仍是 mixed runner 覆盖，不能据此声明完整 SFC Rust-backed。
- [x] Vue 3 SFC descriptor scan syntax diagnostics slice：Rust SFC parser 输出官方 public parse diagnostics，覆盖 bogus `<?...?>`、未闭合 script/style/custom raw blocks、template 嵌套缺失结束标签和 EOF stack recovery，同时保留官方恢复后的 descriptor 内容。Vue 3 SFC 官方 conformance 仍是 mixed runner 覆盖，不能据此声明完整 SFC Rust-backed。
- [x] Vue 3 SFC malformed descriptor syntax diagnostics slice：Rust SFC parser 对齐官方 malformed descriptor scan recovery，覆盖 uppercase top-level tags、raw/template invalid end tags、template CDATA、malformed attrs 和 template child duplicate attrs；Vue 3 SFC 官方 conformance 仍是 mixed runner 覆盖，不能据此声明完整 SFC Rust-backed。
- [x] Vue 3 SFC public `rewriteDefault` API slice：Rust SFC 层实现 Vue 3 默认导出重写，覆盖 default declarations、named default exports、re-exported defaults、decorated class defaults、function defaults 和 TypeScript parser-plugin 边界；generated alias / NAPI alias 均通过 Vue 3 Rust bridge/API 路由 public `rewriteDefault`。`rewriteDefaultAST` 仍是 JavaScript/static AST helper，不计作 Rust-backed。
- [x] Vue 3 SFC public `rewriteDefault()` official `rewriteDefault.spec.ts` Rust-backed migration slice：prepared official SFC rewriteDefault spec 现在从 public `@vue/compiler-sfc` alias 导入并通过 Rust SFC `rewrite_vue3_default` / bridge 路径验收，官方文件计为 `rust-backed 16/16`；`xtask/src/compat.rs` 只承担 import/API 与 coverage materialization，不添加 JavaScript rewriteDefault 语义。该切片当时 Vue 3 SFC 总套件为 `461/461`，coverage 是 `rust-backed 44/44`、`mixed 417/417`、`shim-backed 0/0`。
- [x] Vue 3 SFC public `cssVars.spec.ts` Rust-backed migration slice：prepared official SFC cssVars spec 现在从 public `@vue/compiler-sfc` alias 导入，`parse`、`compileStyle` 和 `compileScript` 均通过 `vuec_node_bridge` 路由到 Rust；Rust `compileScript` 覆盖 Vue 3 normal script / script setup CSS vars 注入、setup refs / maybe refs / props / destructured props aliases、line-comment skip、复杂表达式 projection、test-mode script setup return shape 和 default filename `__name` 边界。`xtask/src/compat.rs` 只生成 per-file `utils.public-api.ts` 以保持 public API route、官方测试工具形状和 Babel `assertCode` 解析，不添加 CSS vars 编译语义。Vue 3 SFC 总套件为 `461/461`，当前 coverage 是 `rust-backed 87/87`、`mixed 374/374`、`shim-backed 0/0`。
- [x] Vue 3 SFC `compileScript` normal-script default merge slice：Rust SFC 在 `<script>` + `<script setup>` 路径分析 normal script default export，重写为 `__default__`，移除 compileScript default-only named export shell，保留显式 `name`，并按 JS `Object.assign(__default__, ...)` / TS `_defineComponent({ ...__default__, ... })` 生成 setup wrapper；返回对象使用 normal script -> setup -> eligible imports 的 AST 顺序。该项不代表完整 macro analysis、TS type resolve、binding metadata、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` runtime macro base slice：Rust SFC 在 `<script setup>` 中分析基础运行时 `defineProps(...)`、`defineEmits(...)`、`defineExpose(...)`，生成 `props` / `emits` runtime options、`__props` / `__emit` / `__expose` setup rewrite、基础 props/setup binding metadata，并恢复 setup-only 静态 literal `const` 提升。该项不代表完整 macro analysis、TS type resolve、withDefaults、defineModel/defineOptions、diagnostics、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` TypeScript macro inference subset：Rust SFC 支持基础 `defineProps<T>()`、`withDefaults(defineProps<T>(), defaults)`、`defineEmits<T>()` 推断，覆盖 inline type literal、setup/local normal-script interface/type references、静态/动态 defaults、Vue 3 prod prop erasure、`_mergeDefaults` import 和 type-based `__props: any`。该项不代表完整 macro analysis、完整 TS type resolve、defineModel/defineOptions、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `withDefaults()` macro diagnostics slice：Rust SFC 将 `withDefaults` 作为 Vue compiler macro 清理 import / public binding，保留已有 static/dynamic defaults runtime 推断，并按官方报告 first-argument、runtime `defineProps` 和 missing second-argument diagnostics。该项不代表完整 `withDefaults` reactive destructure warning、完整 TS type resolve、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineProps()` / `defineEmits()` duplicate diagnostics slice：Rust SFC 在 `<script setup>` macro 收集阶段追踪 `defineProps` / `defineEmits` 调用，包含 `withDefaults(defineProps<T>(), ...)` 嵌套路径，并按官方报告 duplicate-call diagnostics。该项不代表完整 `defineProps` / `defineEmits` macro analysis、reactive destructure、完整 TS type resolve、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineProps()` destructure diagnostics slice：Rust SFC 对 `defineProps()` 解构声明补齐官方诊断边界，拒绝动态 computed key、nested patterns 和默认值引用非 literal setup locals；静态 computed literal key 与静态 setup const 引用按官方 `resolveObjectKey` / `isStaticNode` 边界允许。该项不代表 reactive destructure transform、watch/toRef misuse diagnostics、default value type mismatch、完整 TS type resolve、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineProps()` destructure usage diagnostics slice：Rust SFC 以 OXC AST 作用域扫描补齐解构 prop 只读写入诊断，以及解构 prop 直接传给 `watch()` / `toRef()` 的官方误用诊断；`watch` / `toRef` 支持 normal `<script>` 与 `<script setup>` 中从 `vue` 导入的别名，并尊重函数参数、局部变量和循环变量遮蔽。该项不代表 reactive destructure transform、default value type mismatch、完整 TS type resolve、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineProps()` destructure default type diagnostics slice：Rust SFC 对 type-based `defineProps<T>()` 的解构默认值执行官方 best-effort 类型不匹配诊断，覆盖 String / Number / Boolean / Object / Array / Function 默认值推断，并保留 `null` / unknown runtime type 的官方跳过边界。该项不代表 reactive destructure transform、完整 TS type resolve、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineProps()` destructure setup-body rewrite slice：Rust SFC 将 setup body 中未被局部作用域遮蔽的解构 prop 引用重写为 `__props.foo` / `__props["foo.bar"]`，覆盖别名、非标识符 prop key、对象 shorthand、函数参数遮蔽和循环变量遮蔽，并复用 destructure usage diagnostics 的作用域规则。该项不代表完整 reactive destructure transform、rest proxy、inline template rewrite、默认值 runtime 合并、完整 TS type resolve、top-level await、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineProps()` destructure defaults runtime merge slice：Rust SFC 将解构默认值合并进 runtime props，runtime declaration 路径生成 `/*@__PURE__*/_mergeDefaults(...)`，type-based declaration 路径把 `default:` 写入生成的 prop option；非 literal object/array 默认值按官方 factory wrap，runtime declaration 的函数/identifier 默认值生成 `__skip_*` 标记，Function prop type 默认值保持非 wrapped。该项不代表 rest proxy、inline template rewrite、完整 TS type resolve、top-level await、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineProps()` destructure rest proxy slice：Rust SFC 在对象解构 rest 路径保留声明并按官方生成 `const rest = _createPropsRestProxy(__props, ["foo","bar"])`，左侧 pattern 改写为 rest 本地变量，已解构 public prop key 按源码顺序进入排除列表，rest binding 作为 `setup-reactive-const` 暴露，runtime 与 type-based `defineProps` 路径均覆盖。该项不代表 inline template rewrite、完整 TS type resolve、top-level await、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `propsDestructure` option-boundary slice：Rust SFC 支持 `propsDestructure: false | true | "error"`，默认保持 enabled；disabled 模式只把 `defineProps(...)` 调用改写为 `__props`，保留解构 local 引用和官方 binding metadata 边界，error 模式输出官方禁止解构诊断。bridge、NAPI、WASM 与 CLI `compile-sfc --props-destructure enabled|disabled|error` 均透传该选项。该项不代表完整 reactive destructure parity、inline template destructure rewrite、完整 TS type resolve 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `inlineTemplate` DOM render injection slice：Rust SFC 消费 `inlineTemplate: true`，复用 Rust DOM compiler 以 inline/module 模式编译同文件非 `src` template，并把 DOM helper preamble 与 `return (_ctx, _cache) => { ... }` render 函数注入到 setup 返回；binding metadata 合并 normal script、setup bindings、props 和 destructured prop alias，缺失 template 或 `template src` 时返回 `() => {}`。该项不代表 SSR inline render、script source-map 合并、top-level await、完整 binding metadata/result shape 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `inlineTemplate` SSR render injection slice：Rust SFC 消费 `inlineTemplate: true` 与 `templateOptions.ssr: true` / `inlineTemplateSsr`，复用 Rust SSR compiler 的 inline/module 路径生成 `return (_ctx, _push, _parent, _attrs) => { ... }` SSR render，注入 SSR helper preamble、`__ssrInlineRender: true` runtime option，并为同文件 style `v-bind(...)` 生成 SSR `_cssVars` / `_ssrRenderAttrs(_cssVars)`；bridge、NAPI、WASM 与 CLI 均透传该选项，setup binding metadata 对 Vue `ref` / `reactive` / 静态 const 等 inline rewrite 边界做了更接近官方的分类。该项不代表 script source-map 合并、完整 result shape、完整 binding metadata、完整 TS type resolve 或 full `compileScript` parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` source-map foundation slice：Rust SFC 现在支持 `sourceMap` / `source_map` 选项并默认生成 `compileScript().map`，`sourceMap: false` 可关闭；normal `<script>` 与 `<script setup>` 中能在生成脚本中定位的源码行会映射回原始 `.vue`，inline template DOM/SSR compiler map 会合并进 script result map，bridge、NAPI、WASM 和 CLI 均透传该选项。该项不代表完整官方 source-map parity、完整 result shape、template 表达式 token 完整覆盖或 full `compileScript` parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` inline-template expression token source-map slice：Rust core source-map 生成现在用原始模板表达式 span 建 token 映射，同时用 transform 后表达式定位生成代码，因此 inline setup `count` -> `count.value` 等改写能把 generated token 映射回 `.vue` 模板中的 `count`；SFC inline render map 合并保留 token names，并使用 template content 起点作为 Vue 3 template base offset。该项不代表完整官方 source-map parity、previous map 合并、所有 template expression token 边界或 full `compileScript` parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` inline-template `v-bind` token source-map slice：Rust core source-map 现在对普通 `v-bind` 表达式也建立 token 映射，例如 `:id="count"` 生成的 inline setup `count.value` 可回查到模板原始 `count`，并保留 `:class` 的静态 key 映射；SFC inline render map 合并保留该 token name。该项不代表 dynamic argument token 映射、previous map 合并、所有 template token 边界或 full source-map parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` top-level await slice：Rust SFC 以 OXC AST 扫描 `<script setup>` 顶层 statement / variable declaration，跳过 function、arrow function、object method 和 class method 作用域，将 top-level `await` 重写为 `_withAsyncContext` 包裹，生成 `async setup(...)`、`let __temp, __restore` / TS `any` 临时变量、表达式/语句两种返回形态，并保留官方前导分号规则；bridge 仅透传 `templateOptions.compilerOptions.runtimeModuleName` 到 Rust helper import 来源。该项不代表 script source-map 合并、完整 binding metadata/result shape、完整 TS type resolve 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` import usage / binding metadata slice：Rust SFC 按官方 non-inline template import usage 规则识别组件、custom directive、dynamic directive argument、same-name `v-bind`、template ref、interpolation / attribute expression / template literal / member access 和 TS annotation skip 边界，只把模板实际使用的非 type import 加入 setup return，并按官方为外部 import 生成 getter、为 setup `let` 生成 getter/setter；public binding metadata 现在按 import source / imported kind 输出 `setup-const` 或 `setup-maybe-ref` 并供 inline template 使用。该项不代表完整 normal script analysis、完整 result shape、source map、完整 TS type resolve 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript().imports` public result-shape slice：Rust SFC 现在把 `compileScript().imports` 投影为按 local binding name 索引的官方 `ImportBinding` 对象，字段包含 `isType`、`imported`、`local`、`source`、`isFromSetup`、`isUsedInTemplate`；混合 `<script>` + `<script setup>` 路径会记录 normal 与 setup imports，Vue compiler macro imports 不进入 public imports，TS 同文件普通 template 使用 exact identifier scan，inline template 下保持 unused。该项只关闭 `imports` metadata 子形状，不代表完整 `compileScript` result-shape parity、`scriptAst` / `scriptSetupAst` parity、`deps`、完整 TS type resolve 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `scriptAst` / `scriptSetupAst` public result-shape slice：Rust SFC 现在把 normal `<script>` 和 `<script setup>` 的 OXC Program body 投影为 bounded statement-array JSON，而不是泄漏内部 `JsProgramId(...)`。投影保留 block-local `type`、`start`、`end`、`loc`、`source`，并为常见 import/export、declaration、expression、binding pattern 节点补充子字段；Vue 2.7 bridge compileScript 也保留相同公开数组形状。该项不代表完整 Babel AST parity、parser plugin parity、完整 `compileScript` result-shape parity、完整 TS type resolve 或 full Rust-backed SFC parity 完成。
- [x] Vue 3 SFC `compileScript` relative imported type resolution / `deps` slice：Rust SFC 现在解析一层本地相对 `.ts` / `.tsx` / `.d.ts` 类型导入，供 `defineProps<T>()`、`defineProps<T & U>()`、嵌套 literal reference、`defineEmits<T>()` 和 `defineModel<T>()` 宏类型推断使用；`compileScript().deps` 只在宏类型参数实际使用外部类型时记录归一化 forward-slash 文件路径，未使用 `import type` 不进入 deps，本地同名类型声明会遮蔽导入来源。该项不代表完整 TS `resolveType` parity、bare package/tsconfig/global types、`.vue` 外部类型、动态 import type、链式 re-export graph、namespace/default type import 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` relative re-exported type resolution / chained `deps` slice：Rust SFC 现在沿本地相对 `export { T as U } from './x'` 与 `export * from './x'` 类型重导出图解析宏类型，供 re-exported `defineProps` / `defineEmits` / `defineModel` 类型参数生成 runtime options，并把 facade 与 leaf 类型文件依赖集合写入 `compileScript().deps`。该项不代表完整 TS `resolveType` parity、bare package/tsconfig/global types、`.vue` 外部类型、dynamic import type、default/namespace type import、default re-export 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` relative dynamic import type resolution / nested `deps` slice：Rust SFC 现在解析简单相对 `import('./x').Name` 类型引用，供 `defineProps<import('./foo').Props>()`、`defineEmits<import('./events').Events>()`、`defineModel<import('./model').ModelValue>()` 和外部类型成员内嵌 `import('./leaf').T` 使用；`compileScript().deps` 会记录直接 dynamic import 文件与嵌套 leaf 类型文件。该项不代表完整 TS `resolveType` parity、bare package/tsconfig/global types、default expression/class/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` relative `.vue` external type extraction / arbitrary extension sidecar `deps` slice：Rust SFC 现在解析相对外部 `.vue` descriptor，抽取 normal/setup script 的 TS/TSX 类型源码并送入现有类型上下文，覆盖 `defineProps` / `defineEmits` / `defineModel` 与 chained `.ts` facade re-export；任意扩展名 sidecar（如 `foo.d.vue.ts`、`bar.d.css.ts`）优先于真实任意扩展名文件，`deps` 记录归一化 `.vue` 或 sidecar 路径。该项不代表完整 TS `resolveType` parity、bare package/tsconfig/global types、default expression/class/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` relative namespace / qualified type resolution slice：Rust SFC 现在把 exported namespace type members 投影为 dotted keys，支持 `import * as Types from './types'` 后的 `Types.Props` / `Types.Events` / `Types.ModelValue`，支持 nested namespace keys（如 `Leaf.Nested.ExtraProps`）和 qualified dynamic import type（如 `import('./dynamic').Types.Props`），并让这些类型驱动 `defineProps` / `defineEmits` / `defineModel` 与 `deps`。该项不代表完整 TS `resolveType` parity、bare package/tsconfig/global types、default expression/class/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` TypeScript enum runtime type resolution / `deps` slice：Rust SFC 现在在类型上下文中注册 local/exported TS enum，按官方边界把 string enum 推断为 `String`、numeric enum 推断为 `Number`、mixed enum 推断为 `[String, Number]`、无可识别 initializer 的 enum 回退为 `Number`，并让这些 enum 引用驱动 `defineProps` prop member 和 `defineModel` runtime type 推断；相对外部、re-export、namespace 与 qualified enum 类型路径会通过 bridge 保留 `compileScript().deps`。该项不代表完整 TS `resolveType` parity、bare package/tsconfig/global types、default expression/class/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` declaration merging type-resolution slice：Rust SFC 现在按 Vue 3 `resolveType` 边界合并同作用域同名 `interface` 成员、重复 `namespace` exported type members、`namespace` + `interface` 同名声明，以及重复 TS `enum` runtime constructors；这些 merged declarations 可驱动 `defineProps` indexed access / qualified type reference 和 `defineModel` runtime type，并通过外部 `types.ts` / bridge 路径保留 normalized `deps`。本地 enum 合并只针对当前作用域已注册 enum，避免与同名 imported/global type 遮蔽误合并。该项不代表 TypeScript checker-backed declaration merging、ambient module augmentation、value namespace lookup、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `TSPropertySignature` `@vue-ignore` runtime type slice：Rust SFC 现在识别 property signature type annotation 上的 `@vue-ignore`，将对应 prop runtime constructor 投影为 internal `Unknown`，并通过既有 runtime props codegen 输出官方 no-check `type: null` 形状，同时保留 prop key、requiredness、binding metadata 和相邻普通类型推断；外部 `types.ts` interface 路径通过 bridge 保留 normalized `deps`。该项不代表把 `@vue-ignore` 扩展到任意类型位置、改变 interface heritage ignore 语义、checker-backed `resolveType`、unsupported/cyclic diagnostics 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bare package type resolution / `deps` slice：Rust SFC 现在通过 ancestor `node_modules` 解析受限 bare package 类型导入，覆盖 package root/subpath、`types` / `typings`、简单 `exports` type target、简单 wildcard subpath exports、JS export target 的声明文件 fallback 和 `@types` fallback；package import、re-export graph、`export *`、namespace import 与 `import('pkg').Name` 可驱动 `defineProps` / `defineEmits` / `defineModel`，并通过 bridge 返回归一化 package declaration `deps`。该项不代表完整 TS `resolveType` parity、tsconfig/path/global types、完整 NodeNext conditional exports、default expression/class/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` tsconfig path/project-reference type resolution / `deps` slice：Rust SFC 现在在 bare package fallback 之前从 ancestor `tsconfig.json` 图解析受限 alias 类型导入，覆盖 `compilerOptions.paths`、简单 `baseUrl`、本地 `extends` string/array、project `references` 到 config 文件或文件夹、循环 reference guard 和 `${configDir}` path target；alias 命中的 `.ts` / `.tsx` / `.d.ts` / `.vue` / sidecar 候选可驱动 `defineProps` / `defineEmits` / `defineModel`，bridge 返回 normalized `deps`。该项不代表 TypeScript checker parity、完整 include/exclude 匹配、受限文件/package 切片之外的完整 tsconfig project-service 全局类型行为、任意 conditional/mapped type 求值、unsupported/cyclic diagnostics 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` tsconfig JSONC parsing / `deps` slice：Rust SFC 现在在读取受限 tsconfig alias/project-reference 图时接受 line/block comments 和 object/array trailing commas，并保护字符串字面量中的 `//`、`/* */` 和逗号内容不被误删；该 JSONC 解析只作用于 tsconfig 文件，package manifest 仍走既有严格 JSON package resolver。该项不代表 TypeScript checker parity、完整 include/exclude 匹配、受限文件/package 切片之外的完整 tsconfig project-service 全局类型行为、unsupported/cyclic diagnostics 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` package-based tsconfig `extends` / `deps` slice：Rust SFC 现在从当前 tsconfig 所在目录向上查找 ancestor `node_modules` 来解析非相对 `extends`，覆盖 scoped/unscoped package root、严格 JSON `package.json` 的 `tsconfig` 字段、package root `tsconfig.json` / `index.json`、以及 `pkg/shared` 这类 package subpath config 到 `shared.json` / subpath `tsconfig.json` / subpath `index.json`；扩展出来的 package config 继续进入既有 alias 图并保留 bridge normalized `deps`。该项不代表 TypeScript checker parity、完整 include/exclude 匹配、受限文件/package 切片之外的完整 tsconfig project-service 全局类型行为、完整 NodeNext conditional exports for tsconfig packages、unsupported/cyclic diagnostics 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bounded tsconfig automatic global `.d.ts` discovery / `deps` slice：Rust SFC 现在把当前 `.vue` 文件 ancestor tsconfig 图中发现的全局声明文件并入 Vue 3 全局类型上下文，覆盖 local/package `extends`、project `references`、`files` 中的 exact `.d.ts`，以及受支持 `.d.ts` / `.ts` / directory-like `include` pattern 的递归 `.d.ts` 收集；bridge 返回宏实际使用的 normalized global/leaf `deps`。该项不代表完整 TypeScript project service、完整 include/exclude 语义、动态 TypeScript 版本感知 package resolution、任意 type package layout、完整 NodeNext conditional exports、unsupported/cyclic diagnostics、任意 conditional/mapped type 求值或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bounded tsconfig `types` / `typeRoots` global declaration package discovery / `deps` slice：Rust SFC 现在在遍历 ancestor tsconfig 图时发现 `compilerOptions.types` / `compilerOptions.typeRoots` 指向的全局声明包，覆盖 local/package `extends`、project `references`、配置的 `typeRoots`、默认 ancestor `node_modules/@types`、`types: []` 显式空集、scoped package 目录形态，以及 package `types` / `typings` / 简单 `exports.types` / `index.d.ts` 入口；bridge 返回宏实际使用的 normalized global package declaration `deps`。该项不代表完整 TypeScript project service、完整 include/exclude 语义、动态 TypeScript 版本感知 package resolution、任意 type package layout、完整 NodeNext conditional exports、unsupported/cyclic diagnostics、任意 conditional/mapped type 求值或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bounded package `typesVersions` declaration-entry / `deps` slice：Rust SFC 现在在共享 package declaration-entry resolver 中应用受限 `package.json.typesVersions` 声明入口重定向，覆盖 bare package root/subpath、`@types` fallback package、显式 global re-export 触达的相对 package directory，以及 tsconfig `types` / `typeRoots` global declaration package；支持 root `types` / `typings` 目标重定向、subpath 单通配映射、锁定 TS 5.0 基线上的 Node semver selector、selector object order、`"*"` fallback selector 和单 `*` path capture，并保持既有 `exports` 优先级与 subpath exports blocking 语义。该项不代表动态 TypeScript 版本发现、TypeScript project service、任意 package layout、完整 NodeNext conditional exports、unsupported/cyclic diagnostics、完整官方 `resolveType` 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` package `typesVersions` Node semver selector/order / `deps` slice：Rust SFC 现在使用 `nodejs-semver` 解析 npm/node 风格 `typesVersions` range，并保留 package manifest 字段顺序选择第一个匹配锁定 TS 5.0 基线的 selector；覆盖 comparator sets、`||` alternatives、hyphen ranges、`x` / `*` ranges、tilde ranges、caret ranges、非匹配 future-version branch 和 ordered fallback 行为，并通过 `vuec_sfc` 与 `vuec_node_bridge` public JSON/deps 路径验证。该项不代表动态 TypeScript 版本发现、TypeScript project service、任意 package layout、完整 NodeNext conditional exports、unsupported/cyclic diagnostics、完整官方 `resolveType` 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` package `typesVersions` project-local TypeScript version discovery / `deps` slice：Rust SFC 现在从当前 SFC filename 构建只读 type resolver context，沿 ancestor `node_modules` 发现最近的 `typescript/package.json` 并用 `nodejs-semver` 解析 `version`，以项目本地 TypeScript 版本选择 `package.json.typesVersions` mapping；缺失或非法版本回退到 TS 5.0 基线。该 context 贯穿 bare package、`@types` fallback、package re-export graph、`import('pkg').Name`、显式 global re-export package directory 和 tsconfig `types` / `typeRoots` global package 解析路径，并通过 `vuec_sfc` 与 `vuec_node_bridge` public JSON/deps 路径验证。该项不代表完整 TypeScript project service、任意 package layout、完整 NodeNext conditional exports、unsupported/cyclic diagnostics、完整官方 `resolveType` 或 full Rust-backed SFC parity 完成；当前 Vue 3 SFC 官方 conformance 总体为 `461/461`，其中 public `parse.spec.ts` 为 `rust-backed 28/28`，其余 SFC 覆盖仍为 `mixed 433/433`。
- [x] Vue 3 SFC `compileScript` explicit `globalTypeFiles` type resolution / `deps` slice：Rust SFC 现在从显式 global type files 建立 Vue 3 SFC 类型上下文，覆盖 ambient `declare interface` / `declare type` / `declare enum` / `declare namespace` 文件和 module-style `export {}; declare global { ... }` 文件；global 文件内部 static imports 可参与声明解析但不会暴露为全局名，`compileScript().deps` 只记录宏类型参数实际使用的 global / leaf type 文件。bridge、NAPI、WASM 与 CLI `compile-sfc --global-type-file` 均透传该选项。该项不代表完整 TS `resolveType` parity、动态 TypeScript 版本感知 package resolution、任意 type package layout、default expression/class/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` explicit `globalTypeFiles` global re-export graph / `deps` slice：Rust SFC 现在把 `declare global { ... }` 内的 `export type { T } from './x'`、`export { T } from './x'` 和 `export * from './x'` 投影进显式 global 类型上下文，覆盖全局重导出名驱动 `defineProps` / `defineEmits` / `defineModel` 的 runtime inference，并让 `compileScript().deps` 保留 global 文件、facade/leaf 类型文件和相对 package directory 的 `package.json` `types` / `typings` 声明入口。该项不代表 TypeScript checker-backed graph diagnostics、动态 TypeScript 版本感知 package resolution、任意 type package layout、完整 NodeNext package resolution 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` class declaration type runtime inference / default class `deps` slice：Rust SFC 现在把 TS `class` declaration 作为官方 `Object` runtime type 写入类型上下文，覆盖本地 class、前向 class 引用、named/default exported class、default class re-export facade、named class re-export facade 和显式 global `declare class`；这些 class 引用可驱动 `defineProps` prop member runtime type 和 `defineModel<ClassType>()`，bridge `deps` 会保留使用到的 class type import/re-export/global 链。该项不代表 class members 作为 props 抽取、default expression/function exports、declared props factory / bounded `ReturnType<T>` runtime 投影之外的任意声明函数类型解析、完整 TS `resolveType` parity 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bounded `ExtractPropTypes` / declared props factory `ReturnType` props-options type resolution slice：Rust SFC 现在记录 `declare const props: { ... }` 的 runtime props options，以及 `declare const props: () => { ... }` / `declare function uploadProps<T>(): { ... }` 的 props factory return options；`ExtractPropTypes<typeof props>`、`import('vue').ExtractPropTypes<ReturnType<typeof props>>`、`ExtractPropTypes<ReturnType<typeof uploadProps>>` 可反向推断 runtime props，`Partial` / `Required` 可调整 requiredness，并通过相对外部、re-export、namespace、bare package、显式 `globalTypeFiles` 和 bridge `deps` 路径保留依赖。该项不代表任意 conditional/mapped type 求值、TypeScript checker 集成、完整 default-backed requiredness、checker-backed 任意函数返回类型求值、完整 TS `resolveType` parity 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bounded static runtime props object `ExtractPropTypes<typeof ...>` projection slice：Rust SFC 现在记录静态可分析的 runtime props option object 值，覆盖真实本地/外部 value exports：`export const props = { ... }`、`const props = { ... }; export { props }`、`const props = { ... }; export { props as default }` 和 direct static `export default { ... }`；named/default imports 可驱动 `ExtractPropTypes<typeof importedProps>`，支持 constructor、constructor array、`{ type, required, default, validator }`、`Object as PropType<User>` 等 TS assertion，并通过 bridge `deps` 保留 props 文件与 `PropType` leaf 类型文件。该项不代表 top-level spread/computed key、任意 runtime expression、non-props default expression、完整 default-backed requiredness、任意 `typeof` value lookup 或 full Rust-backed SFC parity 完成。
- [x] Vue 3 SFC `compileScript` utility/generic props type resolution slice：Rust SFC 现在解析 `Readonly<T>` 透明 props 包装、`Pick<T, K>` / `Omit<T, K>` 静态 key 过滤、string literal key aliases、可解析 props members 上的 `keyof`，并通过 scoped type context 绑定简单 generic type alias 参数；`Readonly<Partial<T>>`、`Props<T, U> = T & U & { baz: boolean }`、`Optional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>` 等本地/外部 alias 可驱动 `defineProps` runtime options 和 bridge `deps`。该项不代表任意 mapped/conditional type 求值、TypeScript checker 集成、递归 generic type 求解、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `Record<K, V>` props type projection slice：Rust SFC 现在解析受支持的静态可展开 `Record` props utility，当 `K` 可归约为有限 ordered key set 时生成 runtime props，覆盖 literal/template-literal keys、string manipulation helpers、静态 `Extract` / `Exclude`、intersection、`Partial<Record<...>>` requiredness 和外部 generic alias `deps`。该项不代表 `Record<string, T>` / index signature 展开、任意 conditional/mapped type 求值、TypeScript checker 集成、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` indexed access runtime type projection slice：Rust SFC 现在解析受支持的 `T[K]` runtime type 投影，当 object side 可投影为 props-like members 且 index side 可归约为有限 ordered key set 时，复用被选中成员的 runtime types；当 object side 可投影为数组/元组 element runtime declaration 且 index side 是 `number` 时，复用 element runtime types。该项覆盖直接 `Base['name']`、union key、function member、`ValueOf<T, K extends keyof T> = T[K]` 简单 generic scalar alias、`A[number]`、`Array<T>[number]`、tuple / named tuple `T[number]`，以及外部 `Props` / `ModelValue` alias `deps`，驱动 `defineProps` prop member 和 `defineModel` runtime type。该项不代表宽泛 index signature、完整 parameter tuple utility 语义、任意 conditional/mapped type 求值、TypeScript checker 集成、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` parameter tuple utility indexed-access runtime projection slice：Rust SFC 现在解析受限 `Parameters<T>[number|N]` 与 `ConstructorParameters<T>[number|N]` runtime constructor 投影，覆盖 inline function/constructor types、type literal call/construct signatures、interface call/construct signatures、interface extends inherited call/construct signatures、本地 alias、外部 `Props` / `ModelValue` alias `deps`、numeric tuple index、number-indexed tuple union，以及 rest parameter 的静态 array element runtime type；多个 call/construct signatures 按参数位置合并 runtime constructors 并保留 first-seen 顺序；未 indexed 的 `Parameters<T>` / `ConstructorParameters<T>` 仍按 Vue 3 name-only runtime inference 输出 `Array`。该项不代表 overload selection、完整 TypeScript utility 语义、TypeScript checker-backed parameter inference、任意 conditional/mapped type 求值、递归 generic type 求解、unsupported/cyclic diagnostics、完整官方 `resolveType` 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` runtime utility type projection slice：Rust SFC 现在按 Vue 3 runtime inference 边界解析受支持的 `NonNullable<T>`、`Extract<T, U>` 和 `Exclude<T, U>` runtime constructor types，驱动 `defineProps` prop member 和 `defineModel` runtime type，并覆盖外部 `Props` / `ModelValue` alias `deps`。其中 `NonNullable` 仅过滤 `null`，`Extract` 使用第二个类型参数，`Exclude` 使用第一个类型参数。该项不代表真实 TypeScript conditional utility 求值、任意 conditional/mapped type 求值、TypeScript checker 集成、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bounded static conditional type runtime projection slice：Rust SFC 现在在条件可由有限静态 literal set 判定时解析 `TSConditionalType` runtime constructors，驱动 `defineProps` prop member 和 `defineModel` runtime type，覆盖直接 true/false literal 条件、本地/外部 generic alias（如 `Runtime<T> = T extends 'text' ? string : T extends 'count' ? number : boolean`）和 bridge normalized `deps`；混合或未解析分支保持现有 unknown/no-check 输出。该项不代表 distributive conditional evaluation、`infer` clauses、TypeScript checker assignability、任意 conditional/mapped type 求值、完整官方 `resolveType` 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` BigInt literal runtime constructor projection slice：Rust SFC 现在按 Vue 3 official `inferRuntimeType` 边界把 `TSLiteralType(BigIntLiteral)`（如 `1n`）投影为 `Number` runtime constructor，覆盖 `defineProps` prop member、本地 alias、union、`defineModel` 和外部 `Props` / `ModelValue` alias bridge `deps`；`bigint` 关键字仍保持 official unknown/no-check 输出（`type: null`）。该项不代表任意 BigInt type 语义、TypeScript checker 求值、任意 conditional/mapped type 求值、完整官方 `resolveType` 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `ReturnType<T>` runtime constructor projection slice：Rust SFC 现在记录受限 return-runtime declarations，覆盖 declared functions、declared callable variables、function type aliases、type literal call signatures、interface call signatures、interface extends inherited call signatures、`typeof` declared values、scoped generic aliases、import type、re-export、namespace 和外部 `Props` / `ModelValue` alias `deps`；`ReturnType<T>` 可驱动 `defineProps` prop member 与 `defineModel` runtime constructor 投影。多个 call signatures 沿用当前保守 first-seen runtime constructor 合并。该项不代表 overload selection、TypeScript checker-backed function return inference、arbitrary callable value lookup、arbitrary default expression exports、checker-backed/任意无注解函数体返回类型推断、任意 conditional/mapped type 求值、递归 generic type 求解、unsupported/cyclic diagnostics、完整官方 `resolveType` 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` annotated exported-function `ReturnType<typeof ...>` projection slice：Rust SFC 现在把带显式返回注解的真实外部 `.ts` `export function makeX(): T`、命名 `export default function makeX(): T` 和匿名 `export default function(): T` 注册为 return-runtime declarations；相对 named/default imports 可通过 `ReturnType<typeof imported>` 驱动 `defineProps` prop member 和 `defineModel` runtime constructor 投影，并通过 bridge 保留归一化 `deps`。该项不代表 checker-backed/任意无注解函数体返回类型推断、任意 default expression export 求值、overload selection、TypeScript checker-backed callable resolution 或完整官方 `resolveType` 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` annotated exported function-value `ReturnType<typeof ...>` projection slice：Rust SFC 现在把带显式函数类型/返回注解的真实外部 `.ts` `export const make = (): T => ...`、`export const make: () => T = ...`、`export const make = function(): T { ... }` 和带注解的 default arrow/function-expression export 注册为 return-runtime declarations；相对 named/default imports 可通过 `ReturnType<typeof imported>` 驱动 `defineProps` prop member 和 `defineModel` runtime constructor 投影，并通过 bridge 保留归一化 `deps`。该项不代表 checker-backed/任意无注解函数体返回类型推断、任意 callable/default expression 求值、overload selection、TypeScript checker-backed callable resolution 或完整官方 `resolveType` 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` bounded unannotated function-body `ReturnType<typeof ...>` projection slice：Rust SFC 现在对静态显而易见的无返回注解函数体记录 return-runtime declarations，覆盖真实外部 `.ts` named/default function declarations、arrow/function values、default function expressions、`ReturnType<typeof imported>` 和 `ReturnType<typeof import('./x').make>`，可驱动 `defineProps` prop member 和 `defineModel` runtime constructor 投影，并通过 bridge 保留归一化 `deps`。支持表达式箭头、单一直接 `return`、简单 block wrapping、完整 `if/else` return、字面量、数组、对象、函数值、conditional expression 以及 `new Date()` / `new Error()` 等已知 builtin constructor。该项不代表 checker-backed 任意函数返回类型推断、任意控制流求值、async/promise unwrapping、overload selection、generic return solving、任意 default expression/callable 求值或完整官方 `resolveType` 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` builtin wrapper/container runtime type inference slice：Rust SFC 现在按 Vue 3 name-only runtime inference 边界解析受支持的 builtin wrapper/container 类型，覆盖 `ReadonlyArray` / 未 indexed 的 `Parameters` / 未 indexed 的 `ConstructorParameters` 到 `Array`，`ReadonlyMap` 到 `Map`，`ReadonlySet` 到 `Set`，`Error` 到 `Error`，string helpers 到 `String`，ref wrappers 到 `Object`，以及 `MaybeRef` / `MaybeRefOrGetter` 合并内部 runtime types，并通过外部 `Props` / `ModelValue` alias 保留 bridge `deps`。该项不代表 imported Vue type definition 解析、完整 parameter tuple utility 语义、任意 conditional/mapped type 求值、TypeScript checker 集成、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` mapped identity runtime type inference slice：Rust SFC 现在按 Vue 3 `inferRuntimeType` 的受限 identity mapped 分支解析 generic alias `{ [K in keyof T]: T[K] }`，仅当 scoped generic 参数、`keyof T` constraint、`T[K]` object/key 均匹配官方形状时，把绑定的 `T` runtime constructor types 透传给 `defineProps` prop member 和 `defineModel` runtime type；外部 `Props` / `ModelValue` alias 通过 bridge 保留归一化 `deps`。该项不代表任意 mapped type 求值、非 identity mapped runtime inference、TypeScript checker 集成、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `TSTypeOperator` runtime type inference slice：Rust SFC 现在按 Vue 3 `inferRuntimeType` 的 runtime constructor projection 边界处理 type operators，非 `keyof` operator（如 `readonly`）解包内部类型，`keyof` 对 resolvable type literal、interface、alias、generic alias、import type、union/intersection、`keyof any`、builtin container 和受支持 `Pick` / `Extract` utility 输出运行时构造器集合；外部 `Props` / `ModelValue` alias 通过 `keyof_runtime_type_declarations` 保留归一化 bridge `deps`。该项不代表完整 TypeScript key-space 求值、checker-backed `keyof`、任意 conditional/mapped type 求值、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `TSTypeQuery` / `typeof` declared-value runtime type inference slice：Rust SFC 现在按 Vue 3 受限 declared-value `typeof` 边界解析已注册 value annotation，把 `typeof value`、`typeof Values.text`、`keyof typeof Values.boxed` 和 `defineModel<typeof value>` / `defineModel<typeof Values.text>` 投影到 runtime constructors，并通过外部 `Props` / `ModelValue` alias 与 namespace/re-export 路径保留归一化 bridge `deps`。该项不代表任意 `typeof` expression、TS checker-backed value-space resolve、完整 TypeScript key-space 求值、任意 conditional/mapped type 求值、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` import-type `TSTypeQuery` / `typeof import(...)` declared-value runtime type inference slice：Rust SFC 现在把 `TSTypeQueryExprName::TSImportType` 接入现有受限 dynamic import type resolver，覆盖 `typeof import('./values').text`、`keyof typeof import('./values').boxed`、`ReturnType<typeof import('./values').make>`、`ExtractPropTypes<typeof import('./values').options>` 和 `defineModel<typeof import('./values').text>` 的 runtime constructor 投影，并通过 bridge 保留归一化直接与嵌套 `deps`。该项不代表任意 `typeof` expression、TypeScript checker-backed value-space resolve、任意 runtime/default expression 求值、checker-backed/任意无注解函数体返回类型推断或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` call/construct signature runtime type inference slice：Rust SFC 现在按 Vue 3 object/interface member runtime inference 边界，把 type literal / interface 中的 `TSCallSignatureDeclaration` 和 `TSConstructSignatureDeclaration` 投影为 `Function`，其它成员投影为 `Object`，空 object/interface 仍投影为 `Object`；interface declaration refresh 会同步更新 `defineProps` 与 `defineModel` runtime type 缓存，并通过外部 `Props` / `ModelValue` alias 保留归一化 bridge `deps`。该项不代表 runtime property validation、完整 intersection property validation、任意 TypeScript checker 求值、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `TSIntersectionType` runtime type flattening slice：Rust SFC 现在按 Vue 3 `flattenTypes(...).filter(UNKNOWN_TYPE)` 的受限 runtime constructor 边界，对普通 `defineProps` 和 `defineModel` 的 intersection 分支做 first-seen 合并并过滤内部 `Unknown`；普通 props 生成阶段会把 `Unknown | Boolean` / `Unknown | Function` 输出为 `skipCheck: true`，纯 unknown 输出为 `type: null`。外部 `Props` / `ModelValue` alias 通过 bridge 保留归一化 `deps`。该项不代表完整 intersection property validation、任意 TypeScript checker 求值、任意 conditional/mapped type 求值、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` union/intersection duplicate props member merge slice：Rust SFC 现在按 Vue 3 `resolveTypeElements().mergeElements` 的受限 props member 边界，合并 `defineProps<T>()` 类型中 union / intersection 分支的同名 prop，保留首次出现顺序，合并 runtime constructors，并按官方 `optional || optional` 语义把任一分支 optional 投影为 `required: false`；intersection 重名 prop 合并时沿用 `Unknown` 过滤，union 重名 prop 合并保留 `Unknown` 以触发普通 props `skipCheck` / `type: null` 边界。外部 `Props` alias 通过 bridge 保留归一化 `deps`。该项不代表完整 intersection property validation、失败 extends diagnostics、`@vue-ignore`、任意 TypeScript checker 求值、unsupported/cyclic diagnostics、任意 conditional/mapped type 求值或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` mapped props type projection slice：Rust SFC 现在解析受支持的静态可展开 mapped props 类型，覆盖 template-literal key remapping、有序笛卡尔 key 展开、`Capitalize` / `Uppercase` / `Lowercase` / `Uncapitalize`、静态 `Extract` / `Exclude`、以及 scoped type argument 绑定下的 generic mapped alias。外部 generic mapped alias 会保留同文件静态 string-key helper alias，并通过 bridge 返回归一化 `compileScript().deps`。该项不代表任意 conditional/mapped type 求值、TypeScript checker 集成、递归 generic type 求解、unsupported/cyclic diagnostics、tsconfig/path/project references、default expression/function exports 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` interface `extends` type-member resolution slice：Rust SFC 现在解析受支持的 `interface Props extends Base { ... }` 成员，供 `defineProps` runtime props 和 `defineEmits` inherited call signatures 使用，并保留当前 interface body props 覆盖 inherited props 的官方优先级。外部类型文件会先 seed 自身 `import type` 上下文，因此 default exported interface 可以继承 imported base，同时保留 inherited props 与归一化 bridge `deps`。该项不代表 failed-extends diagnostics、`/* @vue-ignore */`、任意 TypeScript checker 求值或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` declaration-order-independent interface `extends` type resolution slice：Rust SFC 现在在同一 statement set 的所有声明注册后刷新 interface props/emits 投影，并以 interface 声明数量为上限做 bounded fixed-point pass；受支持的同作用域与外部 named exported `interface Props extends Base` 链不再依赖 `Base` 是否写在 `Props` 之前。该项覆盖 `defineProps`、`defineEmits` 和 bridge `deps`，但不代表 failed-extends diagnostics、`/* @vue-ignore */`、unsupported/cyclic interface diagnostics、任意 TypeScript checker 求值或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` interface `extends` failed diagnostics / `@vue-ignore` slice：Rust SFC 现在在 `defineProps<T>()` props type resolution 中按 Vue 3 `resolveInterfaceMembers` 边界报告 unresolved `interface Props extends Base` 的官方 failed-extends diagnostic，并识别紧贴 heritage base 的 `/* @vue-ignore */` / line comment，使该 base 按 fallthrough attrs 语义跳过且不生成 inherited props。诊断会随本地、外部类型文件、re-export/alias type context 传播到 `compileScript().errors`，bridge 保留相关 normalized `deps`。`defineEmits` / `keyof` / runtime inference 继承失败维持当前静默边界。该项不代表 unsupported/cyclic interface diagnostics、任意 TypeScript checker 求值、完整 intersection property validation、任意 conditional/mapped type 求值或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` top-level `defineProps<T>()` `resolveType` error diagnostics slice：Rust SFC 现在区分静默 runtime/member inference 与 `defineProps<T>()` 消费型 props type resolution，按官方报告 unresolved type reference、failed import source、unsupported computed key 和 unsupported indexed access key type；member-level unknown runtime type 仍保持静默，global type file 内部 imports 不暴露为 props/deps 且不误报。该项不代表 arbitrary TypeScript checker evaluation、unsupported/cyclic diagnostics、动态 TypeScript 版本感知 package resolution、任意 type package layout、任意 conditional/mapped type 求值、完整 `resolveType` 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` declaration-order-independent type alias props projection slice：Rust SFC 现在在同一 statement set 的所有声明注册后刷新 type alias props/runtime/string-key/generic 投影，并以 refreshable type 声明数量为上限做 bounded fixed-point pass；受支持的本地与外部 alias 链如 `type Props = Mid & { own: string }; type Mid = Base & { mid?: boolean }; interface Base { inherited?: number }` 不再依赖声明顺序。该项覆盖 `defineProps` runtime props、prop bindings 和 bridge `deps`，但不代表 emits intersection alias、unsupported/cyclic graph diagnostics、任意 mapped/conditional type 求值、TypeScript checker 集成或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` type alias intersection `defineEmits` projection slice：Rust SFC 现在解析受支持的 `defineEmits` type alias intersection，alias body 可由 call-signature literal、interface、import、parenthesized type 和其它已声明 alias 组成；type alias declaration refresh 与 macro resolution 复用同一 emits resolver，因此本地与外部 forward alias 链可生成 runtime emits 并保留归一化 bridge `deps`。该项不代表任意 union-of-function emits、conditional/mapped type 求值、TypeScript checker 集成、unsupported/cyclic graph diagnostics、tsconfig/path/project references 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `defineEmits` property syntax projection slice：Rust SFC 现在从受支持的 Vue 3 type literal / interface property syntax（如 `{ foo: []; bar: [id: number]; 'foo:bar': [] }`）抽取 runtime emits，外部 type alias 通过 bridge 保留归一化 `compileScript().deps`，并按官方报告 property syntax 与 call signature 混用诊断。该项不代表任意 union-of-function emits、完整 `resolveType`、TypeScript checker 集成、unsupported/cyclic graph diagnostics、tsconfig/path/project references 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` `defineEmits` union function type projection slice：Rust SFC 现在解析受支持的 union-of-function emits 类型和有序 string literal 事件名 alias，包含外部 alias 与 bridge `deps`；Vue 2.7 union function rejection 保持不变。该项不代表任意 mapped/conditional emits、TypeScript checker 集成、unsupported/cyclic diagnostics、完整 `resolveType`、tsconfig/path/project references 或 full Rust-backed SFC parity 完成；Vue 3 SFC 官方 conformance 仍按 mixed 覆盖报告。
- [x] Vue 3 SFC `compileScript` normal script binding metadata slice：Rust SFC 对 normal-only `export default {}` 选项执行 Vue 3 public binding metadata 分析，覆盖 `props`、`inject`、`setup()` return、`data()` return、`methods` 和 `computed`，并避免把 normal-only 顶层声明误暴露；混合 `<script>` + `<script setup>` 路径按官方 `walkDeclaration('script')` 边界分类 normal script value declarations，并合并进 public / inline-template binding metadata 与 setup return getter/setter。generated/NAPI alias 仅在 JavaScript package API 边界 hydrate `bindings.__isScriptSetup` 为 non-enumerable，非编译器语义。该项不代表完整 normal script analysis、完整 result shape、source map、完整 TS type resolve 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineOptions` macro slice：Rust SFC 支持 top-level `defineOptions(...)` runtime options 合并、TS assertion unwrap、compiler macro import cleanup、基础 binding 清理和 duplicate/type-argument/assignment/alias/reserved-key diagnostics；generated/NAPI public alias 会把 Rust `compileScript.errors` 投影为官方前缀异常。该项不代表完整 macro analysis、defineModel、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineExpose()` macro diagnostics slice：Rust SFC 支持 top-level `defineExpose(...)` 到 `__expose(...)` 的调用改写、默认空 `__expose()` 抑制、compiler macro import cleanup，并按官方报告 duplicate `defineExpose()` diagnostics。该项不代表完整 `compileScript` result shape、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineSlots()` macro subset：Rust SFC 支持 bound `defineSlots()` 到 `_useSlots()` 的调用改写、unbound type-only 调用擦除、compiler macro import cleanup、基础 binding 清理和 duplicate/runtime-argument diagnostics。该项不代表完整 macro analysis、完整 TS type resolve、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineModel` runtime macro base slice：Rust SFC 支持基础 `defineModel(...)` runtime model props / modifiers props、`update:*` emits、`_useModel(__props, ...)` 调用改写、与 `defineProps` / `defineEmits` runtime options 的 `_mergeModels` 合并、compiler macro import cleanup、基础 binding 清理和 duplicate model diagnostics。该项不代表完整 defineModel parity、TS runtime model type inference、production type erasure、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineModel<T>()` TypeScript runtime type subset：Rust SFC 支持基础 model 类型参数推断，覆盖 primitive / union / function / object / array / literal runtime type、`any | boolean` 的 `skipCheck`、typed runtime options spread，以及 Vue 3 production model type erasure 中 Boolean 与 Function+runtime-options 保留规则。该项不代表完整 defineModel parity、完整 imported/external TS type resolve、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。
- [x] Vue 3 SFC `compileScript` `defineModel` get/set transformer option splitting subset：Rust SFC 对静态 object literal model options 做官方一致拆分：prop options 进入 model props runtime，`get` / `set` transformer options 保留在 `_useModel(__props, ...)` call-site；覆盖 default/named model、typed runtime merge、quoted `"get"` / `"set"` keys 和 transformer-only options，并保持 spread/computed options 不拆分。该项不代表完整 defineModel parity、完整 imported/external TS type resolve、reactive destructure、top-level await、inline template、source map 或 full `compileScript` parity 完成。

完成判定：

- [x] Vue 3 `compiler-sfc` parse 测试通过。
- [x] Vue 3 `compileScript` 测试通过。
- [x] Vue 3 `compileTemplate` 测试通过。
- [x] Vue 3 SFC source map 测试通过。
- [x] Vue 3 `compiler-sfc/__tests__` 100% 通过。

### P5: Style / SSR

目标：完成 SFC style 和 SSR 编译路径。

当前 Vue 3 SSR 官方 generated-alias / NAPI conformance 为 `129/129`，coverage 为 `rust-backed 129/129`、`mixed 0/0`、`shim-backed 0/0`。Vue 3 SFC public `compileStyle.spec.ts` 已通过 public `@vue/compiler-sfc` alias 路由到 Rust `vuec_style`，报告为 `rust-backed 26/26`；JavaScript package boundary 仅 materialize caller-provided `preprocessOptions.additionalData` function callbacks 和 public result shape。Style 的 Vue 2.7 `compileStyle.spec.ts` 仍因 PostCSS callback API 边界标记为 `mixed 10/10`。以下验收项的勾选表示官方/public contract 门禁通过；未勾选的 style / PostCSS 项仍需继续补齐 Rust-backed 覆盖或记录边界。

- [ ] scoped CSS 完成。
- [x] Rust scoped CSS deep-container special-branch slice：`vuec_style` 支持官方 `:is()` / `:where()` / `:has()` / `:not()` 中混合 `:deep(...)`、`:global(...)`、`:slotted(...)` 分支的 scoped rewrite 边界，区分 scope-anchored selector、nested at-rule-only block、direct native nested selector block，并保持 slotted prefix 与 slot scope 注入位置；`vuec_sfc` 已通过 public style wrapper 暴露。未声明完整 scoped CSS、PostCSS 或 preprocessor parity 完成。
- [x] Rust scoped CSS deep-container split pseudo-suffix slice：`vuec_style` 支持官方 deep-bearing `:is()` / `:where()` 后接纯 pseudo suffix（如 `:hover`、`::before`、`:nth-child(...)`）时的 split rewrite，把非 deep 的普通 / `:global(...)` / `:slotted(...)` branch scope 注入保留在 container 内，并保持 `:has(...)` outer-scope 路径与官方 comma whitespace；`vuec_sfc` 已通过 public style wrapper 暴露。未声明完整 scoped CSS、PostCSS 或 preprocessor parity 完成。
- [x] Rust scoped CSS direct nested parent selector slice：`vuec_style` 支持官方 direct native nested rule body 的 parent selector 规范化，包括 leading universal selector 删除、top-level `:global(...)` 替换、top-level `:slotted(...)` 无 slot scope 解包，以及 `:is()` / `:where()` parent container 中 `:global(...)` / `:slotted(...)` / universal branch 在纯 pseudo suffix 下的规范化；`vuec_sfc` 已通过 public style wrapper 暴露。未声明完整 scoped CSS、PostCSS 或 preprocessor parity 完成。
- [x] Rust scoped CSS first-normal direct deep-container nested rule slice：`vuec_style` 支持官方无 trailing suffix 的 direct native nested deep-container 路径，当 container 第一 branch 不是 `:deep(...)` 时，首个 deep branch 前使用 direct nested parent selector 规范化，之后走 scoped/deep branch rewrite，并保持 outer scope 注入、`& { ... }` parent declaration 包装和 nested selector deep passthrough；`vuec_sfc` 已通过 public style wrapper 暴露。完整 scoped CSS、PostCSS parity 仍未声明完成。
- [x] Rust scoped CSS first-normal direct deep-container trailing-suffix slice：`vuec_style` 支持上述 first-normal direct native nested deep-container 的 trailing pseudo / class-compound / descendant suffix 路径，覆盖无 scope anchor 时 `:is()` / `:where()` / `:has()` 的 split selector 输出、有 scope anchor 时的 prefix scope 注入、branch-specific comma spacing、`& { ... }` parent declaration 包装和 nested selector deep passthrough；`vuec_sfc` 已通过 public style wrapper 暴露。完整 scoped CSS、PostCSS parity 仍未声明完成。
- [x] Rust scoped CSS deep passthrough nested at-rule slice：`vuec_style` 在 deep passthrough rule 的 nested `@media` / `@supports` 内继续按官方解包 `:deep(...)`、`:global(...)`、`:slotted(...)`，不额外注入 scope 或 slot scope，并保持 declaration-to-at-rule 格式；`vuec_sfc` 已通过 public style wrapper 暴露。未声明完整 scoped CSS、PostCSS 或 preprocessor parity 完成。
- [x] Rust scoped CSS deep passthrough parent-anchor nested block formatting slice：`vuec_style` 支持 deep passthrough rule body 中 top-level `&... { ... }` native nested block 的官方换行格式，覆盖 anchor 前 comments、declaration-to-anchor 顺序和 deep-container passthrough 分支；`vuec_sfc` 已通过 public style wrapper 暴露。`:has(...)` comma-spacing 细节和完整 scoped CSS、PostCSS parity 仍未声明完成。
- [x] Rust scoped CSS `:has(...)` split pseudo comma-spacing slice：`vuec_style` 支持官方 deep-bearing `:has(...)` selector 在 trailing pseudo suffix 下 split 输出时的 comma spacing：deep branch 前保留 comma-space，后续 non-deep branch 注入保持 compact comma；`vuec_sfc` 已通过 public style wrapper 暴露。完整 scoped CSS、PostCSS parity 仍未声明完成。
- [x] Rust scoped CSS direct nested parent-selector comment slice：`vuec_style` 支持 direct native nested rule parent selector 中 whitespace/combinator 位置 comments 的官方差异：不需要结构性 parent normalization 时保留 comments，需要 selector-list / `:is()` / `:where()` branch normalization 时仍按官方移除；`vuec_sfc` 已通过 public style wrapper 暴露。完整 scoped CSS、PostCSS parity 仍未声明完成。
- [x] CSS vars 完成。
- [x] Rust style CSS vars comment-separated binding slice：`vuec_style` 支持官方 `v-bind /*...*/ (expr)` / `v-bind /**/ /**/ (expr)` / `v-bind/**/ (expr)` CSS variable collect/rewrite 边界，保持 `v-bind/**/(expr)` 在 `compileStyle` 中不重写但在 SFC cssVars 收集中可见的官方差异，并通过 `vuec_sfc` / Vue 2.7 parse bridge 暴露；同轮修复 `:deep` / `::v-deep` 普通声明块缩进格式，未声明完整 CSS vars、scoped CSS 或 PostCSS parity 完成。
- [ ] CSS modules 完成。
- [x] Rust style CSS Modules slice：`vuec_style` 支持默认 local class/id selector scoping、class 属性选择器按官方 public path 保持非本地化、`:global(...)`、`:local(...)`、`scopeBehaviour: "global"`、`globalModulePaths` 静态字符串 pattern 的入口级默认 global scope、官方默认 scoped-name hash / line-number 命名、`generateScopedName` 模板与 `hashPrefix`、local `@keyframes` / `animation-name` / `animation` token scoping、`localsConvention` 字符串模式、`exportGlobals`（class/id）、同文件 `composes` / `compose-with`（已定义本地 class 与 `global(...)`）、`composes ... from global`、ICSS `:export`、本地相对 `:import(...)` symbol replacement、本地相对 `composes ... from "./dep.css"` 外部组合、默认文件系统 loader 的 `node_modules` package subpath / `package.json.main` / 静态 package `exports` root、精确子路径、CJS active condition（`require` / `node` / `default`）对象顺序/嵌套对象和简单单通配符子路径 CSS lookup、同文件/顺序非法 `composes` class 的结构化 diagnostics，以及非单一 local class selector 上 `composes` 的官方前缀错误 diagnostics，并通过 SFC / bridge / NAPI / WASM 返回 module export map；JS `resolve` / `Loader` 回调、`globalModulePaths` RegExp 对象 / 无效 pattern 错误 parity、完整 package `exports` condition set / pattern / Node resolution、完整 PostCSS Modules diagnostics 和完整 PostCSS Modules 生态 parity 仍未声明完成。
- [x] Rust style CSS Modules local `@value` slice：`vuec_style` 支持本地 `@value name: value;` 的移除、module export 和官方 single-pass replacement，覆盖 selectors、at-rule preludes、declarations、quoted strings，并跳过 comments；`vuec_sfc` 已通过 public style wrapper 暴露。`@value ... from` imports、JS `resolve` / `Loader` 回调和完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style CSS Modules static `@value ... from` import slice：`vuec_style` 支持静态 value imports 通过 Rust CSS Modules file loader 解析和递归编译，覆盖 imported value/class token 的 module export、declarations、at-rule preludes、selectors、strings、`composes` replacement，避免 imported class 二次 scoped，并保留官方 missing import 的 `undefined` / `i__const_*` 形状；`vuec_sfc` 已通过 public style wrapper 暴露。JS resolver/Loader 回调和完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style CSS Modules static `@value` missing compose slice：`vuec_style` 区分 imported value 的 CSS replacement 与 module/compose value，missing static value imports 在 CSS 文本中仍替换为官方 `i__const_*`，但在 `composes` / module export 中使用 `undefined`；`vuec_sfc` 已通过 public style wrapper 暴露。JS resolver/Loader 回调和完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style CSS Modules imported CSS formatting slice：`vuec_style` 对 CSS Modules dependency source 走 entry style normalization，同时保留原始 dependency source 参与 scoped-name hash，并按官方区分 entry-level 与 recursive import 的 block-boundary newline；`vuec_sfc` 已通过 public style wrapper 暴露。完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style CSS Modules ICSS import symbol replacement slice：`vuec_style` 支持 `:import(...)` 符号在 selectors、at-rule preludes、declarations、quoted strings、`composes` 中按官方替换，imported selector token 不再作为本地 module key 导出或二次 scoped，并清理移除 ICSS rule 后的空白；`vuec_sfc` 已通过 public style wrapper 暴露。JS resolver/Loader 回调和完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style CSS Modules ICSS missing import/export alias slice：`vuec_style` 支持 missing `:import(...)` remote symbol 的官方行为，selector token 保持 global 且不导出，declaration / at-rule / string 保持未替换，`composes` 与 `:export` alias 使用 `undefined`，found import alias 在 `:export` 中解析为 imported value；`vuec_sfc` 已保持 CSS Modules 成功但无导出时的 `modules: {}` public shape。JS resolver/Loader 回调和完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style CSS Modules native nesting rewrite slice：`vuec_style` 支持 native nested rule body 中 CSS Modules selector 递归 rewrite，覆盖直接嵌套 selector、`&` selector、nested `@media` / `@supports`、nested `:local(...)` / `:global(...)`，并保持 nested block 前后 declaration 顺序；`vuec_sfc` 已通过 public style wrapper 暴露。Native nested `composes` diagnostics、JS resolver/Loader 回调和完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style CSS Modules native nested compose diagnostics slice：`vuec_style` 支持 native nested style rule 内 `composes` / `compose-with` 的官方 fatal diagnostics，错误消息覆盖 nested direct selector、`&` selector、nested `@media`、nested `:local(...)` 和 `from global` -> `global(...)` 投影，并在 fatal compose diagnostics 下返回空 CSS / 无 module map；top-level container at-rule 内合法 composition 仍保持可用。`vuec_sfc` 已通过 public style wrapper 暴露。JS resolver/Loader 回调和完整 PostCSS Modules parity 仍未声明完成。
- [x] Rust style error mapping slice：当前 Rust style 错误会保留 `errors` 字符串并额外返回结构化 `diagnostics`；unsupported preprocessor 和 unresolved `@import` 已有源码 span，SFC 会映射回原 `.vue` offset；仍未声明完整 PostCSS/lightningcss error parity。
- [ ] PostCSS/lightningcss/预处理适配层完成。
- [ ] SSR core transforms 完成。
- [ ] SSR codegen 完成。
- [ ] SSR component / slot / teleport / suspense 行为完成。
- [x] Rust compiler-ssr `ssrScopeId.spec.ts` public compile coverage slice：`vuec_vue3_core` SSR MIR codegen 支持官方 scoped slot VNode fallback 输出，包括 text fallback 的 `_createTextVNode(...)`、element VNode fallback 过滤 SSR-only scope attrs、nested component slot object fallback、helper ordering 和 nested `_ssrRenderComponent(..., _scopeId)` 传播；`xtask` 仅将 `ssrScopeId.spec.ts` 路由到 public `@vue/compiler-ssr` 并标记 Rust-backed。
- [x] Rust compiler-ssr `ssrFallthroughAttrs.spec.ts` public compile coverage slice：`vuec_vue3_core` SSR MIR codegen 将 root fallthrough attrs 定位到唯一非 comment root span，覆盖 comment-wrapped single root、comment + Transition child root，以及 dynamic root fragment 的 closing marker 合并；`xtask` 仅将 `ssrFallthroughAttrs.spec.ts` 路由到 public `@vue/compiler-ssr` 并标记 Rust-backed。生成版与 NAPI Vue 3 SSR conformance 仍为 `129/129`，覆盖更新为 `rust-backed 75/75`、`mixed 54/54`、`shim-backed 0/0`；剩余 `ssrComponent` / `ssrElement` / `ssrInjectCssVars` 仍为 mixed。
- [x] Rust compiler-ssr `ssrInjectCssVars.spec.ts` public compile coverage slice：`vuec_vue3_core` SSR MIR codegen 支持 `ssrCssVars` object shorthand / keyed dynamic value rewrite，跨 fragment roots、component roots、`v-if` fragment branches 和 Suspense slot native roots 注入 `_cssVars`，`vuec_vue3_ssr` 暴露 Rust-observed public helper names，generated / NAPI aliases 仅将其 materialize 为 `result.ast.helpers`。生成版与 NAPI Vue 3 SSR conformance 仍为 `129/129`，覆盖更新为 `rust-backed 81/81`、`mixed 48/48`、`shim-backed 0/0`；剩余 `ssrComponent` / `ssrElement` 仍为 mixed。
- [x] Rust compiler-ssr `ssrElement.spec.ts` public compile coverage slice：`vuec_vue3_core` SSR MIR codegen 以 `Vue3SsrAttrs` 表达 element attrs/custom directive/textarea fallback 目标 payload，按官方顺序重建 SSR attrs，覆盖 static key/ref 过滤、dynamic class/style 合并、boolean attrs、dynamic args、object `v-bind` merge、custom directive `_ssrGetDirectiveProps(...)`、directive textContent/innerHTML fallback、textarea static/dynamic value 和 object-bind value fallback；`xtask` 仅将 `ssrElement.spec.ts` 路由到 public `@vue/compiler-ssr` 并标记 Rust-backed，NAPI compiler-ssr package alias 仅补齐 predicate option 归一化。生成版与 NAPI Vue 3 SSR conformance 仍为 `129/129`，覆盖更新为 `rust-backed 113/113`、`mixed 16/16`、`shim-backed 0/0`；剩余 `ssrComponent.spec.ts` 仍为 mixed。
- [x] Rust compiler-ssr `ssrComponent.spec.ts` public compile coverage slice：`vuec_ast` / `vuec_vue3_core` SSR MIR/codegen 支持 component custom directives、component props/events、static/dynamic/default/named slots、conditional slots、`v-for` dynamic slots、slot params、nested slot fallback scope、Transition/KeepAlive fallthrough、VNode fallback helper ordering 和 `_scopeId` propagation；`xtask` 仅将 `ssrComponent.spec.ts` 路由到 public `@vue/compiler-ssr` 并标记 Rust-backed。生成版与 NAPI Vue 3 SSR conformance 均为 `129/129`，覆盖更新为 `rust-backed 129/129`、`mixed 0/0`、`shim-backed 0/0`。

完成判定：

- [x] Vue 3 `compileStyle` 测试通过。
- [x] Vue 3 `compiler-ssr/__tests__` 100% 通过。
- [x] SSR render runtime smoke 通过。

### P6: API、运行时验证、发布

目标：让 Rust compiler 作为实际包可用。

- [x] Rust public API 完成。
- [x] NAPI 包完成。
- [x] WASM 包完成。
- [x] CLI 完成。
- [x] Runtime test harness 完成。
- [x] Benchmark 完成。
- [x] 发布流程完成。

完成判定：

- [x] generated npm alias 官方 compiler 包能跑官方测试。
- [x] browser/WASM smoke 通过。
- [x] CLI 能编译 Vue 2 template、Vue 3 template、Vue 3 SFC、SSR fixture。
- [x] benchmark 报告稳定生成。

## 模块开发计划

### M00: 仓库与工程骨架

开发工作：

- [x] 建立 `Cargo.toml` workspace。
- [x] 建立 `crates/*` 目录。
- [x] 建立 npm workspace：`packages/*`。
- [x] 建立 `xtask`。
- [x] 配置 rustfmt、clippy、deny lints。
- [x] 配置 TypeScript、pnpm、vitest。
- [x] 配置 GitHub Actions / 本地 CI 脚本。
- [x] 配置官方测试缓存目录。

完成判定：

- [x] `cargo check --workspace` 可运行。
- [x] `cargo test --workspace` 可运行。
- [x] `pnpm install` 成功。
- [x] `pnpm test` 可运行。
- [x] CI 能跑 Rust、Node、conformance 三类 job。

### M01: 官方测试同步与 Conformance Harness

开发工作：

- [x] 固定 Vue 2.6 官方仓库 commit。
- [x] 固定 Vue 2.7 官方仓库 commit。
- [x] 固定 Vue 3 官方仓库 commit。
- [x] 实现 `compat/official-revisions.lock` 读写和校验。
- [x] 实现官方测试下载/更新。
- [x] 实现测试 patch/alias 机制。
- [x] 实现测试套件选择：`vue2-compiler`、`vue27-compiler`、`vue27-sfc`、`vue3-core`、`vue3-dom`、`vue3-sfc`、`vue3-ssr`。
- [x] 实现官方 JS compiler 与 Rust compiler 差分模式。
- [x] 实现 failure report：输入、官方输出、Rust 输出、diagnostic diff。
- [x] 实现 skip 审计：禁止新增非官方 skip。
- [x] 实现 API manifest 导出：官方包与 Rust alias 包。
- [x] 实现 option matrix 生成、审计、运行。
- [x] 实现 output contract 差分：schema、exact JS、diagnostic、source map、runtime。

完成判定：

- [x] `cargo xtask verify-official-lock` 通过。
- [x] `cargo xtask sync-official-tests --locked` 能生成 `vendor/vue2_6`、`vendor/vue2_7` 和 `vendor/vue3`。
- [x] `cargo xtask run-conformance --suite vue2-compiler` 能执行并生成 JSON。
- [x] `cargo xtask run-conformance --suite vue27-compiler` 能执行并生成 JSON。
- [x] `cargo xtask run-conformance --suite vue27-sfc` 能执行并生成 JSON。
- [x] `cargo xtask run-conformance --suite vue3-core` 能执行并生成 JSON。
- [x] `cargo xtask run-option-matrix --all` 能执行并生成 JSON。
- [x] `cargo xtask run-output-contract --all` 能执行并生成 JSON。
- [x] 报告包含 pass/fail/skip 数量。
- [x] 任意新增 skip 会让 CI 失败。

### M02: `vuec_source`

开发工作：

- [x] 实现 `SourceFile`、`FileId`、`Span`、`BytePos`。
- [x] 实现 line/column 映射。
- [x] 实现 UTF-8 byte offset 与 UTF-16 column 辅助。
- [x] 实现 SFC block offset 映射。
- [x] 实现 source slice。
- [x] 实现 code frame。
- [x] 实现 source map 原始位置查询。

完成判定：

- [x] 给定 `.vue` 文件内 template block offset，能映射回原文件行列。
- [x] CRLF / LF / mixed newline 测试通过。
- [x] UTF-8 中文、emoji、surrogate pair 相关列号测试通过。
- [x] `cargo test -p vuec_source` 通过。

### M03: `vuec_diagnostics`

开发工作：

- [x] 建立统一 `Diagnostic` 类型。
- [x] 建立 Vue 2 warning/error/tip 兼容层。
- [x] 建立 Vue 3 `ErrorCodes` 兼容层。
- [x] 实现 diagnostic sink。
- [x] 实现 diagnostic snapshot serializer。
- [x] 实现 code frame 输出。
- [x] 实现 severity、related information、suggestion。

完成判定：

- [x] Vue 2 `outputSourceRange` 相关测试可表达。
- [x] Vue 3 parser / transform error code 可表达。
- [x] diagnostic 快照输出稳定。
- [x] `cargo test -p vuec_diagnostics` 通过。

### M04: `vuec_html`

开发工作：

- [x] 实现 HTML tokenizer。
- [x] 支持 comments、CDATA、doctype、raw text、RCDATA。
- [x] 支持 entity decode。
- [x] 支持 namespace：HTML、SVG、MathML。
- [x] 支持 unary tags。
- [x] 支持 can-be-left-open tags。
- [x] 支持 Vue custom delimiters。
- [x] 保留原始属性 span 和 raw text。

完成判定：

- [x] Vue 2 HTML parser 基础官方测试通过。
- [x] Vue 3 baseParse 基础官方测试通过。
- [x] entity decode 与官方一致。
- [x] namespace 与官方一致。
- [x] `cargo test -p vuec_html` 通过。

### M05: `vuec_js` / Oxc 集成

开发工作：

- [x] 封装 Oxc allocator 生命周期。
- [x] 实现 `JsAstStore` 注册表与 id 分配。
- [x] 实现 `JsExprId` / `JsStmtId` / `JsPatternId` / `JsProgramId` 的存取与反查。
- [x] 实现 `JsParseMode::Expression`。
- [x] 实现 `JsParseMode::Statements`。
- [x] 实现 `JsParseMode::Params`。
- [x] 实现 `JsParseMode::ForExpression`。
- [x] 实现 `JsParseMode::ScriptModule`。
- [x] 实现 `JsParseMode::ScriptClassic`。
- [x] 实现 TypeScript parse。
- [x] 实现 parse error 到 Vue diagnostic 的映射。
- [x] 实现 expression prefix / rewrite 辅助。
- [x] 实现 JS AST source span 映射，包含 `TemplateSource.base_offset` 到绝对 `Span` 的回写。
- [x] 实现 `parse_expr` / `parse_stmt` / `parse_pattern` / `parse_program` 的按 id 取回 API。
- [x] 实现 Vue 2 filter 预解析结构，确保 filter chain 不直接降级为裸 JS expression。

完成判定：

- [x] `{{ foo + bar }}` 走 Oxc expression parse。
- [x] `@click="foo(); bar()"` 走 Oxc statements parse。
- [x] `v-for="(item, i) in list"` 走 Oxc-aware parse。
- [x] `<script setup lang="ts">` 走 Oxc TypeScript parse。
- [x] Vue 2 filter 先切 filter，再把 expression 交给 Oxc。
- [x] JS 表达式、语句、参数、`v-for`、script 的结果都能通过 id 重新取回并稳定序列化。
- [x] `cargo test -p vuec_js` 通过。

### M06: `vuec_ast`

开发工作：

- [x] 定义 lossless CST。
- [x] 定义 `AstDocument` / `NodeId` / `NodeSpan` / `LoweringMap` / `PublicProjection`。
- [x] 定义 `NodeSpan::Source` / `Generated` / `Missing` 的构造和序列化规则，并让 `GeneratedReason` / `MissingSpanReason` 进入快照。
- [x] 定义 Vue 2 方言 AST 与 public projection，严格对齐 `Vue2Root`、`Vue2Element`、`Vue2Text`、`Vue2ExpressionText`、`Vue2Comment` 的字段语义，并保证 `children` 只存在于 arena 树边，public projection 负责还原官方可比较结构。
- [x] 定义 Vue 3 方言 AST 与 public projection，严格对齐 `Vue3Root`、`Vue3Element`、`Vue3Prop`、`Vue3Directive`、`Vue3Text`、`Vue3Comment`、`Vue3Interpolation`、`Vue3CompoundExpression`、`Vue3If`、`Vue3IfBranch`、`Vue3For`、`Vue3TextCall`、`Vue3CodegenRef`、`Vue3SsrCodegenRef` 的字段语义，同时保留 `loc`、`innerLoc`、directive 原文、`codegen_node`、`ssr_codegen_node`，并确保 directive `arg` / `exp` / `modifiers` 的 public projection 与官方一致，不能靠 trim、重写或 synthetic fallback 抹平差异。
- [x] 定义共享 HIR，确保不包含 `Call`、`Helper`、runtime helper、patch flag、hoist/cache、SSR push 或 codegen-only 节点，并对齐 `HirRoot`、`HirElement`、`HirComponent`、`HirText`、`HirInterpolation`、`HirIf`、`HirFor`、`HirSlotOutlet`、`HirSlotDecl`、`HirFragment`。
- [x] 定义 `RuntimeHelper`。
- [x] 定义 Vue2Mir / Vue3DomMir / Vue3SsrMir / VaporMir 及其 target-split MIR 专用字段，确保 codegen 只消费对应目标的 MIR。
- [x] 定义 `PublicProjection` 和 lowering 映射导出。
- [x] 实现 AST visitor。
- [x] 实现 AST mutable visitor。
- [x] 实现 AST snapshot serializer。
- [x] 实现 span consistency checker。

完成判定：

- [x] Vue 2 AST 可输出为官方测试可比结构。
- [x] Vue 3 AST 可输出为官方测试可比结构，且 directive `arg` / `exp` / `modifiers` 的 content / loc / staticity 与官方一致。
- [x] 所有 AST/HIR/MIR 节点有 span 或明确标记生成来源，且 `GeneratedReason` / `MissingSpanReason` 能在 snapshot 中区分。
- [x] `LoweringMap` 可追踪 AST -> HIR -> MIR 的映射。
- [x] `Vue2Ast` / `Vue3Ast` 的 public projection 快照稳定且可重复比较，且 Vue 3 directive 投影无额外 trim、重写或 synthetic 混淆。
- [x] `HIR` / `Vue2Mir` / `Vue3DomMir` / `Vue3SsrMir` / `VaporMir` 的 snapshot 和 target split 可重复比较。
- [x] `cargo test -p vuec_ast` 通过。

### M07: `vuec_pass`

开发工作：

- [x] 定义 `TransformPass`。
- [x] 定义 `TransformContext`。
- [x] 支持 enter / exit callback。
- [x] 支持 Vue 2 module hooks。
- [x] 支持 Vue 3 node transforms。
- [x] 支持 Vue 3 directive transforms。
- [x] 支持 pass ordering。
- [x] 支持 `RuntimeHelper` 枚举收集。
- [x] 支持 scope stack。
- [x] 支持 transform diagnostics。
- [x] 支持 `DocumentPass` depth-first walker。

完成判定：

- [x] Vue 2 `preTransformNode` / `transformNode` / `postTransformNode` 可复刻。
- [x] Vue 3 transform exit order 与官方一致。
- [x] directive transform 能替换/扩展默认行为。
- [x] `DocumentPass` 的 enter / exit 顺序和节点遍历顺序稳定可测。
- [x] `cargo test -p vuec_pass` 通过。

### M08: `vuec_codegen`

开发工作：

- [x] 实现 `CodeWriter`。
- [x] 实现 exact whitespace mode。
- [x] 实现 Vue 2 exact emitter。
- [x] 实现 Vue 3 DOM / SSR exact emitter。
- [x] 实现 MIR-first emit pipeline。
- [x] 实现 `RuntimeHelper` import / call 映射。
- [x] 实现 source map builder。
- [x] 实现 SFC source map merge。
- [x] 实现 snapshot-friendly output。

完成判定：

- [x] Vue 2 codegen 字符串可 exact compare。
- [x] Vue 3 DOM / SSR codegen 字符串可 exact compare。
- [x] source map generated position 能回查 original position。
- [x] `cargo test -p vuec_codegen` 通过。

### M09: `vuec_vue2`

开发工作：

- [ ] 实现 Vue 2.6 `parse`。
- [ ] 实现 attrs / props / events / directives parser。
- [ ] 实现 text interpolation parser。
- [ ] 实现 Vue 2 filters。
- [ ] 实现 `v-if` / `v-else-if` / `v-else`。
- [ ] 实现 `v-for`。
- [ ] 实现 `v-on`。
- [ ] 实现 `v-bind`。
- [ ] 实现 `v-model`。
- [ ] 实现 slots / scoped slots。
- [ ] 实现 components / dynamic component。
- [ ] 实现 `v-pre` / `v-once`。
- [ ] 实现 optimizer static marking。
- [x] 建立结构性 `Vue2Ast -> HIR -> Vue2Mir` lowering contract 入口，覆盖 public projection JS id、基础 element / text / interpolation / comment、`v-if` / `v-for` 结构节点、`LoweringMap` 和 span consistency。
- [x] 将 Vue 2 render/staticRenderFns codegen 迁移到 `Vue2Mir` 输入，禁止 codegen 直接依赖 AST 内部状态。
- [x] 实现 render/staticRenderFns codegen。
- [ ] 实现 platform modules：class、style、model。
- [ ] 实现 `compile`。
- [ ] 实现 `compileToFunctions` NAPI 行为。

完成判定：

- [ ] Vue 2.6 parser 官方测试通过。
- [ ] Vue 2.6 optimizer 官方测试通过。
- [ ] Vue 2.6 codegen 官方测试通过。
- [ ] Vue 2.6 compiler options 官方测试通过。
- [x] `cargo xtask run-option-matrix --version-line vue2_6 --package vue-template-compiler` 通过。
- [x] `cargo xtask run-output-contract --version-line vue2_6 --package vue-template-compiler` 通过。
- [x] `Vue2Ast` / `HIR` / `Vue2Mir` 的 projection 与 lowering 快照通过。
- [x] Vue 2.6 compiler 官方测试 100% 通过。

### M09b: `vuec_vue27`

开发工作：

- [x] 实现 Vue 2.7 template compiler dialect，明确复用 Vue 2.6 行为的边界。
- [x] 实现 Vue 2.7 与 Vue 2.6 的 parser / optimizer / codegen 差异层。
- [x] 保持 Vue 2.7 的方言差异只出现在 Vue2Ast lowering 和 SFC 兼容层，不新增独立 IR 分支。
- [x] 实现 Vue 2.7 `vue-template-compiler` 兼容入口。
- [x] 实现 Vue 2.7 `vue/compiler-sfc` 兼容入口。
- [x] 实现 Vue 2.7 SFC `parse` descriptor。
- [x] 实现 Vue 2.7 SFC `compileTemplate`。
- [x] 实现 Vue 2.7 SFC `compileScript`，覆盖 normal script、script setup、TS、binding metadata。
- [x] 实现 Vue 2.7 SFC `compileStyle`，覆盖 scoped、CSS vars、CSS modules、preprocess。
- [x] 实现 Vue 2.7 source map、diagnostic、warning/tip 兼容层。
- [x] 实现 Vue 2.7 与 Vue 3 SFC 行为差异的 allowed-diff 审计。

完成判定：

- [x] `cargo xtask run-conformance --suite vue27-compiler` 100% 通过。
- [x] `cargo xtask run-conformance --suite vue27-sfc` 100% 通过。
- [x] `cargo xtask diff-api --version-line vue2_7 --package vue-template-compiler` 无未批准 diff。
- [x] `cargo xtask diff-api --version-line vue2_7 --package vue --entry vue/compiler-sfc` 无未批准 diff。
- [x] `cargo xtask run-option-matrix --version-line vue2_7 --package vue-template-compiler` 通过。
- [x] `cargo xtask run-option-matrix --version-line vue2_7 --package vue --entry vue/compiler-sfc` 通过。
- [x] `cargo xtask run-output-contract --version-line vue2_7 --package vue-template-compiler` 通过。
- [x] `cargo xtask run-output-contract --version-line vue2_7 --package vue --entry vue/compiler-sfc` 通过。
- [x] `Vue2Ast` / `HIR` / `Vue2Mir` 的 projection 与 lowering 快照通过。
- [x] Vue 2.7 任一输出不能通过引用 Vue 2.6 golden 文件跳过差分；所有复用必须在报告中标记为 `same-as-vue2_6` 并附带官方对比证据。

### M10: `vuec_vue3_core`

开发工作：

- [ ] 实现 `baseParse`。
- [ ] 实现 `TemplateSource` source anchor / `base_offset`。
- [ ] 实现 text / interpolation / comment / element AST，并输出 `Vue3Ast` public projection；projection 必须保留官方 parse AST 的所有可观察字段，包括 `loc`、`innerLoc` 和 directive 原文信息，且 directive `arg` / `exp` / `modifiers` 的 `content`、`loc`、`isStatic` / `constType` 必须与官方一致。
- [ ] 实现 parser options。
- [x] 实现 `transformExpression` Rust-backed projection bridge：Rust 负责 interpolation / element 分支、directive filtering、`v-for` skip、`v-on:arg` exp skip、`v-slot` params、dynamic arg 与 `v-memo` key skip 调度；官方 `transformExpressions.spec.ts` `47/47`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal / transform harness。
- [x] 实现 `processExpression` Rust-backed projection bridge：Rust 负责 identifier prefixing、setup binding rewrite、local / function / arrow scope、object shorthand rewrite、assignment RHS、parser validation、helper metadata 与 constType 分类；官方 `transformExpressions.spec.ts` `47/47`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal / transform harness。
- [ ] 实现 `transformIf`。
- [ ] 实现 `transformFor`。
- [x] 实现 `transformOnce` Rust-backed projection bridge：Rust 负责 `v-once` eligibility、seen / nested / SSR skip、helper、enter `inVOnce` 与 exit cache intent；官方 `vOnce.spec.ts` `8/8`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal / cache materialization。
- [x] 实现 `transformMemo` Rust-backed projection bridge：Rust 负责 `v-memo` eligibility、seen / SSR skip、组件与普通元素 block conversion、`WITH_MEMO` helper、memo expression 与 cache index；官方 `vMemo.spec.ts` `7/7`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal / materialization。
- [ ] 实现 `transformElement`。
- [x] 实现 `transformSlotOutlet` / `processSlotOutlet` Rust-backed projection bridge：Rust 负责 slot outlet 识别、`name` / `:name` / same-name shorthand、非 name props 筛选与 camelize、fallback / scopeId / slotted 参数形状；官方 `transformSlotOutlet.spec.ts` `14/14`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal、`buildProps` 和 codegen materialization。
- [x] 实现 `trackSlotScopes` / `trackVForSlotScopes` Rust-backed projection bridge：Rust 负责 `v-slot` props/local scope 追踪、`v-for` slot alias locals、`context.scopes.vSlot` / `vFor` 驱动的 dynamic slot 判定；官方 `vSlot.spec.ts` `31/31`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal / slot object materialization。
- [x] 实现 `transformText` Rust-backed projection bridge：Rust 负责 text merge、`TEXT_CALL` 包装、compat `<template>` 与 `context.ssr` patch flag 决策；官方 `transformText.spec.ts` `9/9`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖未完全 Rust-backed 的 transform harness / expression / codegen。
- [x] 实现 `transformVBindShorthand` Rust-backed projection bridge：Rust 负责 same-name shorthand `:foo` / `.foo` 的 expression 合成、browser empty-expression 分支、非法动态 argument 诊断和首字符合法性过滤；官方 `vBind.spec.ts` `19/19`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal / element props materialization。
- [x] 实现 `transformBind` / `v-bind` Rust-backed projection bridge：Rust 负责 empty expression 诊断、dynamic arg `|| ""` guard、`.camel` helper / static camelize、`.prop` / `.attr` prefix、SSR 跳过 prefix 以及 undefined value 分支；官方 `vBind.spec.ts` `19/19`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime traversal / element props materialization。
- [x] 实现 `transformOn` / `v-on` Rust-backed projection bridge：Rust 负责 event key、handler classification、prefixIdentifiers、cacheHandlers、setup constness 与 dynamic handler key metadata；官方 `vOn.spec.ts` `33/33`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime transform harness / materialization。
- [x] 实现 `transformModel` / `v-model` Rust-backed projection bridge：Rust 负责 expression diagnostics、member-expression validation、prop/update key、modifiers、cache eligibility、scope/props/const diagnostics 与 hydration event metadata；官方 `vModel.spec.ts` `21/21`，但文件级覆盖仍按 `mixed` 统计，因为同文件仍依赖 alias-runtime transform harness / materialization。
- [x] 实现 `RuntimeHelper` / components / directives collection：Rust `Vue3Dialect::transform` 现在填充 `Vue3Root.helpers`、`components`、`directives`，并与 `TransformContext.helpers` 保持同步；覆盖组件 asset、内置组件 helper、动态组件 helper、`v-show` helper、自定义 runtime directive asset，并通过 public projection 测试验证。
- [x] 迁移当前 Vue 3 exact render emitter 的 root codegen state 消费：`Vue3Dialect::generate` 现在优先使用 `Vue3Root.helpers`、`components`、`directives` 生成 helper imports/destructure 与 asset declarations；直接 public `generate(ast, options)` 未经过 transform 时保留 raw AST fallback scan；内置组件、动态组件和当前 runtime directive wrapper / `NEED_PATCH` 子集已从 root state 驱动。
- [ ] 实现 `Vue3Ast -> HIR -> Vue3DomMir` lowering pipeline，禁止 DOM / SSR codegen 直接读取 parser 私有状态；当前已建立基础节点、slot outlet、directive-backed `v-for` / `v-if`、相邻 `v-if` / `v-else-if` / `v-else` 分支链、保守静态 hoist wrapper、顶层 `v-once` cache wrapper、非 `v-for` `v-memo` wrapper、ordered props segment / object spread / dynamic arg、component asset / dynamic component / built-in helper tag，以及 stable/dynamic/forwarded component slots (`MirChildren::Slots`) 的结构性 lowering contract；runtime directive 静态 / 动态参数、native `v-show`、slot 动态名、slot `v-if`、slot `v-for`、forwarded slot flag、`v-for` memo cache target 和 cacheHandlers event cache slot 已进入 target payload，standalone DOM MIR codegen 已对当前 JS-store 表达式子集执行 prefix identifier rewrite 并保留 loop / slot / handler locals，但完整 patch flag parity 和 exact codegen 仍未完成。
- [ ] 在 `Vue3DomMir` 中实现 patch flags、hoist static、cache handlers 的目标化投影；当前已覆盖结构性 lowering 子集中的 class/style/text/props/hydration/full-props/vnode-hook/runtime-directive NEED_PATCH/dynamic-slots patch flag、`dynamic_props` 名称、object `v-bind` / `v-on` 与 dynamic arg 的 `FULL_PROPS` 投影、保守静态 subtree `Hoisted` wrapper、render-external `_hoisted_n` declaration emission、顶层 `v-once` `Cache` wrapper、非 `v-for` `Memo` wrapper、`v-for` memo cache target payload 和 cacheHandlers event cache slot；完整官方 patch flag / cacheStatic parity 仍未完成。
- [ ] 实现 codegen；当前 exact render emitter 已消费 AST root helper/components/directives state，并覆盖当前子集的内置/动态组件与 runtime directive wrapper；另已建立 `generate_vue3_dom_mir` standalone target codegen 入口，消费 `Vue3DomMir` + `JsAstStore` 输出当前结构性子集，且 `Vue3VNodeCall` 已携带结构化 tag、attrs / dynamic bindings / events / native `v-show` / runtime directives static/dynamic args / ordered merge props payload 和 stable/dynamic/forwarded slots payload，可从 MIR 输出 `_resolveComponent`、`_resolveDynamicComponent`、内置组件 helper、`_mergeProps`、`_normalizeProps`、`_guardReactiveProps`、`_toHandlers`、`_toHandlerKey`、cacheHandlers `_cache[n]` event wrappers、`_withDirectives` / `_vShow` wrappers、`_withCtx` stable / forwarded slot object、`_createSlots` dynamic slot object、`_renderList` slot loops、保守静态 `_hoisted_n` declarations、非 `v-for` `_withMemo` wrappers 和 `v-for` memo cache path，并在 prefix/module 模式下对当前 JS-store 表达式执行 `_ctx` / bindingMetadata rewrite，同时保护 `v-for` alias、slot params、`$event` 和 arrow handler params；standalone DOM MIR codegen 也会从实际渲染 body 补齐 rewrite 引入的 `_unref` / `_isRef` 等 helper imports。尚未迁移为完整消费 `Vue3DomMir` 的 exact target codegen，也尚未完成官方 compiler-core internal codegen parity。

完成判定：

- [x] `compiler-core` parse 测试通过。
- [x] `compiler-core` transform 测试通过。
- [x] `compiler-core` codegen 测试通过。
- [x] `Vue3Ast` public projection 快照通过，且 `loc` / `innerLoc` / directive arg / exp / modifier 内容与官方一致。
- [x] `HIR` lowering 快照通过，并能稳定映射到 `Vue3DomMir`。
- [x] `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-core` 通过。
- [x] `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-core` 通过。
- [x] `compiler-core/__tests__` 100% 通过。

### M11: `vuec_vue3_dom`

开发工作：

- [ ] 实现 DOM parser options。
- [ ] 实现 DOM entity decode。
- [x] 实现 DOM `decodeHtmlBrowser` Rust-backed projection：Rust 负责官方 direct browser decode helper 的 text/attribute entity 解码；官方 `decoderHtmlBrowser.spec.ts` `1/1`，DOM conformance coverage 计入 Rust-backed。完整 DOM entity table/completeness 仍由上一项继续追踪。
- [x] 实现 DOM `v-html` Rust-backed public compile 与 internal `transformVHtml` projection（`vHtml.spec.ts` `3/3`，DOM conformance coverage 计入 Rust-backed）。
- [x] 实现 DOM `v-text` Rust-backed public compile 与 internal `transformVText` projection（`vText.spec.ts` `3/3`，DOM conformance coverage 计入 Rust-backed）。
- [x] 实现 DOM `v-model` input / textarea / select / checkbox / radio / dynamic type internal `transformModel` Rust-backed projection：Rust 负责 native runtime helper selection、DOM diagnostics、duplicate dynamic `:value` checks、native `modelValue` prop filtering 与 helper registration order；官方 `vModel.spec.ts` `18/18`，DOM conformance coverage 计入 Rust-backed。
- [x] 实现 DOM `v-on` modifiers internal `transformOn` Rust-backed projection（`vOn.spec.ts` `12/12`，DOM conformance coverage 计入 Rust-backed）。
- [ ] 实现 DOM `v-bind` modifiers。
- [x] 实现结构化 DOM MIR native `v-show` payload/codegen（standalone MIR 路径）与 DOM internal `transformShow` Rust-backed projection（`vShow.spec.ts` `2/2`，DOM conformance coverage 计入 Rust-backed）；完整官方 DOM transform/codegen parity 仍随 DOM 总目标推进。
- [x] 实现 DOM `Transition` internal `transformTransition` Rust-backed projection：Rust 负责 child filtering、invalid-child diagnostics、transformed IF/FOR child-shape checks 与 native `v-show` persisted prop injection；官方 `Transition.spec.ts` `14/14`，DOM conformance coverage 计入 Rust-backed。`TransitionGroup` 在官方 compiler-dom 中没有对应 node transform，内置组件识别沿用 parserOptions / helper projection。
- [x] 实现 DOM HTML nesting validation Rust-backed projection：Rust 负责官方 `isValidHTMLNesting` 规则表与 `validateHtmlNesting` warning projection；官方 `validateHtmlNesting.spec.ts` `17/17`，DOM conformance coverage 计入 Rust-backed。
- [x] 实现 DOM `ignoreSideEffectTags` Rust-backed projection：Rust 负责 native `<script>` / `<style>` remove operation 与 DOM error `64` loc projection；官方 `ignoreSideEffectTags.spec.ts` `2/2`，DOM conformance coverage 计入 Rust-backed。
- [x] 实现 DOM `stringifyStatic` transform-hoist Rust-backed projection：Rust 负责官方 cached public AST children 的 thresholding、bailouts、static HTML serialization、scope-id injection、SVG/MathML namespace handling 与 `CREATE_STATIC` AST replacement decisions；官方 `stringifyStatic.spec.ts` `25/25`，DOM conformance coverage 计入 Rust-backed。`xtask/src/compat.rs` 仅做 prepared import 与 Rust projection materialization。
- [ ] 实现 asset URL transform。
- [ ] 实现 custom element 选项。
- [ ] 确保 DOM transform 继续沿用 `Vue3Ast` -> HIR -> Vue3DomMir 的目标链路。

完成判定：

- [x] `compiler-dom` parser options 测试通过。
- [x] `compiler-dom` transform 测试通过。
- [x] `compiler-dom` codegen 测试通过。
- [x] `Vue3DomMir` 输出快照通过。
- [x] `Vue3Ast` / `HIR` / `Vue3DomMir` 的 projection 与 lowering 快照通过。
- [x] `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-dom` 通过。
- [x] `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-dom` 通过。
- [x] `compiler-dom/__tests__` 100% 通过。
- [x] Vue 3 DOM generated/NAPI conformance 覆盖已全部计入 Rust-backed：`133/133` Rust-backed，`mixed 0/0`，`shim-backed 0/0`。

### M12: `vuec_vue3_ssr`

开发工作：

- [ ] 实现 SSR transform preset。
- [ ] 实现 SSR text / interpolation emit。
- [x] 实现 SSR element attrs emit：Rust SSR MIR/codegen 已覆盖 `ssrElement.spec.ts` public compile attrs 行为，包括 attrs 顺序重建、class/style/boolean/dynamic arg/object bind/custom directive/textarea fallback。
- [x] 实现 SSR component emit：Rust SSR MIR/codegen 已覆盖 `ssrComponent.spec.ts` public compile component 行为，包括 component props/events、dynamic component target、custom directive props、KeepAlive/Transition fallthrough 和 component slot VNode fallback。
- [x] 实现 SSR slot emit：Rust SSR MIR/codegen 已覆盖 `ssrComponent.spec.ts` / `ssrSlotOutlet.spec.ts` public compile slot 行为，包括 default/named/static/dynamic/conditional/v-for slots、slot params、slot outlet fallback、nested slot scope 和 `_scopeId` propagation。
- [ ] 实现 SSR `v-if`。
- [ ] 实现 SSR `v-for`。
- [ ] 实现 SSR `v-model`。
- [ ] 实现 SSR `v-show`。
- [ ] 实现 SSR teleport。
- [ ] 实现 SSR suspense。
- [ ] 实现 SSR source map。
- [x] 建立结构性 `Vue3Ast` -> HIR -> `Vue3SsrMir` 独立 lowering 入口，覆盖基础 Root/Element/Text/Interpolation/Comment、component render、slot outlet、dynamic attrs marker、directive-backed `v-for` / `v-if`、相邻 `v-if` / `v-else-if` / `v-else` 分支链、`LoweringMap` 和 `JsAstStore` 登记；这只是 contract foundation，尚未完成 exact SSR codegen/control-flow parity。

完成判定：

- [x] `compiler-ssr` transform 测试通过。
- [x] `compiler-ssr` codegen 测试通过。
- [x] `Vue3SsrMir` 输出快照通过。
- [x] `Vue3Ast` / `HIR` / `Vue3SsrMir` 的 projection 与 lowering 快照通过。
- [x] `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-ssr` 通过。
- [x] `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-ssr` 通过。
- [x] SSR runtime smoke 通过。
- [x] `compiler-ssr/__tests__` 100% 通过。

### M13: `vuec_sfc`

开发工作：

- [ ] 实现 `parse` descriptor。
- [x] 实现 Vue 3 public `parse()` official `parse.spec.ts` Rust-backed coverage：official SFC parse spec 通过 public `@vue/compiler-sfc.parse()` alias 路由到 Rust SFC parser / bridge / NAPI projection；报告为 `rust-backed 28/28`，其余 SFC spec 仍为 `mixed 433/433`。
- [x] 实现 Vue 3 public `parse()` descriptor projection：官方字段名、public attrs、block loc、默认 source map、`cssVars`、`slotted` 和 Rust DOM template AST 可通过 generated alias / NAPI 公开入口返回；SFC conformance 暴露的多行 SSR CSS vars 格式边界已在 Vue 3 core 修复；完整 descriptor parser parity 仍不因此声明完成。
- [x] 实现 Vue 3 public `parse()` `descriptor.shouldForceReload` API/HMR adapter：generated alias 与 NAPI alias 在 JS package 边界提供官方 HMR reload 函数，使用 Rust template AST 识别 TS/TSX `<script setup>` import 是否新增模板使用；这是非 JSON package API/HMR adapter，不计入 Rust parser/compiler 语义完成度。
- [x] 实现 Vue 3 duplicate block / script-src descriptor diagnostics：Rust parse result 现在覆盖 duplicate `template` / `script` / `script setup`、空 script 忽略、`script setup src`、`script src` + `script setup` 和缺失 template/script 的官方 public error shape；完整 descriptor parser parity 仍不因此声明完成，`shouldForceReload` 由上方 API/HMR adapter 单独覆盖。
- [x] 实现 Vue 3 parse options / padding / dedent：Rust parse result 现在覆盖 `pad`、`ignoreEmpty`、pug/jade template descriptor dedent，以及 padded descriptor source-map line offset / dedent column offset；完整 descriptor parser parity 仍不因此声明完成，`shouldForceReload` 由上方 API/HMR adapter 单独覆盖。
- [x] 实现 Vue 3 descriptor `src` presence / functional template diagnostics：Rust parse result 现在保留 boolean / empty-string `src` blocks、按官方 map / AST / script setup conflict 语义区分 `src` presence 与 non-empty `src` value，并为 accepted first `<template functional>` 输出属性级 loc 诊断；完整 descriptor parser parity 仍不因此声明完成，`shouldForceReload` 由上方 API/HMR adapter 单独覆盖。
- [x] 实现 Vue 3 descriptor block attribute decode / duplicate attr diagnostics：Rust parse result 现在按官方解码 SFC block attrs、投影解码后的 `lang` / `src` / `module` / `generic`，并为重复 attr 输出 `Duplicate attribute.` zero-length loc；完整 descriptor parser parity 仍不因此声明完成，`shouldForceReload` 由上方 API/HMR adapter 单独覆盖。
- [x] 实现 Vue 3 descriptor scan syntax diagnostics：Rust parse result 现在覆盖 bogus question tag、未闭合 raw/custom block、template nested mismatch 和 EOF stack 的官方 public parse errors，并保持官方恢复后的空块 / template content；完整 descriptor parser parity 仍不因此声明完成，`shouldForceReload` 由上方 API/HMR adapter 单独覆盖。
- [x] 实现 Vue 3 malformed descriptor scan recovery：Rust parse result 现在覆盖 uppercase top-level block recovery、invalid end tag、CDATA XML-context error、malformed attr diagnostics 和 template child duplicate attr diagnostics；完整 descriptor parser parity 仍不因此声明完成，`shouldForceReload` 由上方 API/HMR adapter 单独覆盖。
- [ ] 支持 template/script/script setup/style/custom blocks。
- [ ] 支持 block attrs、lang、src、scoped、module。
- [x] 实现 duplicate block diagnostics：Vue 3 top-level `template` / `script` / `script setup` 重复块已由 Rust parser 诊断并保持官方 first-block selection；style/custom 多块按官方允许。
- [ ] 实现 SFC source map。
- [x] 实现 SFC `compileStyle` style-block source map 可回溯到原 `.vue` 文件：合并多个 style block 时保留单一 `.vue` `sourcesContent`、原始 block offset、line-level original position，并跳过空 style block 的 generated mapping；这不是全量 SFC source-map parity。
- [x] 实现 Vue 3 SFC `compileScript` source-map foundation：Rust `compile_script` 现在按默认开启的 `source_map` 返回 script map，支持 `sourceMap` / `source_map` 禁用；normal script / script setup 的可匹配源码行映射回原 `.vue`，inline template DOM/SSR compiler map 合并到 script result map；bridge、NAPI、WASM、CLI 入口透传该选项。这不是完整官方 source-map parity，template 表达式 token 完整覆盖、previous map 合并和完整 result-shape parity 仍需后续推进。
- [x] 实现 Vue 3 SFC `compileScript` inline-template expression token source map：Rust core 以原始模板表达式 span 生成 token 映射，并在 inline setup binding rewrite 后继续定位 generated token；SFC inline render map 合并保留 token names，template base offset 改为 content start。这不是完整官方 source-map parity，previous map 合并、所有 template token 边界和完整 result-shape parity 仍需后续推进。
- [x] 实现 `rewriteDefault`：Vue 3 public `@vue/compiler-sfc.rewriteDefault` 现在通过 Rust SFC 实现、generated alias `sfc.rewriteDefault` 和 NAPI `rewriteDefaultVue3` 暴露；`rewriteDefaultAST` 仍保持 JavaScript/static AST helper。
- [x] 实现 Vue 3 `compileScript` normal `<script>` default export setup-merge 子片段：当 `<script setup>` 与 normal `<script>` 同时存在时，Rust SFC 负责 default export AST 分析、`__default__` rewrite、JS/TS wrapper 合并和 AST 顺序 return binding；完整 normal script analysis / script setup merge / macros / TS type resolve 仍按下方未完成项推进。
- [x] 实现 Vue 3 `compileScript` runtime macro 基础子片段：Rust SFC 负责基础运行时 `defineProps` / `defineEmits` / `defineExpose` 代码生成、宏调用移除/改写、基础 runtime options、setup 参数和返回 binding，并恢复 setup-only 静态 literal `const` 提升；完整宏诊断、TS 类型推导、withDefaults、defineModel/defineOptions、reactive destructure 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` TypeScript macro inference 基础子片段：Rust SFC 负责基础 `defineProps<T>()` / `withDefaults(defineProps<T>(), defaults)` / `defineEmits<T>()` 推断，覆盖 inline type literal、local/normal-script interface/type references、静态/动态 defaults、Vue 3 prod prop erasure 和 `_mergeDefaults` 生成；完整宏分析、完整 TS type resolve、defineModel/defineOptions、reactive destructure 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `withDefaults()` macro diagnostics 子片段：Rust SFC 负责 `withDefaults` compiler macro import/public binding 清理、保留 type-based static/dynamic defaults runtime 推断，并补齐官方 first-argument、runtime `defineProps` 和 missing defaults diagnostics；完整 reactive destructure warning、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineProps()` / `defineEmits()` duplicate diagnostics 子片段：Rust SFC 负责 `defineProps` / `defineEmits` 重复调用追踪和官方 duplicate-call diagnostic，`withDefaults(defineProps<T>(), defaults)` 内部的 `defineProps` 也计入重复检测；完整 `defineProps` / `defineEmits` 宏分析、reactive destructure、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineProps()` destructure diagnostics 子片段：Rust SFC 负责 `defineProps()` 解构的动态 computed key、nested patterns、默认值引用非 literal setup locals 诊断，并保留静态 computed literal key 与静态 setup const 引用的官方允许边界；reactive destructure transform、watch/toRef misuse diagnostics、default value type mismatch、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineProps()` destructure usage diagnostics 子片段：Rust SFC 负责解构 prop 写入 readonly 诊断，以及解构 prop 直接传给 `watch()` / `toRef()` 的官方误用诊断；Vue import alias 同时覆盖 normal `<script>` 与 `<script setup>`，并尊重局部作用域遮蔽。reactive destructure transform、default value type mismatch、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineProps()` destructure default type diagnostics 子片段：Rust SFC 负责 type-based `defineProps<T>()` 解构默认值的官方 best-effort 类型不匹配诊断，覆盖 String / Number / Boolean / Object / Array / Function 静态默认值，并保留 `null` / unknown runtime type 跳过边界；reactive destructure transform、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineProps()` destructure setup-body rewrite 子片段：Rust SFC 负责 setup body 中解构 prop 引用到 `__props` 的作用域感知重写，覆盖别名、非标识符 prop key、对象 shorthand、函数参数遮蔽和循环变量遮蔽；rest proxy、inline template rewrite、默认值 runtime 合并、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineProps()` destructure defaults runtime merge 子片段：Rust SFC 负责 runtime declaration 的 `_mergeDefaults(...)` 生成，以及 type-based declaration 生成 runtime prop option `default:`；覆盖 literal 默认值、object/array factory wrap、runtime 函数/identifier `__skip_*` 标记、Function prop 默认值不 wrap 和 production prop option 边界。rest proxy、inline template rewrite、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineProps()` destructure rest proxy 子片段：Rust SFC 负责对象 rest 解构到 `_createPropsRestProxy(__props, [...])` 的源码改写、helper import、已解构 public key 排除列表和 rest binding metadata；inline template rewrite、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `propsDestructure` option-boundary 子片段：Rust SFC 支持 `propsDestructure: false | true | "error"`，disabled 模式保留 defineProps 解构 local 语义并仅把 call-site 改写为 `__props`，error 模式输出官方禁止解构诊断；bridge、NAPI、WASM 和 CLI 均透传该选项。完整 reactive destructure parity、inline template destructure rewrite、完整 TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineOptions` 基础子片段：Rust SFC 负责 top-level `defineOptions(...)` 移除与 runtime options 合并、TS assertion unwrap、compiler macro import cleanup、基础 public binding 清理和官方错误投影；完整宏分析、defineModel、reactive destructure 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineExpose()` 基础 macro/diagnostic 子片段：Rust SFC 负责 top-level `defineExpose(...)` 到 `__expose(...)` 的调用改写、无显式调用时的默认空 `__expose()` 生成边界、compiler macro import cleanup 和 duplicate-call diagnostics；完整 `compileScript` result shape、reactive destructure 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineSlots()` 基础子片段：Rust SFC 负责 bound `defineSlots()` 到 `_useSlots()` 的改写、unbound 调用擦除、compiler macro import cleanup 和 duplicate/runtime-argument diagnostics；完整宏分析、完整 TS type resolve、reactive destructure 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineModel` runtime macro 基础子片段：Rust SFC 负责基础 model props / modifiers props、`update:*` emits、`_useModel(__props, ...)` 调用改写、`_mergeModels` 合并和 duplicate model diagnostics；完整 defineModel parity、TS runtime model type inference、production type erasure 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineModel<T>()` TypeScript runtime type 基础子片段：Rust SFC 负责基础类型参数到 model runtime prop options 的推断、`skipCheck` 与 production erasure 规则；完整 defineModel parity、完整 imported/external TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `defineModel` get/set transformer option splitting 子片段：Rust SFC 负责静态 object literal options 的 prop/runtime transformer 拆分，typed model runtime merge，以及 spread/computed options 的官方不拆分边界；完整 defineModel parity、完整 imported/external TS type resolve 和完整 binding metadata 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` 相对 dynamic import type 子片段：Rust SFC 负责简单相对 `import('./x').Name` 类型解析，覆盖 `defineProps` / `defineEmits` / `defineModel` 的直接 dynamic import type 参数，以及外部类型成员内嵌 dynamic import type 的 nested deps；bare package、tsconfig/global、default expression/class/function exports 等完整 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` 相对 `.vue` 外部类型 / arbitrary extension sidecar 子片段：Rust SFC 负责解析相对 `.vue` descriptor 中 normal/setup script 的 TS/TSX 类型源码，覆盖 `defineProps` / `defineEmits` / `defineModel` 与 chained `.ts` facade re-export，并让 `foo.d.vue.ts` / `bar.d.css.ts` 这类 sidecar 优先于真实任意扩展名文件；bare package、tsconfig/global、default expression/class/function exports 等完整 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` 相对 namespace / qualified type 子片段：Rust SFC 负责把 exported namespace types 投影为 dotted keys，覆盖 namespace imports、nested namespace keys 和 `import('./x').A.B` qualified dynamic import type，并让这些相对类型驱动 `defineProps` / `defineEmits` / `defineModel` 与 `deps`；bare package、tsconfig/global、default expression/class/function exports 等完整 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` TS enum runtime type 子片段：Rust SFC 负责注册 local/exported enum，并按官方 `inferEnumType` 边界把 string/numeric/mixed/initializer-free enum 分别投影为 `String`、`Number`、`[String, Number]` 和 `Number`，覆盖 `defineProps` / `defineModel` 中的本地、相对外部、re-export、namespace 与 qualified enum 引用及对应 `deps`；bare package、tsconfig/global、default expression/class/function exports 等完整 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` bare package type 子片段：Rust SFC 负责 ancestor `node_modules` 的受限 package root/subpath type 解析，覆盖 `types` / `typings`、简单 package `exports` type target、简单 wildcard exports、JS export target 声明 fallback、`@types` fallback、package re-export graph、namespace import 和 `import('pkg').Name` 的 `deps`；tsconfig/path/global types、完整 NodeNext conditional exports、default expression/class/function exports 等完整 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` tsconfig path/project-reference type 子片段：Rust SFC 负责从 ancestor `tsconfig.json` 图解析受限 alias 类型导入，覆盖 `compilerOptions.paths`、简单 `baseUrl`、本地 `extends` string/array、project `references` 到 config 文件或文件夹、循环 reference guard、`${configDir}` path target、aliased `.vue` 类型抽取和 bridge normalized `deps`；TypeScript checker parity、完整 include/exclude 匹配、受限文件/package 切片之外的完整 tsconfig project-service 全局类型行为、任意 conditional/mapped type 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` tsconfig JSONC parsing 子片段：Rust SFC 负责 tsconfig alias/project-reference 图读取时的受限 JSONC 解析，覆盖 line/block comments、object/array trailing commas，以及字符串字面量中注释形态和逗号内容的保留；完整 include/exclude 匹配、受限文件/package 切片之外的完整 tsconfig project-service 全局类型行为、TypeScript checker parity 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` package-based tsconfig `extends` 子片段：Rust SFC 负责从当前 tsconfig 目录向上查找 ancestor `node_modules` 并解析非相对 `extends`，覆盖 scoped/unscoped package root、严格 JSON `package.json` `tsconfig` 字段、package root `tsconfig.json` / `index.json`、以及 package subpath config 到 `.json` / `tsconfig.json` / `index.json` 的受限入口；完整 TypeScript checker parity、完整 include/exclude 匹配、受限文件/package 切片之外的完整 tsconfig project-service 全局类型行为、完整 NodeNext conditional exports for tsconfig packages 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` bounded tsconfig automatic global `.d.ts` discovery 子片段：Rust SFC 负责从当前 `.vue` 文件 ancestor tsconfig 图发现全局声明文件，覆盖 local/package `extends`、project `references`、`files` exact `.d.ts` 和受支持 `.d.ts` / `.ts` / directory-like `include` pattern 的递归 `.d.ts` 收集；发现的全局类型可驱动 `defineProps` / `defineEmits` / `defineModel`，并通过 bridge normalized `deps` 暴露实际使用的 global/leaf 文件。完整 TypeScript project service、完整 include/exclude 语义、动态 TypeScript 版本感知 package resolution、任意 type package layout、unsupported/cyclic diagnostics 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` bounded tsconfig `types` / `typeRoots` global declaration package discovery 子片段：Rust SFC 负责从 ancestor tsconfig 图发现全局声明包，覆盖配置的 `compilerOptions.types`、`compilerOptions.typeRoots`、默认 ancestor `node_modules/@types`、`types: []` 显式空集、scoped package 目录形态，以及 package `types` / `typings` / 简单 `exports.types` / `index.d.ts` 入口；发现的全局声明包可驱动 `defineProps` / `defineEmits` / `defineModel`，并通过 bridge normalized `deps` 暴露实际使用的 declaration 文件。完整 TypeScript project service、完整 include/exclude 语义、动态 TypeScript 版本感知 package resolution、任意 type package layout、完整 NodeNext conditional exports 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` bounded package `typesVersions` declaration-entry 子片段：Rust SFC 负责在共享 package declaration-entry resolver 中应用受限 `typesVersions` 声明入口重定向，覆盖 bare package root/subpath、`@types` fallback package、显式 global re-export 中的相对 package directory，以及 tsconfig `types` / `typeRoots` global declaration package；支持 root `types` / `typings` remap、subpath remap、Node semver selector、selector object order、`"*"` fallback 和单通配 path capture。动态 TypeScript 版本发现、TypeScript project service、任意 package layout、完整 NodeNext conditional exports 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` package `typesVersions` Node semver selector/order 子片段：Rust SFC 负责用 `nodejs-semver` 解析 npm/node 风格 selector，并按 `package.json.typesVersions` 字段顺序选择第一个匹配锁定 TS 5.0 基线的 mapping；覆盖 comparator sets、`||` alternatives、hyphen ranges、`x` / `*` ranges、tilde ranges、caret ranges、非匹配 future-version branch 和 ordered fallback。动态 TypeScript 版本发现、TypeScript project service、任意 package layout、完整 NodeNext conditional exports 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` package `typesVersions` project-local TypeScript version discovery 子片段：Rust SFC 负责从当前 SFC filename 的 ancestor `node_modules/typescript/package.json` 发现项目本地 TypeScript 版本，并把该版本贯穿共享 package declaration-entry resolver 的 `typesVersions` selector matching；缺失或非法版本回退 TS 5.0。TypeScript project service、任意 package layout、完整 NodeNext conditional exports 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` explicit `globalTypeFiles` type 子片段：Rust SFC 负责显式 global type files 的 ambient / `declare global` 类型解析，覆盖 `defineProps` / `defineEmits` / `defineModel` 的全局类型推断和 `deps`；global 文件内部 static imports 可参与解析但不暴露 import 名称为全局类型。动态 TypeScript 版本感知 package resolution、任意 type package layout、default expression/class/function exports 等完整 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` explicit `globalTypeFiles` global re-export graph 子片段：Rust SFC 负责 `declare global { ... }` 内显式 `export type { T } from './x'`、`export { T } from './x'`、`export * from './x'` 的全局类型名投影和 `deps` 传播，并支持这类相对目录重导出解析 package `types` / `typings` 入口；TypeScript checker-backed graph diagnostics、动态 TypeScript 版本感知 package resolution、任意 type package layout、完整 NodeNext package resolution 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` class declaration type 子片段：Rust SFC 负责把本地、外部 named/default、re-exported 和显式 global `declare class` 类型按官方 `Object` runtime type 投影，覆盖 `defineProps` / `defineModel` 的 class 引用及 default class `deps`；class member prop extraction、default expression/function exports、declared props factory / bounded `ReturnType<T>` runtime 投影之外的任意声明函数类型解析等完整 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `ExtractPropTypes` / declared props factory `ReturnType` props-options 子片段：Rust SFC 负责记录 declared runtime props option object 和 declared props factory return options，覆盖 `ExtractPropTypes<typeof props>`、`import('vue').ExtractPropTypes<ReturnType<typeof props>>`、`ExtractPropTypes<ReturnType<typeof uploadProps>>`、`Partial` / `Required` requiredness 调整、外部/全局类型路径和 `deps`；checker-backed 任意函数返回类型求值、完整 default-backed requiredness、conditional/mapped type 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` 静态 runtime props object `ExtractPropTypes<typeof ...>` props-options 子片段：Rust SFC 负责记录静态可分析的 runtime props option object 值，覆盖 `export const props = { ... }`、`const props = { ... }; export { props }`、`const props = { ... }; export { props as default }` 和 direct static `export default { ... }`，支持 constructor、constructor array、`{ type, required, default, validator }` 和 `Object as PropType<User>` 这类 TS assertion，并通过 named/default imports 驱动 `ExtractPropTypes<typeof importedProps>` 与 bridge `deps`；top-level spread/computed key、任意 runtime expression、non-props default expression、完整 default-backed requiredness 和任意 value-space lookup 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` utility/generic props type 子片段：Rust SFC 负责 `Readonly<T>`、`Pick<T, K>`、`Omit<T, K>`、string literal key aliases、resolvable `keyof` 和简单 generic type alias 参数绑定，覆盖本地/外部 generic props alias 的 `defineProps` runtime options 与 `deps`；任意 mapped/conditional type 求值、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `Record<K, V>` props type 子片段：Rust SFC 负责有限静态 key 集的 `Record` props 展开，复用 ordered string-key resolver，覆盖 template-literal keys、string manipulation helpers、静态 `Extract` / `Exclude`、`Partial<Record<...>>` 和外部 generic alias `deps`；宽泛 index signature、任意 conditional/mapped type 求值、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` indexed access runtime type 子片段：Rust SFC 负责受支持的 `T[K]` runtime type 投影，当 object side 可解析为 props-like members 且 key side 可解析为有限 ordered key set 时，覆盖直接 indexed access、union key、function member、简单 generic scalar alias；当 object side 可解析为数组/元组 element runtime declaration 且 key side 为 `number` 时，覆盖 `A[number]`、`Array<T>[number]`、tuple / named tuple `T[number]`，并覆盖外部 `Props` / `ModelValue` alias `deps`；宽泛 index signature、完整 parameter tuple utility 语义、任意 conditional/mapped type 求值、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` indexed access method-signature runtime parity 子片段：Rust SFC 负责官方直接 method prop 与 indexed access 选中 method signature 的 runtime 边界；直接 `defineProps<{ method(): void }>()` 仍输出 `Function`，但 `Base['method']` 投影为 internal `Unknown` 并输出 no-check `type: null`，`Base['method'] | Base['run']` 输出 `Function` + `skipCheck: true`，`Base['method'] | Base['value']` 输出 `type: null`，并覆盖外部 `Props` / `ModelValue` alias `deps`；宽泛 index signature、checker-backed indexed access 求值、任意 conditional/mapped type 求值和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` parameter tuple utility indexed-access runtime 子片段：Rust SFC 负责受限 `Parameters<T>[number|N]` 与 `ConstructorParameters<T>[number|N]` 的 runtime constructor 投影，覆盖 inline function/constructor types、type literal call/construct signatures、interface call/construct signatures、interface extends inherited call/construct signatures、本地 alias、外部 `Props` / `ModelValue` alias `deps`、numeric tuple index、number-indexed tuple union 和静态 rest parameter array element runtime type；overload selection、TypeScript checker-backed parameter inference、完整 TypeScript utility 语义和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` runtime utility type 子片段：Rust SFC 负责受支持的 `NonNullable<T>`、`Extract<T, U>` 和 `Exclude<T, U>` runtime constructor type 投影，覆盖 `defineProps` / `defineModel` 的本地与外部 `Props` / `ModelValue` alias `deps`；真实 TypeScript conditional utility 求值、任意 conditional/mapped type 求值、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` bounded static conditional type runtime 子片段：Rust SFC 负责条件可由有限静态 literal set 判定时的 `TSConditionalType` runtime constructor 投影，覆盖直接 true/false literal 条件、本地/外部 generic alias 的 `defineProps` / `defineModel` runtime inference 与 bridge `deps`；distributive conditional evaluation、`infer` clauses、checker-backed assignability、任意 conditional/mapped type 求值和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` BigInt literal runtime constructor 子片段：Rust SFC 负责 `TSLiteralType(BigIntLiteral)` 的 official `Number` runtime constructor 投影，覆盖 `defineProps` / `defineModel` 的本地 alias、union 和外部 `Props` / `ModelValue` alias `deps`；`bigint` keyword、任意 BigInt type 语义、TypeScript checker 求值和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `ReturnType<T>` runtime constructor 子片段：Rust SFC 负责受限 callable return runtime constructor 投影，覆盖 declared function、declared callable const、function type alias、type literal/interface call signature、interface extends inherited call signature、`typeof` declared value、scoped generic alias、本地/外部 `Props` / `ModelValue` alias 与 bridge `deps`；overload selection、checker-backed function return inference、arbitrary callable value lookup、arbitrary default expression exports、checker-backed/任意无注解函数体返回类型推断、递归 generic type 求解和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` annotated exported-function `ReturnType<typeof ...>` 子片段：Rust SFC 负责带显式返回注解的外部 `.ts` `export function` 和 `export default function` return-runtime 投影，覆盖 named/default imports 在 `defineProps` / `defineModel` 中的 `ReturnType<typeof ...>` runtime constructor 推断和 bridge `deps`；checker-backed/任意无注解函数体返回类型推断、任意 default expression export、overload selection 和 checker-backed callable resolution 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` annotated exported function-value `ReturnType<typeof ...>` 子片段：Rust SFC 负责带显式函数类型/返回注解的外部 `.ts` `export const make = (): T => ...`、`export const make: () => T = ...`、`export const make = function(): T { ... }` 和带注解 default arrow/function-expression export 的 return-runtime 投影，覆盖 named/default imports 在 `defineProps` / `defineModel` 中的 `ReturnType<typeof ...>` runtime constructor 推断和 bridge `deps`；checker-backed/任意无注解函数体返回类型推断、任意 callable/default expression 求值、overload selection 和 checker-backed callable resolution 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` bounded unannotated function-body `ReturnType<typeof ...>` 子片段：Rust SFC 负责静态显而易见函数体返回表达式的 runtime constructor 投影，覆盖外部 named/default function declarations、arrow/function values、default function expressions、`ReturnType<typeof imported>` 和 `ReturnType<typeof import('./x').make>` 的 `defineProps` / `defineModel` runtime inference 与 bridge `deps`；checker-backed 任意函数返回类型推断、任意控制流、async/promise unwrapping、overload selection、generic return solving 和任意 default expression/callable 求值仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` builtin wrapper/container runtime type 子片段：Rust SFC 负责 Vue 3 name-only runtime inference 的受支持 builtin wrappers / containers，覆盖 `ReadonlyArray`、未 indexed 的 `Parameters` / `ConstructorParameters`、`ReadonlyMap`、`ReadonlySet`、`Error`、string helpers、ref wrappers、`MaybeRef` / `MaybeRefOrGetter` 在 `defineProps` / `defineModel` 中的 runtime constructor 投影和外部 `Props` / `ModelValue` alias `deps`；imported Vue type definition 解析、完整 parameter tuple utility 语义、任意 conditional/mapped type 求值、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` mapped identity runtime type 子片段：Rust SFC 负责 Vue 3 `inferRuntimeType` 的受限 `{ [K in keyof T]: T[K] }` generic alias 分支，把 scoped generic `T` 的 runtime constructor types 透传给 `defineProps` prop member 和 `defineModel` runtime type，并覆盖外部 `Props` / `ModelValue` alias `deps`；任意 mapped type 求值、非 identity mapped runtime inference、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `TSTypeOperator` runtime type 子片段：Rust SFC 负责 Vue 3 runtime constructor projection 边界内的 `readonly` 解包和 `keyof` runtime constructor 推断，覆盖 resolvable literal/interface/alias/generic/import type、union/intersection、`keyof any`、builtin container、`Pick` / `Extract` 和外部 `Props` / `ModelValue` alias `deps`；完整 TypeScript key-space 求值、checker-backed `keyof`、任意 conditional/mapped type 求值、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `TSTypeQuery` / `typeof` declared value runtime type 子片段：Rust SFC 负责 Vue 3 受限 declared-value `typeof` 边界内的 runtime constructor 投影，覆盖 `typeof value`、`typeof Values.text`、`typeof import('./values').text`、`keyof typeof value`、`keyof typeof Values.boxed`、`keyof typeof import('./values').boxed`、`ReturnType<typeof import('./values').make>`、`ExtractPropTypes<typeof import('./values').options>`、`defineModel<typeof value>`、`defineModel<typeof Values.text>`、`defineModel<typeof import('./values').text>` 和外部 `Props` / `ModelValue` alias / namespace re-export / dynamic import type `deps`；任意 `typeof` expression、TS checker-backed value-space resolve、完整 TypeScript key-space 求值、递归 generic type 求解、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` call/construct signature runtime type 子片段：Rust SFC 负责 type literal / interface 成员中的 call signature / construct signature runtime constructor 投影，覆盖 `defineProps` prop member、`defineModel` runtime type 和外部 `Props` / `ModelValue` alias `deps`；runtime property validation、完整 intersection property validation、任意 TypeScript checker 求值和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `TSIntersectionType` runtime type flattening 子片段：Rust SFC 负责受支持 intersection runtime constructor 合并、`Unknown` 过滤和普通 props `skipCheck` / `type: null` 输出边界，覆盖本地与外部 `Props` / `ModelValue` alias `deps`；完整 intersection property validation、任意 TypeScript checker 求值和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` union/intersection duplicate props member merge 子片段：Rust SFC 负责受支持 `defineProps<T>()` props type 中 union / intersection 分支同名 prop 的官方 `mergeElements` 投影，合并 runtime constructors、保留首次出现顺序、按任一分支 optional 输出 `required: false`，并覆盖外部 `Props` alias bridge `deps`；完整 intersection property validation、unsupported/cyclic diagnostics、任意 TypeScript checker 求值和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` interface `extends` failed diagnostics / `@vue-ignore` 子片段：Rust SFC 负责 `defineProps<T>()` props type resolution 中 unresolved interface base 的官方 failed-extends diagnostic，并识别紧贴 heritage base 的 `/* @vue-ignore */` / line comment，使该 base 按 fallthrough attrs 处理；本地、外部类型文件和 bridge `deps` 路径均覆盖。`defineEmits` / `keyof` / runtime inference 继承失败仍保持静默边界；unsupported/cyclic interface diagnostics、任意 TypeScript checker 求值和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` top-level `defineProps<T>()` `resolveType` error diagnostics 子片段：Rust SFC 负责 `defineProps<T>()` 消费型 props type resolution 中 unresolved type reference、failed import source、unsupported computed key 和 unsupported indexed access key type 的官方错误边界；member-level unknown runtime type、global type file 内部 import name 和普通 runtime inference 仍保持静默边界，完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` mapped props type 子片段：Rust SFC 负责静态可展开 mapped props、template-literal key remapping、有序笛卡尔 key 展开、`Capitalize` / `Uppercase` / `Lowercase` / `Uncapitalize`、静态 `Extract` / `Exclude` 和 generic mapped alias 参数绑定，覆盖外部 generic alias 内部非导出 string-key helper 类型和 bridge `deps`；任意 conditional/mapped type 求值、递归 generic type 求解、unsupported/cyclic diagnostics、TypeScript checker 集成和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` declaration merging type-resolution 子片段：Rust SFC 负责同作用域重复 `interface`、重复 exported `namespace`、同名 namespace/interface 和重复 TS enum runtime constructor 的受限声明合并，覆盖 `defineProps` indexed access / qualified type reference、`defineModel` runtime type、外部 `types.ts` bridge `deps`，并保持本地 enum 对同名 import/global type 的遮蔽边界；TypeScript checker-backed declaration merging、ambient module augmentation、value namespace lookup、unsupported/cyclic diagnostics 和完整官方 `resolveType` 仍按未完成项推进。
- [x] 实现 Vue 3 `compileScript` `TSPropertySignature` `@vue-ignore` runtime type 子片段：Rust SFC 负责识别 property signature type annotation 上紧贴类型节点的 `@vue-ignore`，把对应 prop runtime constructor 投影为 `Unknown` 并输出官方 no-check `type: null` runtime prop 形状，覆盖本地 inline type literal、外部 interface 和 bridge `deps` 路径；任意类型位置 ignore、checker-backed `resolveType`、unsupported/cyclic diagnostics 和完整官方 `resolveType` 仍按未完成项推进。
- [ ] 实现 normal script analysis。
- [ ] 实现 script setup merge。
- [ ] 实现完整 `defineProps` parity（已完成基础 runtime/TS macro inference、`withDefaults` diagnostics、duplicate diagnostics、destructure diagnostics、destructure usage diagnostics、destructure default type diagnostics、destructure setup-body rewrite、destructure defaults runtime merge、destructure rest proxy 和 `propsDestructure` option-boundary 子片段；完整 reactive destructure transform、inline template rewrite、完整 TS type resolve、完整 binding metadata 等仍未完成）。
- [ ] 实现完整 `defineEmits` parity（已完成基础 runtime/TS macro inference 和 duplicate diagnostics 子片段；完整宏分析、完整 TS type resolve、完整 binding metadata 等仍未完成）。
- [x] 实现 `defineExpose` 基础 macro/diagnostic 子片段。
- [x] 实现 `defineOptions` 基础 runtime macro 子片段。
- [x] 实现 `defineSlots()` 基础 macro 子片段。
- [x] 实现 `defineModel` 基础 runtime macro 子片段。
- [x] 实现 `defineModel` get/set transformer option splitting 子片段。
- [x] 实现 `withDefaults()` macro diagnostics 子片段。
- [ ] 实现 reactive props destructure（已完成 setup body 引用重写、默认值 runtime 合并、rest proxy 和 `propsDestructure` option-boundary 子片段；inline template rewrite 等仍未完成）。
- [x] 实现 top-level await：Rust SFC 负责 `<script setup>` 顶层 await 的 AST 检测、`_withAsyncContext` 重写、`async setup` / temp var 注入、函数/方法作用域跳过、表达式/语句 await 输出和官方前导分号规则；source map 合并和完整 compileScript parity 仍按独立未完成项推进。
- [x] 实现 import usage / binding metadata Rust-backed 子片段：Rust SFC 负责 Vue 3 non-inline template import usage 识别、模板实际使用 import 的 setup return getter、setup `let` getter/setter、import binding metadata 分类，并把同一 metadata 供 inline template 使用；完整 normal script analysis、完整 result shape、source map、完整 TS type resolve 仍按独立未完成项推进。
- [x] 实现 normal script binding metadata Rust-backed 子片段：Rust SFC 负责 normal-only `export default {}` 的 `props` / `inject` / `setup` / `data` / `methods` / `computed` public binding metadata，并在混合 `<script>` + `<script setup>` 路径按官方 script declaration 分类合并 normal script metadata、inline-template metadata 和 setup return getter/setter；generated/NAPI alias 只 hydrate `bindings.__isScriptSetup` 的 non-enumerable API 形状，完整 normal script analysis、完整 result shape、source map、完整 TS type resolve 仍按独立未完成项推进。
- [x] 实现 `compileScript` source-map foundation 子片段：Rust SFC 负责 `compileScript().map` 基础生成、`sourceMap` / `source_map` option 解析、script block 行级/字符级回溯和 inline template map 合并；完整 source-map parity、template expression token 细粒度覆盖、previous map 合并、完整 result shape 仍按独立未完成项推进。
- [ ] 实现 TS type resolve（已完成相对 named/default/re-export/dynamic/namespace/qualified type 解析、相对 `.vue` script 类型抽取、arbitrary extension sidecar、TS enum、受限 bare package、受限 package `typesVersions` declaration-entry / Node semver selector/order / project-local TypeScript version discovery、受限 tsconfig path/project-reference alias、受限 tsconfig JSONC parsing、受限 package-based tsconfig `extends`、显式 `globalTypeFiles`、显式 global re-export graph、受限 tsconfig automatic global `.d.ts` discovery、受限 tsconfig `types` / `typeRoots` global declaration package discovery、class declaration/default class、静态 runtime props object `ExtractPropTypes<typeof ...>` props-options、`ExtractPropTypes` / declared props factory `ReturnType<typeof ...>` props-options、bounded `ReturnType<T>` runtime constructor 投影、带显式返回注解的外部 `.ts` named/default function export `ReturnType<typeof ...>` 投影、带显式函数类型/返回注解的外部 `.ts` function-value/default expression export `ReturnType<typeof ...>` 投影、受限无注解函数体 `ReturnType<typeof ...>` runtime constructor 投影、`Readonly` / `Pick` / `Omit` / simple generic props alias、有限静态 key `Record<K, V>`、受支持 indexed access（含 method-signature indexed access no-check 边界）与 array/tuple `T[number]`、受限 `Parameters<T>[number|N]` / `ConstructorParameters<T>[number|N]` indexed-access runtime 投影（含 function/constructor types、type literal/interface call/construct signatures 和 interface extends inherited signatures）、受支持 runtime utility、bounded static conditional type runtime projection、BigInt literal runtime constructor projection、builtin wrapper / mapped identity / type operator / type query / qualified type query / import-type query / call-construct signature / intersection flattening / union-intersection duplicate props merge、interface extends failed diagnostics / `@vue-ignore`、`TSPropertySignature` `@vue-ignore`、top-level `defineProps<T>()` `resolveType` error diagnostics，以及静态可展开 mapped props / template-literal key remapping / string manipulation helper 子切片，覆盖 `defineProps` / `defineEmits` / `defineModel` 的相关 runtime inference 与 normalized `deps`；TypeScript checker parity、完整 include/exclude 语义、完整 TypeScript project service、任意 type package layout、完整 NodeNext conditional exports、arbitrary default expression exports、checker-backed/任意无注解函数体返回类型推断、checker-backed 任意函数返回类型求值、完整 default-backed requiredness、runtime property validation、完整 intersection property validation、unsupported/cyclic diagnostics、宽泛 index signature、imported Vue type definition 解析、完整 parameter tuple utility 语义、overload selection、distributive conditional evaluation、`infer` clauses、checker-backed assignability、真实 TypeScript conditional utility 求值、完整 TypeScript key-space 求值、checker-backed `keyof`、任意 `typeof` expression、未注册/任意 qualified value lookup、TS checker-backed value-space resolve、任意 conditional/mapped type、递归 generic type 求解和完整官方 `resolveType` 仍未完成）。
- [ ] 实现 `compileTemplate` wrapper。
- [ ] 实现 `compileScript` wrapper，并对齐官方结果形状：`type`、`content`、`loc`、`attrs`、`setup`、`lang`、`bindings`、`imports`、`map`、`scriptAst`、`scriptSetupAst`、`deps`（`imports` public object shape、bounded `scriptAst` / `scriptSetupAst` statement-array projection、source-map foundation、相对 named/default/dynamic/qualified/namespace import、TS enum runtime type、class/default class type deps、`.vue` / sidecar 外部类型、re-export graph、bare package type、package `typesVersions` declaration-entry / project-local TS version `deps`、显式 `globalTypeFiles` `deps`、显式 global re-export graph `deps`、bounded static runtime props object `ExtractPropTypes` `deps`、bounded `ExtractPropTypes` / declared props factory `ReturnType` `deps`、external `ReturnType<T>` runtime alias `deps`、external annotated function-value `ReturnType<typeof ...>` alias `deps`、external unannotated function-body `ReturnType<typeof ...>` alias `deps`、external generic utility props alias `deps`、external generic `Record` props alias `deps`、external indexed access / method-signature indexed access alias `deps`、external array/tuple indexed access alias `deps`、external signature parameter tuple utility alias `deps`、external interface-extends signature parameter tuple utility alias `deps`、external runtime utility type alias `deps`、external BigInt literal runtime alias `deps`、external builtin wrapper/container type alias `deps`、external mapped identity runtime alias `deps`、external type-operator runtime alias `deps`、external type-query / qualified type-query / import-type query runtime alias `deps`、external signature runtime alias `deps`、external intersection runtime alias `deps`、external duplicate union/intersection props alias `deps` 和 external generic mapped props alias `deps` 子切片已完成；完整 result-shape parity 仍未完成）。
- [x] 实现 `compileStyle` wrapper，并对齐官方结果形状：`code`、`dependencies`、`errors`、`map`、`rawResult`；Vue 3 public `compileStyle.spec.ts` 已经通过 public `@vue/compiler-sfc` alias 路由到 Rust `vuec_style`，覆盖 raw CSS source、scoped CSS、CSS Modules、preprocess dependencies 和 public `rawResult` shape。JS `preprocessOptions.additionalData` function callbacks 仍在 package boundary materialize，不计作 Rust compiler semantics。

完成判定：

- [x] SFC parse 官方测试通过。
- [x] `rewriteDefault` 官方测试通过。
- [x] `compileScript` 官方测试通过。
- [x] `compileStyle` 官方测试通过。
- [x] `compileTemplate` 官方测试通过。
- [x] `cargo xtask run-option-matrix --version-line vue3 --package @vue/compiler-sfc` 通过。
- [x] `cargo xtask run-output-contract --version-line vue3 --package @vue/compiler-sfc` 通过。
- [x] `compiler-sfc/__tests__` 100% 通过。

### M14: `vuec_style`

开发工作：

- [ ] scoped CSS selector rewrite。
- [x] `:deep()` / `::v-deep` / `/deep/`：Rust scoped selector rewrite 已覆盖 top-level deep、deprecated `>>>` / `/deep/` combinator、combinator `::v-deep`、以及 `:is()` / `:where()` / `:not()` / `:has()` 内嵌 deep 的递归和 mixed branch split；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] `:slotted()`：top-level `:slotted(...)` / `::v-slotted(...)` 已按官方 slot scope `[id-s]` 注入规则保留前缀/后缀并覆盖 universal selector 目标；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] `:slotted()` universal-leading combinator：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为处理无前缀 `:slotted(* + .foo)` 及 top-level `:is()` / `:where()` 包裹形态，去除 inner universal selector 消失后多余的 leading whitespace，同时保留 `.a :slotted(* + .foo)` 等有前缀 selector 的官方间距；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] selector-list spacing / slotted inner container：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为保留普通 selector-list branch 的 comma whitespace，并在 branch 起始 scope 注入 / replacement 路径（leading `*`、`:slotted(...)`、`:global(...)`、`:deep(...)`、`:not(...)`、`:has(...)`）去除该 whitespace；`:slotted(:is(...))` / `:slotted(:where(...))` 已改为对 inner branches 注入 `[id-s]`；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] escaped selector tokens：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为处理 escaped `:` / `,`、escaped pseudo-like class names（如 `.foo\:global(.bar)`）、CSS hex escapes（如 `.\31 foo`）和非 ASCII identifier token，不再把 escaped colon/comma 当作 pseudo 或 selector-list 边界；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] selector comments：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为跳过 selector comment 内部的 comma、pseudo、attribute bracket、deep combinator 等伪语法，删除 whitespace/combinator 位置的 comments，保留 compound selector comments（如 `.foo/*x*/.bar`），并在 `.foo/*x*/:hover` 等形态中把 scope 注入到保留 comment 之后；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] `:global()`：top-level `:global(...)` / `::v-global(...)` 已按官方规则用第一个 inner selector branch 替换整条 selector，嵌套在 `:is()` / `:where()` / `:not()` / `:has()` 内的 `:global(...)` 保持外层 scoped 注入路径；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] `:is()` / `:where()` 注入目标：当 top-level `:is(...)` / `:where(...)` 是当前 selector 的 scope injection target 时，Rust scoped selector rewrite 已按官方规则递归给每个 inner branch 注入 `[data-v-*]`；`.host:is(...)`、`:where(...).active`、`:not(...)`、`:has(...)` 等非目标形态仍走外层注入路径；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] native CSS nesting scoped rewrite：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖直接嵌套 selector、混合父级 declaration 包装到 `&[data-v-*] { ... }`、`&` 锚点注入、嵌套 `@media` / `@supports` 内层 selector 递归 scoped，以及普通 rule 内嵌 `@keyframes` 收集/重命名和 animation value rewrite；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] deep/native nesting scoped rewrite：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖 functional `:deep(...)` / `::v-deep(...)` 只使用第一 selector branch、deep rule body 内 native nested selector 保持非 scoped、deep body 内 nested `@media` / `@keyframes` 和 animation value rewrite、`:is()` / `:where()` / `:has()` / `:not()` 内 deep branch 在 native nesting 下的 split/injection 规则，以及 deprecated `>>>` / `/deep/` 与 native nested rule 的输出；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] deep-container split pseudo-suffix：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖 deep-bearing `:is()` / `:where()` 后接纯 pseudo suffix 时的 split branch scoped 注入位置，支持 normal / `:global(...)` / `:slotted(...)` branch、`::before`、多 pseudo、`:nth-child(...)` 和 direct native nested selector block；`:has(...)` 保留外层 scoped 注入但匹配官方 comma whitespace；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] direct nested parent selector：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖 direct native nested rule 的 parent selector 规范化，删除 selector-list / container branch 起始 universal selector，解包 top-level `:global(...)` / `:slotted(...)`，并在 top-level `:is()` / `:where()` parent container 里规范化 `:global(...)` / `:slotted(...)` / universal branches；完整 scoped CSS 仍需覆盖剩余复杂 selector parity。
- [x] first-normal direct deep-container nested rule：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖无 trailing suffix 的 direct native nested deep-container，当第一 branch 不是 deep branch 时在首个 deep branch 前复用 direct nested parent selector 规范化，之后复用 scoped/deep branch rewrite，parent declarations 输出为 `& { ... }`，nested selectors 保持 deep passthrough；完整 scoped CSS parity 仍需后续覆盖。
- [x] first-normal direct deep-container trailing-suffix：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖 first-normal direct native nested deep-container 的 trailing pseudo / class-compound / descendant suffix，区分 no-anchor split 输出与 scope-anchor combined 输出，并保持 `:has()` / `:is()` / `:where()` 的官方 comma spacing 和 parent declaration `& { ... }` 包装；完整 scoped CSS parity 仍需后续覆盖。
- [x] deep passthrough parent-anchor nested block formatting：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖 deep passthrough rule body 中 top-level `&... { ... }` native nested block 的换行格式，保留 comments 与 declaration ordering，并保持 nested selector 不额外 scoped；`:has(...)` split comma spacing 和完整 scoped CSS parity 仍需后续覆盖。
- [x] `:has(...)` split pseudo comma-spacing：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖 deep-bearing `:has(...)` 后接 `:hover`、`::before`、`:nth-child(...)` 等纯 pseudo suffix 时的 split selector comma spacing，deep branch 前保留 comma-space，non-deep branch 注入保持 compact comma；完整 scoped CSS parity 仍需后续覆盖。
- [x] direct nested parent-selector comments：Rust scoped selector rewrite 已按官方 public `compileStyle` 行为覆盖 direct native nested rule parent selector 中 whitespace/combinator 位置 comments 的保留/移除边界，纯 parent selector 保留 comments，存在 leading universal、`:global(...)`、`:slotted(...)` 或 `:is()` / `:where()` branch normalization 时继续移除；完整 scoped CSS parity 仍需后续覆盖。
- [x] deprecated deep warning parity：Vue 3 public scoped style path 已对 deprecated `>>>`、`/deep/`、combinator `:deep`、combinator `::v-deep` 产出 Rust warning diagnostics，并在 Vue 3 SFC package alias 边界转换为 `console.warn`；functional `:deep(...)` / `::v-deep(...)`、`:global(...)`、`@keyframes` body 和 Vue 2.7 SFC style path 保持无 warning。
- [x] CSS vars collect / rewrite：Rust style/SFC 已区分 Vue 3 escaped naming 与 Vue 2.7 legacy underscore naming，覆盖 prod hash leading-digit guard、Unicode var 名、`//` line comment 跳过差异、block comment 跳过、复杂 `v-bind(...)` lexer 和 public compileStyle 输出对比；Vue 3 CSS vars script 注入已由 public `cssVars.spec.ts` Rust-backed SFC slice 覆盖。
- [ ] CSS modules。
- [x] 实现 CSS Modules Rust slice：默认 local class/id selector rewrite、class 属性选择器按官方 public path 保持非本地化、`:global(...)` 排除、`:local(...)` 纳入、`scopeBehaviour: "global"`、`globalModulePaths` 静态字符串 pattern 的入口级默认 global scope、官方默认 scoped-name hash / line-number 命名、`generateScopedName` 模板与 `hashPrefix`、local `@keyframes` / `animation-name` / `animation` token scoping、`localsConvention: "camelCase"` / `"camelCaseOnly"` / `"dashes"` / `"dashesOnly"`、`exportGlobals`（class/id）、同文件 `composes` / `compose-with`（已定义本地 class 与 `global(...)`）、`composes ... from global`、ICSS `:export`、本地相对 `:import(...)` symbol replacement、本地相对 `composes ... from "./dep.css"`、默认文件系统 loader 的 `node_modules` package subpath / `package.json.main` / 静态 package `exports` root、精确子路径、CJS active condition（`require` / `node` / `default`）对象顺序/嵌套对象和简单单通配符子路径 CSS lookup、同文件/顺序非法 `composes` class diagnostics、非单一 local class selector 上 `composes` 的官方前缀错误 diagnostics 和 result `modules` map；尚未覆盖完整 `postcss-modules` option surface、JS `resolve` / `Loader` 回调、`globalModulePaths` RegExp 对象 / 无效 pattern 错误 parity、完整 package `exports` condition set / pattern / Node resolution 或完整 PostCSS Modules diagnostics。
- [x] local `@value`：CSS Modules Rust path 已支持本地 `@value name: value;` 的移除、导出和官方 single-pass replacement，覆盖 selectors、at-rule preludes、declarations、strings，并跳过 comments；尚未覆盖 `@value ... from` imports、JS resolver/Loader 回调或完整 PostCSS Modules parity。
- [x] static `@value ... from` imports：CSS Modules Rust path 已支持静态 value imports 的文件解析、递归 dependency 编译、imported value/class export、selector/declaration/at-rule/string/`composes` replacement、imported class 非二次 scoped，以及 missing import 的 `undefined` / `i__const_*` 行为；尚未覆盖 JS resolver/Loader 回调或完整 PostCSS Modules parity。
- [x] static `@value` missing compose：CSS Modules Rust path 已支持 missing static value import 在 CSS 文本中使用 `i__const_*` replacement、在 `composes` 与 module export 中使用 `undefined` 的官方分离行为；完整 PostCSS Modules parity 仍未声明完成。
- [x] imported CSS formatting：CSS Modules Rust path 已让依赖 CSS 走 entry style normalization，并保留官方 static value import 递归拼接的 block-boundary newline；完整 PostCSS Modules parity 仍未声明完成。
- [x] ICSS import symbol replacement：CSS Modules Rust path 已支持 `:import(...)` imported symbols 在 selector、at-rule prelude、declaration、string、`composes` 中替换，避免 imported selector token 生成本地导出或二次 scoped，并清理移除 ICSS rule 后的空白；完整 PostCSS Modules parity 仍未声明完成。
- [x] ICSS missing import/export aliases：CSS Modules Rust path 已支持 missing `:import(...)` symbol 在 selector / declaration / at-rule / string / `composes` / `:export` alias 中的官方行为，并保持空导出时的 `modules: {}` public shape；完整 PostCSS Modules parity 仍未声明完成。
- [x] native nesting rewrite：CSS Modules Rust path 已支持 native nested rule body 中 nested selectors、`&` selectors、nested at-rules 和 nested `:local(...)` / `:global(...)` 的 module rewrite 与导出，并保持 nested block 前后 declaration 顺序；完整 PostCSS Modules parity 仍未声明完成。
- [x] native nested compose diagnostics：CSS Modules Rust path 已支持 native nested style rule 内 `composes` / `compose-with` 的官方 fatal diagnostics 与空 CSS / 无 module map result shape；完整 PostCSS Modules parity 仍未声明完成。
- [ ] source map。
- [x] 实现 style source map traceability slice：`compile_style` 支持真实 `sourcesContent`、原始文件 id 和 base offset，生成 CSS 行级 mapping 可回查 style 源行；尚未覆盖所有官方 source-map diff parity。
- [x] `compileStyle.sourceMap` public option shape：Vue 3 / Vue 2.7 官方 package alias 已对齐 `compileStyle({ sourceMap: true })` 的结果形状，public `sourceMap` / `source_map` 不再让 Rust bridge 生成新 map，返回对象保留 `map` 字段且值为 `undefined`；`map` / `inMap` previous-map 合并和完整 source-map parity 仍未声明完成。
- [x] preprocessor adapter：scss / sass Rust slice：`vuec_style` 已接入 Rust `grass` Sass 引擎，支持 SCSS/Sass 编译、入口 `filename` 相对 `@import` 解析、解析后的 `dependencies`、`preprocessOptions.additionalData` 字符串，以及 JS API 边界对函数型 `additionalData(source, filename)` 的求值后传入 Rust；Less/Stylus 和完整 PostCSS/lightningcss 生态 parity 仍未声明完成。
- [x] preprocessor adapter：less Rust slice：`vuec_style` 已用 Rust 解析/渲染路径覆盖 Less 变量、规则内变量、嵌套 selector / `&`、selector list、嵌套 media/container at-rule、`preprocessOptions.additionalData`、本地 `@import` 内联、`loadPaths` / `includePaths` 和 normalized dependencies；完整 Less 语言、插件函数和 PostCSS/lightningcss 生态 parity 仍未声明完成。
- [x] preprocessor adapter：stylus Rust slice：`vuec_style` 已用 Rust 缩进 parser/renderer 覆盖 Stylus 变量、裸变量替换、`$name` 替换、嵌套 selector / `&`、selector list、嵌套 media/container at-rule、`preprocessOptions.additionalData`、本地 `@import` 内联、`loadPaths` / `includePaths` 和 normalized dependencies；完整 Stylus 语言、函数/mixin 和 PostCSS/lightningcss 生态 parity 仍未声明完成。
- [x] error mapping：当前 Rust style 错误（unsupported preprocessor、unresolved `@import`）已输出结构化 `Diagnostic` span，SFC/CLI 可映射到原 `.vue` 源码 offset；完整 PostCSS/lightningcss error parity 仍归属于适配层工作。

完成判定：

- [x] scoped CSS 官方测试通过。
- [x] CSS vars 官方测试通过。
- [x] CSS modules 官方测试通过。
- [x] preprocessor smoke 通过。
- [x] `cargo test -p vuec_style` 通过。

### M15: `vuec_runtime_tests`

开发工作：

- [x] Node runtime runner。
- [x] jsdom DOM runner。
- [x] Vue 2 runtime mount helper。
- [x] Vue 3 runtime mount helper。
- [x] SSR render helper。
- [x] hydration warning capture。
- [x] generated render execution sandbox。
- [x] fixture snapshot。

完成判定：

- [x] Vue 2 generated render 可执行并产生 DOM。
- [x] Vue 3 generated render 可执行并产生 DOM。
- [x] Vue 3 SSR string 与官方一致。
- [x] hydration smoke 通过。

### M16: `vuec_napi`

开发工作：

- [x] NAPI crate。
- [x] platform package metadata 和当前平台 smoke：主 `@vuec-rs/native` loader 可回退到 optional platform package，`cargo xtask verify-napi-platform` 会在隔离目录验证当前平台包加载。
- [x] JS loader。
- [x] 类型声明生成。
- [x] 官方包名 NAPI alias smoke：`vue-template-compiler`、`vue/compiler-sfc`、`@vue/compiler-core`、`@vue/compiler-dom`、`@vue/compiler-ssr`、`@vue/compiler-sfc` 可从 `target/napi-alias/node_modules` require 并调用代表性 NAPI-backed API。
- [x] API 对齐 Vue 2.6 `vue-template-compiler`：`cargo xtask verify-napi-api` 对比官方 `vue-template-compiler` API manifest 与 NAPI-backed 官方包名 alias。
- [x] API 对齐 Vue 2.7 `vue-template-compiler`：`cargo xtask verify-napi-api` 对比官方 `vue-template-compiler` API manifest 与 NAPI-backed 官方包名 alias。
- [x] API 对齐 Vue 2.7 `vue/compiler-sfc` 导出：`cargo xtask verify-napi-api` 对比官方 `vue/compiler-sfc` API manifest 与 NAPI-backed 官方包名 alias。
- [x] API 对齐 `@vue/compiler-core`：`cargo xtask verify-napi-api` 对比官方 `@vue/compiler-core` API manifest 与 NAPI-backed 官方包名 alias，当前 145 个导出形状一致。
- [x] API 对齐 `@vue/compiler-dom`：`cargo xtask verify-napi-api` 对比官方 `@vue/compiler-dom` API manifest 与 NAPI-backed 官方包名 alias，当前 164 个导出形状一致。
- [x] API 对齐 `@vue/compiler-ssr`：`cargo xtask verify-napi-api` 对比官方 `@vue/compiler-ssr` API manifest 与 NAPI-backed 官方包名 alias。
- [x] API 对齐 `@vue/compiler-sfc`：`cargo xtask verify-napi-api` 对比官方 `@vue/compiler-sfc` API manifest 与 NAPI-backed 官方包名 alias。
- [x] error conversion。
- [x] source map JSON conversion。
- [x] 支持 npm alias：`vue-template-compiler` -> Rust Vue 2.6/2.7 对应包，`cargo xtask verify-napi-alias` / `run-napi-conformance --all` 通过。
- [x] 支持 npm alias：`vue` -> Rust Vue 2.7 主包，并暴露 `vue/compiler-sfc`，`verify-napi-api` 与 `run-napi-conformance --all` 覆盖该入口。
- [x] 支持 npm alias：`@vue/compiler-core`、`@vue/compiler-dom`、`@vue/compiler-ssr`、`@vue/compiler-sfc` -> Rust Vue 3 包，`verify-napi-api` 与 `run-napi-conformance --all` 覆盖四个入口。

完成判定：

- [x] `pnpm test:napi` 通过。
- [x] `cargo xtask verify-napi-alias` 通过官方包名 NAPI alias smoke。
- [x] `cargo xtask verify-napi-api` 通过 Vue 2.6 / Vue 2.7 `vue-template-compiler`、Vue 2.7 `vue/compiler-sfc`、Vue 3 `@vue/compiler-core`、Vue 3 `@vue/compiler-dom`、Vue 3 `@vue/compiler-ssr` 与 Vue 3 `@vue/compiler-sfc` 官方 API manifest 对比（`7/7`）。
- [x] `cargo xtask verify-napi-platform` 通过当前平台 package smoke。
- [x] `cargo xtask run-napi-output-contract --all` 通过 NAPI-backed 官方包名 alias 的 public output contract（`7/7`）。
- [x] `cargo xtask run-napi-option-matrix` 已覆盖 Vue 3 core/dom/ssr NAPI-backed public option rows：`@vue/compiler-core` `8/8`、`@vue/compiler-dom` `4/4`、`@vue/compiler-ssr` `2/2`。
- [x] `cargo xtask run-napi-option-matrix` 已覆盖 Vue 2 `vue-template-compiler` NAPI-backed public option rows：Vue 2.6 `10/10`、Vue 2.7 `3/3`。
- [x] `cargo xtask run-napi-option-matrix --version-line vue2_7 --package vue --entry vue/compiler-sfc` 已覆盖 Vue 2.7 SFC NAPI-backed public option rows：`parse`、`compileTemplate`、`compileScript`、`compileStyle` 为 `5/5`。
- [x] `cargo xtask run-napi-option-matrix --all` 全部通过；当前七个 NAPI-backed official package-name targets 为 `7/7`。
- [x] `cargo xtask run-napi-conformance --all` 已接入 NAPI-backed official package-name alias 执行后端，报告写入 `target/conformance/<lock>/napi-*.json` 并标记 `alias_backend: "napi"`；当前七个官方套件全量通过：Vue 2.6 compiler `188/188`、Vue 2.7 compiler `190/190`、Vue 2.7 SFC `144/144`、Vue 3 core `652/652`、Vue 3 DOM `133/133`、Vue 3 SFC `461/461`、Vue 3 SSR `129/129`。
- [x] `cargo xtask verify-npm-alias --version-line vue2_6 --all` 通过。
- [x] `cargo xtask verify-npm-alias --version-line vue2_7 --all` 通过。
- [x] `cargo xtask verify-npm-alias --version-line vue3 --all` 通过。
- [x] 官方 Vue 2.6 compiler tests 通过 npm alias 调用 NAPI：`cargo xtask run-napi-conformance --suite vue2-compiler` / `--all`。
- [x] 官方 Vue 2.7 compiler/SFC tests 通过 npm alias 调用 NAPI：`cargo xtask run-napi-conformance --suite vue27-compiler`、`--suite vue27-sfc` / `--all`。
- [x] 官方 Vue 3 compiler tests 通过 npm alias 调用 NAPI：`cargo xtask run-napi-conformance --suite vue3-core`、`vue3-dom`、`vue3-sfc`、`vue3-ssr` / `--all`。
- [x] `cargo xtask diff-api --all` 无未批准 diff。
- [x] Windows/Linux/macOS 平台包 smoke 已纳入 `.github/workflows/ci.yml` 三平台矩阵；实际远端通过状态以 CI 运行结果为准，当前本机 `cargo xtask verify-napi-platform` 通过 Windows x64 平台包 smoke。

### M17: `vuec_wasm`

开发工作：

- [x] wasm-bindgen API 初版。
- [x] WASI API 初版：`vuec_wasm` 提供 `wasi_smoke` WASI binary，经 stdin/stdout JSON 请求分发到 Rust JSON helper，并由 `cargo xtask verify-wasm-wasi` 通过 wasmtime 验证。
- [x] browser/Node loader 初版：`@vuec-rs/wasm` 提供 async `init()` loader、类型声明和 Node smoke；真实 browser headless 由 `cargo xtask verify-wasm-browser` 验证。
- [x] 初始化 allocator / panic hook：wasm32 构建使用显式 `wee_alloc` 全局 allocator，并通过 wasm-bindgen start hook 安装 `console_error_panic_hook`。
- [x] serializable options/result 初版：`vuec_wasm` 通过 JSON-string ABI 暴露 Vue 2 template、Vue 3 DOM/SSR、SFC parse/template/script/style 结果。
- [x] error conversion 初版：WASM JSON ABI 对无效 options JSON 和序列化失败返回结构化 `errors`/`diagnostics` JSON；unwind-capable 构建额外覆盖边界 panic-to-JSON，wasm32 panic 仍按 abort/console hook 处理。
- [x] source map conversion 初版：`cargo xtask verify-wasm` 的 Node smoke 覆盖 Vue 3 DOM `sourceMap` JSON 输出。
- [x] Node smoke fixture：`packages/wasm/smoke.js` 覆盖 Vue 2、Vue 3 DOM/SSR、SFC parse/template/style、diagnostic 和 source map。
- [x] browser smoke fixture：`wasm-bindgen-test` 在 headless Chrome 覆盖 Vue 3 DOM template、SFC template 和 invalid options error envelope；`cargo xtask verify-wasm-browser` 包装 `wasm-pack test --headless --chrome crates/vuec_wasm`。

完成判定：

- [x] `wasm-pack test --headless --chrome` 通过：`cargo xtask verify-wasm-browser`，本机验证使用与 Chrome 148 匹配的 `VUEC_WASM_CHROMEDRIVER`。
- [x] WASI smoke 通过：`cargo xtask verify-wasm-wasi`。
- [x] Node wasm-bindgen compile template smoke 通过：`cargo xtask verify-wasm`。
- [x] Node wasm-bindgen compile SFC smoke 通过：`cargo xtask verify-wasm`。
- [x] browser compile template smoke 通过：`cargo xtask verify-wasm-browser`。
- [x] browser compile SFC smoke 通过：`cargo xtask verify-wasm-browser`。

### M18: `vuec_cli`

开发工作：

- [x] `vuec compile-template`。
- [x] `vuec compile-sfc`。
- [x] `vuec compile-ssr`。
- [x] `vuec parse-sfc`。
- [x] `vuec conformance`。
- [x] `vuec bench`。
- [x] 支持 JSON 输出。
- [x] 支持 diagnostics 输出。
- [x] 支持 source map 输出。

完成判定：

- [x] CLI 能编译 Vue 2 template：`cargo xtask verify-cli`。
- [x] CLI 能编译 Vue 3 template：`cargo xtask verify-cli`。
- [x] CLI 能编译 Vue 3 SFC：`cargo xtask verify-cli`。
- [x] CLI 能编译 SSR：`cargo xtask verify-cli`。
- [x] CLI JSON 输出可被测试读取：`cargo xtask verify-cli`。

### M19: 性能与增量编译

开发工作：

- [x] benchmark framework：`cargo xtask bench`。
- [x] 官方 JS compiler 对照：`cargo xtask bench`。
- [ ] Vize / Verter / Fervid 对照可选。
- [x] arena allocation 优化：`cargo xtask verify-arena`。
- [x] string interning：`cargo xtask verify-string-interning`。
- [x] AST cache：`cargo xtask verify-ast-cache`。
- [x] SFC block cache：`cargo xtask verify-incremental`。
- [x] 并行编译：`cargo xtask verify-parallel`。
- [x] incremental invalidation：`cargo xtask verify-incremental`。
- [x] memory peak 统计：`cargo xtask bench` 的 `peakRssBytes`。

完成判定：

- [x] `cargo xtask bench` 生成稳定报告。
- [x] benchmark 输入、环境、commit 都写入报告。
- [x] 性能优化不改变 conformance 结果：`cargo xtask summarize-compat --locked`。
- [x] 增量编译 fixture 正确失效：`cargo xtask verify-incremental`。

### M20: 发布与文档

开发工作：

- [x] 兼容矩阵文档：`docs/COMPATIBILITY_MATRIX.md` + `cargo xtask verify-release-docs`。
- [x] API 文档：`docs/API.md` + `cargo xtask verify-release-docs`。
- [x] 架构文档：`docs/ARCHITECTURE.md` + `cargo xtask verify-release-docs`。
- [x] conformance 报告模板：`docs/CONFORMANCE_REPORT_TEMPLATE.md` + `cargo xtask verify-release-docs`。
- [x] CHANGELOG：`CHANGELOG.md` + `cargo xtask verify-release-docs`。
- [x] npm package README：`packages/**/README.md` + `cargo xtask verify-release-docs`。
- [x] crates.io package metadata：`cargo xtask verify-crate-metadata`。
- [x] release checklist：`docs/RELEASE_CHECKLIST.md` + `cargo xtask verify-release-docs`。
- [x] security / supply-chain 检查：`docs/SECURITY_SUPPLY_CHAIN.md` + `cargo xtask verify-supply-chain`。

完成判定：

- [x] 每个 npm package 目录都有 README：`cargo xtask verify-release-docs`。
- [x] 每个公开 API 有文档：`cargo xtask verify-public-api-docs`。
- [x] release dry-run 成功：`cargo xtask verify-release-dry-run` 已实现并执行真实 dry-run；跨平台原生产物与首次 crates.io 发布顺序限制记录在 `docs/UNRESOLVED_PROBLEMS.md`。
- [x] 发布产物安装 smoke 通过：`cargo xtask verify-release-install-smoke` 已实现并验证当前 Windows x64 原生包与 WASM 包；非当前平台安装 smoke 限制记录在 `docs/UNRESOLVED_PROBLEMS.md`。

## 官方测试进度看板

| 测试套件 | 目标 | 完成判定 |
| --- | --- | --- |
| Vue 2.6 parser | 100% | `vue2-compiler/parser` 无失败 |
| Vue 2.6 optimizer | 100% | `vue2-compiler/optimizer` 无失败 |
| Vue 2.6 codegen | 100% | `vue2-compiler/codegen` 无失败 |
| Vue 2.6 compiler options | 100% | warnings、modules、directives、delimiters、option matrix 无失败 |
| Vue 2.6 output contract | 100% | schema、exact JS、diagnostic、source map、runtime 无失败 |
| Vue 2.7 template parser | 100% | `vue27-compiler/parser` 无失败 |
| Vue 2.7 template optimizer | 100% | `vue27-compiler/optimizer` 无失败 |
| Vue 2.7 template codegen | 100% | `vue27-compiler/codegen` 无失败 |
| Vue 2.7 template options | 100% | `vue-template-compiler@2.7.x` option matrix 无失败 |
| Vue 2.7 SFC parse | 100% | `vue/compiler-sfc` descriptor/source map 无失败 |
| Vue 2.7 SFC compileScript | 100% | script/setup、TS、binding metadata、diagnostic 无失败 |
| Vue 2.7 SFC compileTemplate | 100% | asset、scope、SSR template、source map 无失败 |
| Vue 2.7 SFC compileStyle | 100% | scoped、vars、modules、preprocess 无失败 |
| Vue 2.7 output contract | 100% | template compiler 与 SFC compiler schema/exact JS/diagnostic/source map/runtime 无失败 |
| Vue 3 compiler-core parse | 100% | parse snapshots 无 diff |
| Vue 3 compiler-core transform | 100% | transform tests 无失败 |
| Vue 3 compiler-core codegen | 100% | codegen exact compare、option matrix、output contract 无失败 |
| Vue 3 compiler-dom | 100% | DOM transform/codegen、option matrix、output contract 无失败 |
| Vue 3 compiler-sfc parse | 100% | descriptor/source map 无失败 |
| Vue 3 compileScript | 100% | macros、TS、binding metadata 无失败 |
| Vue 3 compileTemplate | 100% | asset、scope、HMR、SSR template 无失败 |
| Vue 3 compileStyle | 100% | scoped、vars、modules、preprocess 无失败 |
| Vue 3 compiler-sfc options/output | 100% | option matrix、output contract 无失败 |
| Vue 3 compiler-ssr | 100% | SSR codegen/runtime smoke、option matrix、output contract 无失败 |
| API manifest parity | 100% | Vue 2.6、Vue 2.7、Vue 3 所有官方包 `diff-api` 无未批准 diff |
| npm alias parity | 100% | Vue 2.6、Vue 2.7、Vue 3 所有官方测试可通过 alias 调用 Rust NAPI |

## 每周开发节奏

建议每周只接受满足以下格式的增量：

- [ ] 本周目标只属于一个 P 阶段，除非是修 CI。
- [ ] 每个 PR 只推进一个模块或一个官方测试子套件。
- [ ] PR 描述必须包含“本 PR 完成判定”。
- [ ] PR 合并前必须更新本文对应 checklist。
- [ ] 每周生成一次 conformance 趋势报告。

## 不允许的完成声明

以下情况不得标记完成：

- [ ] 只通过自写 fixture，没有跑官方测试。
- [ ] 只生成看似正确的 render string，没有 runtime 执行验证。
- [ ] 只处理 ASCII/简单表达式，没有 Oxc parse 和 span 映射。
- [ ] source map 不可回溯到原 `.vue` 文件。
- [ ] 通过新增 skip 或修改官方断言来“通过测试”。
- [ ] panic / todo 仍存在主路径。
