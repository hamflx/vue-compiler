# xtask

Internal development, conformance, benchmark, and release verification tasks for vuec.

This package is marked `publish = false`. It is used through `cargo xtask ...` from the repository root.
